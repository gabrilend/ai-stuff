# The RAM tiers.  tmp/ is a symlink to /tmp/<project>, and inside it
# shared-memory/ points at /dev/shm/<project>.  Both are recreated by the
# run scripts; neither belongs in the record.
tmp

# The shared secret.  Never in the record.
input/secret

# Where the program says goodbye.  A run artifact, not source.
output/goodbye
