# 053-test-context — info

Checks the atom context, and mostly checks the properties that make it worth having rather than the operations that are obviously right.

*Lifted from this file's own comments by `147`. To change this page,
change the comments in `053-test-context.lua` and run the sweep again.*

## Invocation

```
luajit 053-test-context.lua [--dir ROOT]
```

## What it describes

| Field | Value | |
|---|---|---|
| &nbsp;&nbsp;↳ `origin` | `"carried on the chip" }` |  |
| &nbsp;&nbsp;↳ `origin` | `"carried on the chip" }` |  |
| &nbsp;&nbsp;↳ `origin` | `"carried on the chip" }` |  |
| `topic` | `"the allocator I am writing", content = "a first draft", ...` |  |
| &nbsp;&nbsp;↳ `origin` | `"carried on the chip" }` |  |
| &nbsp;&nbsp;↳ `origin` | `"carried on the chip" }` |  |

