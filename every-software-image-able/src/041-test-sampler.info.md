# 041-test-sampler — info

Checks the sampler. Mostly it checks determinism, because that is the whole claim: the same image with the same carried numbers, given the same input, produces the same machine.

*Lifted from this file's own comments by `147`. To change this page,
change the comments in `041-test-sampler.lua` and run the sweep again.*

## Invocation

```
luajit 041-test-sampler.lua [--dir ROOT]
```

