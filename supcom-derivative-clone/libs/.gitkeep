Third-party code lives here, renumbered into the 900 band so a reader can skip a
contiguous range. ./install-dependencies fills it from the manifest in
input/dependencies; the copies are not committed. See docs/013-the-shape-of-the-code.md.
