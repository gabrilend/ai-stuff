# 027-an-opponent

Phase 9: the bot sees only the snapshot and its commands go through the door;
the overnight runner writes one line per match; difficulty is decision quality.

## What it claims

- Thinking on a snapshot does not touch the live world; every answer is a verb
  the door knows, for the bot's own team; the measuring bot is deterministic.
- Two bots in a headless match each place a factory with a pattern of at least
  two points within the first minute.
- The overnight runner writes one line per match to the RAM tier and says where.
- The difficulties are a table, and every one thinks from the same snapshot.

904 (a person against the bot) is played, not asserted.

## Subjects it loads

`the-bot` (901, 903), `overnight` (902), `headless-runner` (110), `snapshot`
(109), `the-world` (104), `commands` (108).
