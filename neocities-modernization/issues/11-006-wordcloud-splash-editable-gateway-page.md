# 11-006: Editable Splash/Gateway Page Before the Word Cloud

## Status
- **Phase**: 11 (Advanced Exploration)
- **Priority**: Medium
- **Type**: Enhancement
- **Status**: Open
- **Builds on**: 11-004 (explore pages), 11-005 (editable page templates),
  10-059 (word cloud is its own stage)

## Current Behavior

`wordcloud.html` is the de-facto menu/hub: most pages link to it as "Menu"
(e.g. `flat-html-generator.lua` emits `<a href="../wordcloud.html">Menu</a>`,
and `generate-word-pages.lua` links to it the same way). It is generated by
`src/wordcloud-generator.lua` (config `word_cloud.output_file = "wordcloud.html"`)
and overwritten on every build.

There is no introductory/landing page in front of it — a visitor lands directly
on the cloud with no framing text. The only editable, hand-written page copy in
the project today is the explore pages: `M.generate_explore_page` /
`M.generate_explore_math_page` in `src/flat-html-generator.lua` read their prose
from a template (`page-templates/explore.txt`, `explore-math.txt`) via
`page_template.render_file(template_path, values)`, filling `{PLACEHOLDER}`
markers with live numbers (Issue 11-005). Those templates are restored into the
ephemeral `input/pages/` by `scripts/sync-page-templates` at HTML-build time.

## Intended Behavior

A second editable page in the explore-page family — a **splash/gateway shown
before `wordcloud.html`** — whose words the author edits in a plain-text template,
exactly like the explore page. The machinery (filling live numbers, wrapping in
the site's shell) stays in Lua; the prose lives in a committed, editable file.

- New template: `page-templates/wordcloud-splash.txt`, carrying `{PLACEHOLDER}`
  markers for any live values the author wants (e.g. total word count, distinct
  word count, model name, generation date).
- Rendered to a stable output page (e.g. `output/wordcloud-splash.html`) that
  contains the author's framing text plus a clear "enter" link to
  `wordcloud.html`.
- The splash becomes the thing visitors reach FIRST: the entry points that
  currently link to `wordcloud.html` (the "Menu" links) point at the splash, and
  the splash links onward to `wordcloud.html`. (Which links redirect vs. stay is
  a placement decision — see Open Questions.)
- Same fairness/robustness rules as 11-005: unknown markers are a hard error, not
  a silent pass-through; a missing template is a loud failure.

## Suggested Implementation Steps

1. Add `page-templates/wordcloud-splash.txt` (committed canonical copy) with the
   author's draft prose and the markers it needs. `scripts/sync-page-templates`
   already copies `page-templates/*.txt` into `input/pages/`, so it will pick this
   up with no change — confirm its glob covers the new file.
2. Add a `M.generate_wordcloud_splash(output_dir, ...)` to
   `src/flat-html-generator.lua`, modeled directly on `M.generate_explore_page`
   (lines ~3127): build the `values` table of live numbers, call
   `page_template.render_file`, wrap with the site shell, write the output file.
   Reuse the same corpus-stats source the explore page uses for its counts.
3. Decide the live values to expose as markers (word counts come from the
   word-cloud word set / `word_embeddings.json`; date + model from the run).
4. Wire it into the word-cloud stage so it regenerates whenever `wordcloud.html`
   does — call it alongside the word-cloud generation in the stage that owns
   `wordcloud.html` (`src/wordcloud-generator.lua` / the word-cloud stage from
   10-059), or from the HTML stage next to `generate_explore_page`.
5. Repoint the "Menu" links (`../wordcloud.html`) to the splash where the splash
   should be the first stop, and add the splash's onward link to `wordcloud.html`.
6. Add any config the splash needs (output filename, whether it is enabled) to the
   `word_cloud` section of `config.lua`.

## Related Documents / Tools

- `src/flat-html-generator.lua` — `M.generate_explore_page` (the pattern to copy),
  the `../wordcloud.html` "Menu" links to repoint.
- `libs/page-template.lua` — `render_file` (marker substitution; errors on unknown
  markers).
- `scripts/sync-page-templates` — restores `page-templates/*.txt` into `input/pages/`.
- `src/wordcloud-generator.lua`, `config.lua` `word_cloud` section — the word-cloud
  stage and its config.
- Issue 11-005 (editable templates), 11-004 (explore pages), 10-059 (word-cloud stage).

## Open Questions

- Placement: should EVERY "Menu" link land on the splash first, or only the
  top-level entry points (so in-flow navigation still jumps straight to the cloud)?
- Should the splash be skippable/remembered (e.g. only shown once), or always the
  gateway? (Static site: "remember" would need a cookie/localStorage snippet.)
- Output filename and whether `wordcloud.html` itself should be renamed so the
  splash can take the canonical "menu" URL.
