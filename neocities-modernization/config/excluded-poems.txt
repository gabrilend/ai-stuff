# Poems to Exclude from the Collection
# =====================================
#
# This file specifies poems to exclude during extraction.
# Excluded poems leave gaps in the ID sequence (tombstoning) -
# they don't shift other poem IDs down.
#
# Format:
#   - Lines starting with # are comments
#   - Blank lines are ignored
#   - Category headers end with a colon (e.g., "fediverse:")
#   - IDs are listed one per line under their category
#
# ID Formats by Category:
#   notes:     Filename without extension (e.g., "what-a-lame-movie")
#   fediverse: Numeric post ID from ActivityPub (e.g., "113847291038475")
#   messages:  Numeric message index (e.g., "42")
#   bluesky:   AT Protocol record key (e.g., "3k...abc")
#
# Finding poem IDs:
#   - Browse chronological.html - each poem shows its source ID
#   - Search poems.json: jq '.poems[] | select(.content | contains("keyword"))'
#   - Grep generated HTML for content snippets
#
# Example:
#   fediverse:
#   113847291038475
#   113902847102938
#
#   notes:
#   test-post-please-ignore
#   mundane-grocery-list

# ─────────────────────────────────────────────────────────────────────────────
# Add your exclusions below, organized by category
# ─────────────────────────────────────────────────────────────────────────────

fediverse:
# (add fediverse post IDs here)
# Example: 0042

notes:
# (add note filenames here, without extension)
# Example: test-post-please-ignore

messages:
# (add message indices here)
# Example: 0001

bluesky:
# (add bluesky record keys here)
