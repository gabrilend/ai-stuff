# Conversation Summary: ff171126-9f2c-4ac2-a615-3e15afe50761

Generated on: 2026-08-09 18:19:05

--------------------------------------------------------------------------------

### User Request 1

hi, can you give me a report on the easy issue files we could work on? I'm
specifically focused on one where the "chronological" links are broken, can you
find it?


--------------------------------------------------------------------------------

### Assistant Response 1

I'll look through the project's issue files. Let me start by getting the lay of
the land.


Let me check the actual generated site to see whether these links currently
resolve.


Now let me survey the open issues for size/effort so I can rank them.


I found it, and I confirmed it's live in the current build. Here's the report.


---


## The chronological-link issue you're after


**`issues/completed/10-036-fix-word-page-chronological-links.md`**


It's filed as **COMPLETED (2026-03-23)**, but the behavior it promises is broken
again — and broken in a *different* place than the one it fixed. This is why
grepping for an open issue about it turns up nothing.


That issue is the project's blueprint for "a poem's `chronological` link must
land on the chronological page that actually contains that poem." Your
`next-issue-please-sort` scratch file records the symptom in your own words:


> *"clicking 'chronological' takes you to chronological page 1."*


## Confirmed against the 2026-06-29 build


| Page family | Chronological links | Where they point |

|---|---|---|

| `output/similar/*.html` | 694,530 | **100% → `chronological/01.html`** |

| `output/different/*.html` | all found | **100% → `chronological/01.html`** |

| `output/wordcloud/*.html` | many | correct spread — pages 19, 20, 21, 31, 35, 37, 44–47, 52, 56, 59, 72, 73, 80, 82, 83 … |


The chronological pages themselves are fine: 90 pages at 88 poems each (built
with `--chrono-per-page 88`), and `01.html`'s "next" goes to `02.html`, which
exists. So the *targets* are healthy — it's the *pointers* on the
similar/different pages that all collapse to page 1. A link like
`chronological/01.html#poem-7320` asks the browser for an anchor that lives on
page 83.


The word-cloud half of 10-036's fix is still holding. Only the similar/different
half regressed.


## Root cause — an argument that stops being passed, four calls deep


The mapping is computed correctly and then dropped on the floor:


- `src/flat-html-generator.lua:3479` — `chrono_mapping` is built correctly
  (poem index → page number), before the parallel/sequential split, exactly as
  10-036 specified.

- `src/flat-html-generator.lua:4415` and `:4452` — the sequential path calls
  `M.generate_all_paginated_pages_for_poem(...)` with 6 arguments.

- `src/flat-html-generator.lua:2594` — that function's signature has **no
  `chrono_mapping` parameter at all**. It was never updated by 10-036.

- `src/flat-html-generator.lua:2631` — it calls
  `M.generate_paginated_poem_page_html(...)` with 7 arguments; that function's
  8th parameter (`:2500`) *is* `chrono_mapping`, so it arrives `nil`.

- `nil` then flows into `format_all_poems_with_progress_and_color` (`:2333`) →
  `format_single_poem_with_progress_and_color` (`:2116`) → and hits the
  fallback at `:2151-2152`:


```lua

local chrono_info = chrono_mapping and chrono_mapping[poem_index]

local chrono_page = chrono_info and string.format("%02d", chrono_info.page_number) or "01"

```


Every poem gets `"01"`.


## Why 10-036 thought it was safe


Its "Code Path Analysis" says *"Production Path (correct before fix) —
Parallel workers … already had correct pagination logic."* That was true then.
The worker at `:3742` still has correct logic. But **that worker never runs
anymore**:


```lua

-- :3482

local use_parallel = num_threads > 1 and has_threading and effil

```


I checked — `require("effil")` fails on this machine. Issue 9-001f removed the
effil dependency and 9-001f1 (pthreads replacement) is still **In Progress**. So
the correct chronological-link code now lives exclusively in a branch that can't
execute, and the branch that *does* execute is the one 10-036 classified as
"acceptable fallback."


`★ Insight
─────────────────────────────────────`

**Two code paths, one maintained.** 10-036 was written when parallel was the
production path, so it fixed the archive/sequential path only enough to compile
and left the "01" fallback in as a compatibility cushion. When effil vanished,
the untested path became the *only* path — and its cushion silently became the
product. This is precisely what your CLAUDE.md warns about: *"consider fallbacks
as warnings, and consider warnings as errors."* A hard error at `:2151` instead
of `or "01"` would have stopped the build the first time effil went missing,
rather than shipping 694,530 wrong links.


**This is also exactly the failure 8-058 is filed to prevent** — "eliminate
main thread / worker code duplication." Two copies of the same formatting logic
drifted, and the drift was invisible because only one copy was being read.

`─────────────────────────────────────────────────`


## Fix size: small


Two function signatures and two call sites, all in one file:


1. Add a `chrono_mapping` parameter to `M.generate_all_paginated_pages_for_poem`
   (`:2594`) and forward it at `:2631`.

2. Pass `chrono_mapping` at the two sequential call sites (`:4415`, `:4452`) —
   the variable is already in scope from `:3479`.

3. Replace the `or "01"` fallback at `:2152` with a hard error, so this can't
   regress silently a third time.


Per your rule that bugfixes don't need new issue files: the right move is to fix
it and update 10-036's *Current Behavior* to reflect that the sequential path is
now the production path.


---


## A larger finding right next door


While measuring the above, I found something worse on the same page family:


| | pages with **any** `href` | total pages |

|---|---|---|

| `output/similar/` | 7,835 | 7,904 (99%) |

| `output/different/` | **48** | 7,904 (**0.6%**) |


`output/different/0001-01.html` is 366 KB of poem content with **zero links**
— no per-poem `similar` / `chronological` / `different` nav boxes at all, and
its `[◀ Previous Page]` / `[Next Page ▶]` markers are inert plain text (the
page is "Page 1 of 1"). The 48 pages that *do* have nav look normal (85
chronological + 85 similar + 85 different links each).


So roughly 7,856 "different" pages are dead ends — reachable, but with no way
out. I have not diagnosed this one; it's a separate mechanism from the
chronological bug. Flagging it because it's adjacent and it's a much bigger hole
in the site than the page-1 problem.


---


## Easy issues you could actually finish


Ranked most-likely-to-succeed first.


**1. `10-042d-gallery-chronological-image-list` — already done, just needs
filing.**

The issue says the gallery's "chronological" link "does not lead to an
images-only chronological view." I checked:
`src/generate-gallery-pages.lua:411-491` builds it,
`output/gallery/chronological.html` exists with 642 images, the nav link at
`:336` is wired, and the colon-joined titles (`my-art: my-view.png`,
`poem-pictures: two-perspectives-is-better-than-one.png`) appear twice each per
the spec. This is a move-to-`completed/` and a progress-file update, nothing
more.


**2. `10-063-view-archived-html-pages-in-source-browser`** — status already
reads *"Implemented (2026-06-26) — verified by syntax + classification
tests."* 65 lines. Same situation: verify and file.


**3. `10-062-chronological-image-viewer-reverse-and-pagination`** — 67 lines,
marked Low priority / deferred. Split one existing image list into 10 pages, add
a `1 2 3 … 10` jump bar and a forward/reverse toggle. Self-contained in one
generator file, no cross-process coordination. The sibling issue 10-064
explicitly puts the gallery out of its own scope, so nothing collides.


**4. `10-060-retire-untethered-dead-code`** — step 1 (delete the
mass-diversity cluster) is done; steps 2 and 3 are deferred and carry real risk
(the issue documents a previous deletion that broke embedding generation for a
whole regeneration). Easy to *close the first step*; the rest should stay
deferred.


**5. `11-006-wordcloud-splash-editable-gateway-page`** — 88 lines, and it
explicitly names the function to copy (`M.generate_explore_page`, ~line 3127)
and the template mechanism (`page_template.render_file`). Mostly mechanical.
**But it has three unanswered Open Questions** about link placement and
filenames, so by your own rule it isn't startable until you answer them.


**6. `10-056-phase-demos-compute-stats-and-show-real-features`** — 82 lines,
no new subsystems. Replace hardcoded `echo` statistics (e.g. `"6,860+ poems"`
when the corpus is ~7,900) with live computations from the existing
`scripts/validate-*` family. Tedious rather than hard, and it touches the
deliverable directly.


Everything else in the open set is Medium or above: the phase 13
TTS/Stable-Diffusion/video chain, phase 16 Android/torrent work, the maze system
(11-002*), and the GPU/threading issues (9-001f1, 8-058, 9-007).


---


## Open questions I need from you


1. **Do you want the chronological-link fix now, or just the report?** I've
   located it precisely but haven't touched any code.

2. **The `different/` dead-end pages** — should I dig into that too? It's a
   bigger break than the page-1 problem and I don't yet know its cause.

3. **`11-006`'s three open questions** (does every "Menu" link land on the
   splash, or only top-level entry points; is the splash skippable; does
   `wordcloud.html` get renamed) — that issue is blocked on your answers.

4. **Is `9-001f1` (pthreads HTML generation) still the plan?** Its "In Progress"
   status is what makes the parallel worker unreachable. If pthreads is coming,
   the sequential path is temporary and the fix above is a stopgap. If it isn't,
   the parallel worker at `:3742-3779` is dead code and 8-058's deduplication
   becomes the real fix.


--------------------------------------------------------------------------------

### User Request 2

Oh um can we work on that?


--------------------------------------------------------------------------------

### Assistant Response 2

Reading "that" as the chronological links — your stated focus. I'll take the
`different/` dead-end pages next; say the word if you'd rather flip the order.


Let me read the code I'm about to change.


Now I'll make the fix. First, the link builder itself — the place where the
wrong page number is chosen.


Now the two functions that were dropping the mapping entirely — this is the
actual break.


Now the two call sites in the sequential path — where `chrono_mapping` was
already in scope but never passed.


Now a test — this regression was an argument silently not passed, so the test
needs to assert the plumbing end-to-end.


Let me prove the test actually fails on the old code — a test that passes both
ways is worthless.


Space freed. Now the regression proof — calling the function with the *old*
6-argument shape, exactly as the old call sites did:


No test runner exists yet — issue 10-010 proposes `scripts/run-tests` but it's
still open, so this joins the other standalone `.test.lua` files in `src/`.


Now updating the issue file. Its "Code Path Analysis" is the claim that let this
regress, so it needs correcting rather than appending to.


Done. Here's what changed and what's still outstanding.


## The fix


The mapping from poem → chronological page was being computed correctly and
then silently dropped. `M.generate_all_paginated_pages_for_poem` had no
parameter for it, so it forwarded 7 of the 8 arguments the next function
expects; the 8th arrived `nil` and the formatter's `or "01"` default took over.


Threaded the mapping through the whole sequential chain in
`src/flat-html-generator.lua` — `generate_all_paginated_pages_for_poem` →
`generate_paginated_poem_page_html` →
`format_all_poems_with_progress_and_color` →
`format_single_poem_with_progress_and_color` — plus the two archive functions.


Two things beyond plain threading:


**A second latent bug in those same three lines.** The old code always emitted
`chronological/NN.html`, but the chronological writer only produces numbered
files when pagination is on; unpaginated builds write *only*
`chronological/index.html`. Since `config.lua` ships `chronological_paginated =
false` and real builds turn it on via `--chrono-per-page`, the committed default
was a mode where every link named a file that was never written. The link now
follows the same branch the writer uses, which is why `chrono_paginated` had to
be threaded alongside the mapping — it can't be read from config, because
`--chrono-per-page` enables pagination at runtime without touching the config
table.


**The `"01"` guess is gone.** A missing mapping now falls back to `index.html`
— the one file that exists in both modes, costing only the `#poem` anchor —
and logs one warning per process naming why.


```

[WARN] chronological links fell back to chronological/index.html (no chronological

mapping reached this generator) - the #poem anchor is lost across that redirect...

```


`★ Insight
─────────────────────────────────────`

**Why the fallback was the actual bug.** `or "01"` produced output
indistinguishable from correct output. A hard error would have stopped the first
build after effil disappeared; instead the guess was right for the ~1% of poems
genuinely on page 1, and wrong for the other 7,800 in a way nothing could see
without cross-referencing anchors against page contents.


**Warn-once is deliberate, not laziness.** The check sits inside the per-poem
formatter, which runs ~700,000 times per build. A warning per occurrence would
bury itself. The flag is process-global and the message names *which* way the
mapping failed, so the caller that dropped it is findable without re-deriving
the chain.

`─────────────────────────────────────────────────`


## The test


`src/flat-html-generator.chronological-links.test.lua` — 18 cases, all
passing. It asserts the *plumbing*, not the formatting: poems mapped to pages 2,
7, 41, 83 and 90 must produce five distinct link targets. If any function in the
chain stops forwarding, they collapse and the test fails.


I verified it actually catches the regression rather than passing both ways —
calling `generate_all_paginated_pages_for_poem` with the old 6-argument shape
(byte-identical to what the old call sites did) collapses all links to a single
target, failing two assertions.


It also covers the cases that had no coverage: zero-padding (`07.html`, not
`7.html`), the unpaginated mode, a missing mapping, and a poem absent from the
mapping.


## Test suite state


I ran everything. 12 pass including the new one. Two fail, **both pre-existing
and unrelated** — neither requires any module I touched:


- `src/augment-embeddings-with-images.test.lua` — 14 passed, 2 failed on
  class-2 neighbour-averaged embeddings. That's the redesign issue 9-013 is
  currently reopened for.

- `libs/hope-card-formatter-test.lua` — 2 failures in the hope-card formatter.


I left both alone as out of scope.


## Not done


**The live site still has the old links.** The fix is in the generator;
`output/similar/` and `output/different/` were built 2026-06-29 and nothing has
regenerated them. Until an HTML rebuild runs, all 694,530 links still point at
page 1.


**I have not committed anything.** Your rule is that I ask first when the tree
has modified files that aren't mine — `llm-transcripts/jul-11-26.md`,
`jul-19-26.md`, and an untracked `aug-5-26.md` are modified and aren't my work.
Mine are exactly four files: the generator, `main.lua`, the new test, and the
issue file.


## Open questions


I put these in the issue file rather than closing it out, since it isn't
finished while they're unanswered:


1. **Regenerate now, or fold into the next build?** A full HTML rebuild is
   expensive and I don't know your schedule.

2. **Should the parallel worker's duplicate copy be deleted?** It's unreachable
   while effil is gone, but it still contains a second implementation of this
   exact logic. Keeping it means every change here gets made twice; deleting it
   means 9-001f1 rebuilds it from this one. This is 8-058's territory and it's
   the structural reason the bug was possible.

3. **Is `chronological_paginated = false` still the intended config default?**
   Real builds pass `--chrono-per-page`, so the committed default is the mode
   nobody runs — which is precisely how the unpaginated branch went untested
   long enough to grow a bug.


Want me to commit these four files, and shall I take the `different/` dead-end
pages next? That one's still unexplained — 7,856 of 7,904 pages have no links
at all.


--------------------------------------------------------------------------------

### User Request 3

  2. Should the parallel worker's duplicate copy be deleted? It's unreachable
     while effil is gone, but it still contains a second implementation of this
     exact logic. Keeping it means every change here gets made twice; deleting
     it means 9-001f1 rebuilds it from this one. This is

  8-058's territory and it's the structural reason the bug was possible.


Um, how are we handling parallelization now if effil is removed?


  Want me to commit these four files, and shall I take the different/ dead-end
  pages next? That one's still unexplained — 7,856 of 7,904 pages have no
  links at all.


oh yes please. Though, I think you're wrong. The pages on the live website seem
to have the correct links on the different pages...?


also, this thing here:



Poetry Collection


Poems sorted by similarity to: Poem 6762


Download full collection: [.txt] [.html]


we should resize it instead of "download full collection" we should "download
these page(s)" with the s added if there's more than one, but not if there's
only one page.


I think the main problem is if you click this link:


similar-different/chronological/01#poem-527


which is the "chronological" link on the fediverse/503 poem, it takes you to
page 01 and wants to scroll to poem 527. But, poem 527 is not on page 1, it's on
page... whatever. I think that's the initial issue I requested a fix for.


--------------------------------------------------------------------------------

### Assistant Response 3

Good questions — let me check both before I answer, starting with
parallelization since I asserted something I only half-verified.


You're right and I was wrong. My measurement was broken, not the pages.


Corrected: **all 7,904 pages in both trees have their navigation.** No dead ends
— my grep was silently skipping these files as binary. The real number for the
chronological bug is larger than I reported: 694,530 in `similar/` plus 670,724
in `different/`, about 1.37 million links all aimed at page 1.


Let me find the byte that made grep call these binary, since that's a genuine
(if smaller) problem.


I have the answers. Now the download-links change you asked for.


Committing now. Your rule is one commit per concern, so I'll split the
chronological fix from the label change.


Exactly my four files staged; everything else left alone.


Now re-applying the download label change as its own commit.


Both committed. Taking your points in order.


## You were right about the `different/` pages


They're fine. All 7,904 of them have their navigation, same as `similar/`.


My measurement was broken, not the pages. These HTML files contain a stray
invalid byte, so `grep` classified them as binary and silently reported nothing
instead of matching. Without `-a` it found 48 files; with `-a` it finds all
7,904. There is no dead-end problem and nothing to investigate there.


The correction cuts the other way on the real bug — it's bigger than I said.
`different/` contributes another 670,724 links pointing at page 1, so the total
is about **1.37 million**, not 694,530.


## How parallelization works now: it doesn't


You're right to poke at this. Here's the actual state.


**effil** was a Lua threading library that gave each thread its own interpreter
state and moved data between them through shared tables. `require('effil')`
fails on this machine, so `has_threading` is false, so `use_parallel` is false,
so HTML generation runs **single-threaded in one luajit process**. Passing
`--threads 8` to the HTML stage does nothing except trigger a warning that says
so.


The replacement was built and never plugged in:


| Piece | State |

|---|---|

| `libs/html-threaded/build/libhtmlgen.so` | compiled 2026-03-18 |

| `scripts/generate-html-pthreads` | written 2026-03-18 |

| Anything that calls either | **none** |


`run.sh` stage 9 runs `luajit src/main.lua --html-only`, and neither `main.lua`
nor `flat-html-generator.lua` mentions the library. The script's only referrer
is itself.


Worth noting the design shift, because it changes what "parallel" even means
here: the effil approach ran *page generation* on many threads. The pthreads
approach keeps generation sequential in Lua — LuaJIT is genuinely fast at
string assembly — and hands only the *file writing* to native threads through
FFI. It's parallel disk I/O, not parallel rendering. That's a much smaller and
safer surface, which is probably why it was designed that way after effil kept
getting the process killed.


Other stages do still parallelize: GPU similarity runs through
`libvkcompute.so`, and `--threads` feeds its CPU-side sorting.


`★ Insight
─────────────────────────────────────`

This is the direct answer to open question #2. The parallel worker inside
`flat-html-generator.lua` isn't waiting to be reactivated — the replacement
architecture doesn't need it, because pthreads only writes files and never
formats a poem. That worker is a copy of the *old* design's rendering logic, and
the new design has no place to put it. Deleting it removes the duplication that
let this bug exist, and 9-001f1 loses nothing.

`─────────────────────────────────────────────────`


## The chronological link — yes, that's the one


`similar-different/chronological/01#poem-527` where poem 527 isn't on page 1.
Exactly it, and exactly the mechanism I found: the page number was computed
correctly at the start of the build and dropped in transit, so the renderer
filled the gap with a guess of "01".


Fixed and committed, with the 18-case test. The guess is gone — where the
position is known the link names the page, and where it isn't, the link goes to
the timeline's front door and the build says so once out loud.


## Download links


Done, committed separately. `Download this page:` / `Download these pages:`,
driven by the same page total the navigation uses.


One thing I want to flag rather than paper over: the two files being linked are
still **whole-collection** exports, not per-page ones. So the new label promises
something narrower than what actually downloads. I implemented the wording you
asked for and noted the discrepancy in the commit — either the exports become
per-page, or the wording has to admit it's the collection. Your call which.


I also skipped adding a page count ("Download these 12 pages") since you asked
only for the `s`; say the word if you want the number in there.


## What that stray byte actually is


This is the real finding hiding behind my bad grep, and I think it's connected
to something already in your notes.


A line-splitting step is cutting multi-byte characters in half. `─` is three
bytes (`E2 94 80`); the split lands between `E2 94` and `80`, leaving an orphan
continuation byte. Here's a golden-poem frame from `different/0001-01.html`
where the top border got wrapped:


```

 ╭─────────────────────────

 ─────╮

 │ trans-rights-are-human-rights │

```


Sampled 200 files from each tree — not exhaustive, so treat these as rates,
not totals:


| | invalid UTF-8 |

|---|---|

| `different/` | 200 of 200 |

| `similar/` | 1 of 200 |


The asymmetry makes sense: difference pages gather the *most unlike* poems, so
they collect the odd long-titled ones far more often than a similarity page full
of near-duplicates.


The reason I think this matters beyond tooling: it's a byte-count where a
character-count belongs, and that's the same arithmetic error behind your own
note — *"progress bars are fucked"*, *"sometimes progress bars are too
long?"*, *"the content warning is wrapped weirdly."* A bar drawn with `═`
(also three bytes) will come out roughly three times the intended width, or
truncated mid-character, if the code measures the string in bytes.


I haven't found which function does the slicing yet — I stopped at the
evidence rather than guessing. Want me to chase that next? It's one root cause
with several visible symptoms, which is a good trade.


--------------------------------------------------------------------------------

### User Request 4


  One thing I want to flag rather than paper over: the two files being linked
  are still whole-collection exports, not per-page ones. So the new label
  promises something narrower than what actually downloads. I implemented the
  wording you asked for and noted the discrepancy in the

  commit — either the exports become per-page, or the wording has to admit
  it's the collection. Your call which.


um, that doesn't seem right. When I click on one it takes me here:


similar-different/similar/similar/4355.txt


that tells me it's just 4355.txt? which also seems wrong. Why is it
similar/similar/4355.txt as well? That double nesting is suspicious. Also, the
link is dead, it doesn't actually download anything. BUT it should download the
similar page for 4355 as a .txt file. Not the entire collection, and not...
nothing... which is what it does now.


--------------------------------------------------------------------------------

### Assistant Response 4

Let me look at what's actually on disk and how that URL gets built.


Found it — two independent defects, either one alone would kill the link.


Now I have the full picture. Let me fix it.


Let me sanity-check the naming against real data on disk.


The comment that misled me is in the source. Fixing it, per your rule about
tracing a mistake back to the documentation that caused it.


Fixed and committed. You were right to push on this — there were three faults
stacked, and my earlier flag was wrong.


## First, retracting what I flagged


The `.txt` is **not** a whole-collection export. I checked the actual file:
`fediverse-4544.txt` holds 90 poem entries, and the page shows 89 plus the
anchor. It's exactly the page's content, unsplit.


I believed a comment in the source that said `-- Generate TXT version (full
corpus export - not paginated)`. It's wrong. I've corrected that comment and its
three siblings, since a wrong note costs more than no note. So `Download this
page:` is accurate wording, and nothing needs to change about what gets
exported.


## Why the link was dead — three separate faults


**1. Two numbering schemes, mixed together.** This is the interesting one.


| Artifact | Named by | Example |

|---|---|---|

| HTML page | `poem_index` — one sequence across the whole corpus | `similar/4355-01.html` |

| TXT export | category + `id` — numbering restarts per archive | `similar/fediverse-4544.txt` |


`similar/4355-01.html` is the page for **fediverse poem 4544**. Same poem, two
different numbers. The link builder took the page's number (4355) and dropped it
into the export's naming slot, producing `4355.txt`. That file doesn't exist —
and `fediverse-4355.txt` *does* exist, belonging to an entirely different poem.


The category prefix exists on purpose: issue 8-019 added it because
`fediverse/0002` and `messages/0002` both have id 2 and would otherwise
overwrite each other. The link builder never learned about it.


**2. The double nesting you spotted.** The link was `similar/4355.txt`, emitted
on a page that already lives at `similar/4355-01.html`. Browsers resolve
relative URLs against the current document's *directory*, so `similar/` +
`similar/4355.txt` = `similar/similar/4355.txt`. The poem navigation links dodge
this by going up first (`../similar/...`); the download link didn't. The export
is a sibling of the page, so it's now addressed by bare filename.


**3. The `.html` link could never work.** `generate_html_archives = false` ships
in config ("redundant with paginated pages"), so no `-archive.html` file is ever
written — there are zero of them in `output/`. The link was emitted
unconditionally anyway. It's now only offered when archives are actually
generated, following the rule issue 10-055 already states for the source
browser: skip it, don't emit a dead link.


`★ Insight
─────────────────────────────────────`

**The writer and the advertiser were reading different sources.** The code that
*writes* the export calls `get_unique_poem_filename_id`; the code that *links*
to it rebuilt the name from scratch with a different formula. Two
implementations of one fact, and only one of them was ever checked against the
disk.


The fix isn't "correct the formula" — it's "delete the second formula." The
link now calls the same function the writer calls, so they can't disagree. This
is the same shape as the chronological bug: one value derived in two places,
drifting silently.

`─────────────────────────────────────────────────`


## Committed


Three commits now:

- `f86d903a` — chronological links aim at the right page (18 tests)

- `eeef9cdf` — download label wording

- `5bea3780` — download links point at files that exist (10 tests)


Full suite: everything passes except `augment-embeddings-with-images.test.lua`,
which was already failing before I started and belongs to the reopened 9-013
image pseudo-embedding redesign.


## Still outstanding


**None of this shows until a rebuild.** The generator is fixed; `output/` is
still the 2026-06-29 build with all the broken links in it.


**The UTF-8 wrap bug** — still unclaimed. A line-splitting step cuts
three-byte box characters in half (`─` = `E2 94 80`, split between `94` and
`80`). 200 of 200 sampled `different/` pages are invalid UTF-8. I think it's the
same byte-vs-character arithmetic behind your "progress bars are fucked" and
"content warning wrapped weirdly" notes — one cause, several symptoms. I
haven't found the function yet.


Want me to chase that, or run a rebuild first so you can see the link fixes
live?


--------------------------------------------------------------------------------
