
hello claude. can you create a roadmap plan document that details the steps we'd need to create a 3D rts game in C with raylib and pthreads? Something very simple, with just rectangle boxes throwing cylinder javelins at each other. The units should move across a surface that
has different height values, and the javelins should only be thrown at units in line of sight. The projectiles should not alter their course or momentum once thrown. Each time a unit misses, the next time it shoots at that target there will be an increasing amount of variance in
it's aiming location, which is calculated using just X/Y values but is thrown toward the point those X/Y values intersect with the terrain. We should have a button we can click on in the interface, which will allow us to place a building that produces more of these rectangles
every 10 seconds or so. When we click on the factory, we can move the rally point around by dragging it. The point will only drag on the X/Y plane but will render at the position on the Z axis that intersects with the terrain. If we box-select units, we can move them about. They
will fire while moving. If we hold shift, we can make multiple movement orders for them, and if we hold shift we can add additional rally points to the factory. Each time we push shift, while it's held down it'll build a "chain" of rally points / movement orders. The units will
move along these one after another until they reach their destination. If the user releases the shift key, then placing a rally point or movement order with the right click will overwrite and erase all the previous orders, and just have that one. If the user pushes shift again,
they can create a new chain. For factories, every other unit will go down a different chain until the last one has been picked - then it will restart from the top. They won't be shuffled. For units, every new chain that's created (with a new shift key-press-then-held-down) the
selected units will split in half and half of them will go to one chain and the other half will go to the other. Can you make a document for these requirements and design goals? We will add resources and such later. This phase can be called "basic movement options" and we will
later get to "resources" and "advanced movement options" which includes things like patrol, attack move, and others.
