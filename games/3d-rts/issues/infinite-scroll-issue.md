we should make it so the terrain map can scroll infinitely.
I mean pan. The world should wrap so that we can scroll off the top and find the
bottom of the map, and the top left corner should point to the bottom right
corner.
