# 033-porch-test — proof for the porch

Runnable directly (`luajit src/033-porch-test.lua [project-dir]`).
Proves: the grammar is the same words twice and speaks every hue,
fade, and easing with times structurally tenths; the prompt carries
the contract and the vision translation; the collector sorts by
declared time with arrival breaking ties; the full loop against a
scripted fake orchestrator corrects a misspelled hue in exactly two
attempts with the wall's teaching quoted back between them; the
person's sentence rides as a comment; exhausted patience surfaces
errors beside the best draft; the cluster roster parses purely and
a missing file refuses politely. No cluster required anywhere.
Exits nonzero on failure.
