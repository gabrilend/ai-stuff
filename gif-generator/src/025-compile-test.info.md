# 025-compile-test — proof for the compiler's wall

Runnable directly (`luajit src/025-compile-test.lua [project-dir]`).
Proves: both reference scores compile (the vision to its six
tracks, hues seated in first-use order, capacity sized); refusals
name their stroke and teach the nearest word (ebmer→ember,
strok→stroke, rte→rate); tenths discipline and canvas-length
discipline hold; thirty fps is refused with its reason; turnless
arcs, ghost tips, fill tips, self-borrowing circles, and duplicate
names are refused; three mistakes arrive together as three; and
borrowing forward in time compiles, because endpoints are geometry,
not history. Exits nonzero on failure.
