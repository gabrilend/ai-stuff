# 076-token-flow — info

*Generated by `096` from the declarations in `076-token-flow.md`. Do not edit this page;
edit the blueprint and run the sweep again.*

**The dataflow of one pass** — phase 11.
Described by `1102`.

## What it publishes

| symbol | unit | kind | value | meaning |
|---|---|---|---|---|
| `C_qkv` | MB | derived | 43.2538 MB | one layer's query, key and value projections |
| `C_proj` | MB | derived | 34.603 MB | its projection back |
| `C_ffn` | MB | derived | 363.332 MB | its feedforward |
| `C_inter_layer` | MB | derived | 0.106496 MB | intermediates inside one layer that never leave the die |
| `C_kv_token` | MB | derived | 0.002048 MB | cache one layer writes for one token |
| `C_kv_read` | MB | derived | 8.38861 MB | and reads back, at full context, per layer per token |
| `B_weight_tok` | MB | derived | 36376.4 MB | weight traffic per token: every weight once |
| `B_kv_seq` | MB | derived | 671.089 MB | cache traffic for one token of one sequence, at full context |
| `B_kv_tok` | MB | derived | 18790.5 MB | and across the whole batch, which is a different number and is the one 079 uses |
| `B_act_tok` | MB | derived | 2.75251 MB | activations crossing between stages |
| `f_kv_traffic` | 1 | derived | 0.516557 | the cache's traffic against the weights', which is the ratio that changes character with context |
| `n_ctx_cross` | 1 | derived | 7929.43 | the context length at which the two are equal |
| `f_act_traffic` | 1 | derived | 7.56675e-05 | the handoff's share, which 050 asserts is negligible and this is where the number comes from |
| `C_handoff` | MB | derived | 0.0764587 MB | one staging buffer's worth: a microbatch of activation vectors |

## What it consumes

| symbol | from | value | meaning |
|---|---|---|---|
| `C_activation` | `078` | 0.016384 MB | one token's activation vector, which is what crosses between stages |
| `C_face_slice` | `047` | 1157.79 MB | and of a whole face's |
| `C_layer_weights` | `078` | 441.188 MB | one transformer layer's share |
| `C_weights` | `078` | 36.3764 GB | the weights, resident, at the format in 046 |
| `batch_design` | `047` | 28 | the batch the machine is provisioned for; 079 derives the crossover independently and C-079-1 requires the two to agree |
| `d_ff` | `078` | 28672 | the feedforward's inner width |
| `d_head` | `078` | 128 | width of one head |
| `d_model` | `078` | 8192 | the residual stream's width |
| `n_ctx` | `078` | 4096 | context length the machine is provisioned for |
| `n_head` | `078` | 64 | attention heads |
| `n_kv_head` | `078` | 8 | key and value heads, fewer than query heads so the cache is smaller |
| `n_layer` | `078` | 80 | transformer layers |
| `n_microbatch` | `053` | 6 | microbatches in flight above the crossover, one per stage |
| `n_stage` | `010` | 6 | pipeline stages a token falls through, one per face |
| `w_act` | `046` | 16 bit | activations in the residual stream |
| `w_kv` | `046` | 8 bit | key and value cache entries |
| `w_weight_eff` | `046` | 4.125 bit | bits per weight including the amortised scale, which is the number that decides how long a token takes |

## What consumes it

Change one of these and the blueprints beside it are what break.

| symbol | read by |
|---|---|
| `C_qkv` | `076` |
| `C_proj` | `076` |
| `C_ffn` | `076` |
| `C_inter_layer` | `076` |
| `C_kv_token` | `076` |
| `C_kv_read` | `076` |
| `B_weight_tok` | `076` |
| `B_kv_seq` | `053`, `055`, `061`, `076`, `080` |
| `B_kv_tok` | `053`, `076`, `079` |
| `B_act_tok` | `076` |
| `f_kv_traffic` | `076` |
| `n_ctx_cross` | `076` |
| `f_act_traffic` | `076` |
| `C_handoff` | `050`, `055`, `080` |

## What it asserts

| tag | relation | because |
|---|---|---|
| `C-076-1` | `C_qkv + C_proj + C_ffn ~= C_layer_weights` | the tensors of one layer must account for the layer. Two routes to the same megabytes: 078 derives it from a parameter count and this derives it from the tensors themselves, and a shape error in either shows up here |
| `C-076-2` | `f_act_traffic < 0.001` | activations crossing between stages must be under a thousandth of the weight traffic, which is the number 050 asserts when it argues that a mesh would be carrying nothing |
| `C-076-3` | `C_inter_layer < C_face_slice` | intermediates inside a layer must fit alongside the weights on a face, since they never leave the die and have nowhere else to be |
| `C-076-4` | `n_ctx_cross > n_ctx` | the context at which cache traffic overtakes weight traffic must be beyond the reference context. Past that point the machine is a different machine, and this is the only place in the project that says where the point is |
| `C-076-5` | `f_kv_traffic < 1` | at the reference context the weights still dominate, which is what makes every performance claim in 080 a claim about weight bandwidth |

## What it draws

- **one layer, one token, one sequence [not-dimensioned]** — dimensions `C_layer_weights`, `C_activation`

