# 076a-the-sieve-in-reverse — info

*Generated by `096` from the declarations in `076a-the-sieve-in-reverse.md`. Do not edit this page;
edit the blueprint and run the sweep again.*

**The sieve, run backwards** — phase 11.
Described by `1107`.

## What it publishes

| symbol | unit | kind | value | meaning |
|---|---|---|---|---|
| `r_adapter` | 1 | given | 16 | rank of the low-rank adapter |
| `w_master` | bit | given | 32 bit | width of a master weight during training |
| `n_moment` | 1 | given | 2 | optimiser moments kept per trained parameter |
| `f_recompute` | 1 | given | 1.33 | how much more arithmetic recomputation costs than saving everything |
| `n_micro_train` | 1 | given | 12 | microbatches in flight when training, twice generation's |
| `p_adapter` | 1 | derived | 9.43718e+07 | parameters in a rank-limited adapter across every layer |
| `C_adapter_st` | GB | derived | 1.50995 GB | its master weights, gradients and moments |
| `C_ckpt_bound` | GB | derived | 5.36871 GB | activations at layer boundaries only, which is what recomputation keeps |
| `batch_train` | 1 | given | 1 | sequences trained at once; one, because the checkpoints scale with it and the core does not |
| `C_full_train` | GB | derived | 1128.77 GB | what training every parameter would need, which is the number that does not fit |
| `C_train_res` | GB | derived | 43.2551 GB | resident during adapter training |
| `ratio_full` | 1 | derived | 15.6893 | how far out of reach full training is |
| `flop_train` | flop | derived | 5.16413e+11 flop | arithmetic per token when training: the forward pass, the backward pass, and the recomputation |
| `B_grad_stage` | MB | derived | 0.016384 MB | what crosses between two stages on the way back |

## What it consumes

| symbol | from | value | meaning |
|---|---|---|---|
| `C_activation` | `078` | 0.016384 MB | one token's activation vector, which is what crosses between stages |
| `C_core_usable` | `034` | 71.9454 GB | what a model may actually use |
| `C_staging` | `038` | 0.458752 MB | the six forward staging buffers |
| `C_staging_r` | `038` | 0.458752 MB | and the six reverse ones for training |
| `C_weights` | `078` | 36.3764 GB | the weights, resident, at the format in 046 |
| `d_ff` | `078` | 28672 | the feedforward's inner width |
| `d_model` | `078` | 8192 | the residual stream's width |
| `flop_token` | `078` | 1.41096e+11 flop | operations to generate one token: two per weight |
| `n_ctx` | `078` | 4096 | context length the machine is provisioned for |
| `n_layer` | `078` | 80 | transformer layers |
| `n_param` | `078` | 7.05482e+10 | the whole model |
| `n_stage` | `010` | 6 | pipeline stages a token falls through, one per face |
| `w_act` | `046` | 16 bit | activations in the residual stream |

## What consumes it

Change one of these and the blueprints beside it are what break.

| symbol | read by |
|---|---|
| `r_adapter` | `076a` |
| `w_master` | `076a` |
| `n_moment` | `076a` |
| `f_recompute` | `076a` |
| `n_micro_train` | `076a` |
| `p_adapter` | `076a` |
| `C_adapter_st` | `076a` |
| `C_ckpt_bound` | `076a` |
| `batch_train` | `076a` |
| `C_full_train` | `076a` |
| `C_train_res` | `076a` |
| `ratio_full` | `076a` |
| `flop_train` | `076a` |
| `B_grad_stage` | `076a` |

## What it asserts

| tag | relation | because |
|---|---|---|
| `C-076a-1` | `C_train_res < C_core_usable` | adapter training must fit alongside the resident model |
| `C-076a-2` | `B_grad_stage ~= C_activation * batch_train` | backward traffic between stages is the same size as forward traffic. The property that makes this cheap, asserted so that a design drifting away from it is noticed |
| `C-076a-3` | `n_micro_train >= 2 * n_stage` | interleaving forward and backward needs at least two microbatches per stage, or half the pipeline is idle on every pass |
| `C-076a-4` | `C_staging_r ~= C_staging` | the reverse staging buffers must be the same size as the forward ones, which follows from the property above and is what lets 038 allocate them without a second calculation |
| `C-076a-5` | `ratio_full > 10` | training every parameter must be out of reach by at least an order of magnitude. Asserted in the failing direction on purpose: a blueprint set that is silent about what a machine cannot do is worse than one that says so |
| `C-076a-6` | `flop_train > flop_token * 3` | training costs at least three times generation's arithmetic, which is what recomputation buys its memory saving with |

