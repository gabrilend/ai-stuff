# 091-units — info

*Generated by `104` from the folds in `091-units.lua`. Do not edit this page;
edit the program and run the sweep again.*

**An instrument** — phase 14. Not part of the machine, and it does not ship
to whoever builds one.

## What it is

Quantities that know what they are, and refuse to be added to things they are
not. Every number in this project's blueprints is a value plus a dimension;
this file is where the dimension lives and where arithmetic on it is enforced.

For a general reader: the point is that a length and a temperature cannot be
added, a power divided by a voltage is a current whether or not anybody said
so, and a bandwidth in bits per second is a different kind of thing from a
clock in hertz even though both are one-over-seconds. The last of those is
the reason this file has ten slots where physics only needs seven.

## What it offers

| call | takes | what it does |
|---|---|---|
| `M.SLOTS` | — | The dimension vector. |
| `M.dim_name` | `d` | A dimension rendered as something a person can read in an error message. |
| `M.dim_equal` | `a, b` | Whether two quantities are the same kind of thing. |
| `M.parse_unit` | `s` | Turn a written unit into a scale and a dimension. |
| `M.new` | `value, unit` | A quantity from a number and a written unit: `M.new(52, "mm")`. |
| `M.raw` | `value, d` | A quantity built directly from base-unit value and dimension. |
| `M.isq` | `x` | Whether a value is one of this engine's quantities. |
| `M.DIMENSIONLESS` | — | The dimension of a pure number. |
| `M.value_in` | `q, unit` | The value of a quantity expressed in a named unit. |
| `M.format` | `q, unit, digits` | Human-readable. |

### In the author's words

**`M.SLOTS`**

The dimension vector. Ten slots, in this fixed order, and the order is part
of the file format in the sense that nothing outside here may assume a
different one.

  1..7  the international system's base dimensions
  8     bit   -- information
  9     tok   -- tokens, the thing this machine produces
 10     flop  -- floating point operations

Slots eight through ten are not physics. They exist because bits per second,
tokens per second and operations per second are all reciprocal seconds, and
confusing any two of them is a mistake somebody will make in a bandwidth
blueprint. Without these slots nothing catches it; with them, the addition
simply refuses. Three extra integers to make a whole class of error
impossible is the cheapest thing in this project.
The ten slots a dimension is a vector over: the seven SI base units, and
three of this project's own. Bits, tokens and floating-point operations are
all dimensionless to physics and are three different things here, which is
what makes a bandwidth, a token rate and a clock frequency refuse to be added
to one another.

**`M.dim_name`** — published from `dim_name`

A dimension rendered as something a person can read in an error message.
"m*kg/s^2" rather than a vector of ten integers, because the error message is
the whole value of this module and a vector teaches nobody anything.

**`M.dim_equal`** — published from `dim_equal`

Whether two quantities are the same kind of thing. Compares all ten slots,
because two dimensions that agree on nine and differ on the tenth are exactly
the pair worth catching -- bits a second against hertz, tokens a second
against either.

**`M.parse_unit`** — published from `parse_unit`

Turn a written unit into a scale and a dimension. "mm" gives a thousandth and
a length; "W/(m^2*K)" gives one and the dimension a convection coefficient
has. Raises on a unit it does not know rather than guessing, because a
guessed unit is a wrong number that looks right.

The scale is what converts the declared number into base units, and every
value in this project is stored in base units and converted back only when
something is printed.

**`M.new`**

A quantity from a number and a written unit: `M.new(52, "mm")`. The number is
converted to base units on the way in, so everything downstream compares
like with like without knowing what anybody wrote.

**`M.raw`**

A quantity built directly from base-unit value and dimension. Used by the
expression evaluator, which has already done the conversion.

**`M.isq`** — published from `isq`

Whether a value is one of this engine's quantities.

It tests a marker field rather than the metatable, and that is not
fastidiousness. Two modules each reaching for this file with `dofile` load it
twice, and `dofile` does not cache -- so there are two metatables, and a
quantity built by one copy is rejected by the other with a message saying it
is not a quantity. Correct, baffling, and cost an afternoon.

**`M.DIMENSIONLESS`**

The dimension of a pure number. Every literal in this notation has it, which
is the rule that stops an unlabelled physical quantity entering the project.

**`M.value_in`**

The value of a quantity expressed in a named unit. Raises if the unit does
not match the quantity's dimension, which is how a specification sheet is
stopped from printing a length in watts.

**`M.format`**

Human-readable. Given a unit, uses it; given none, falls back to base units,
which is correct and often unreadable -- so every place that prints for a
person is expected to name a unit rather than rely on this.

## How it works inside

Nothing outside this file can call these. They are listed so a reader
knows what is in it, not how — a companion page treats a function as a
box with a label on it.

| function | takes | what it is for |
|---|---|---|
| `newdim` | `t` | *(no description in the source)* |
| `dim_add` | `a, b, sign` | *(no description in the source)* |
| `dim_scale` | `a, k` | *(no description in the source)* |
| `defunit` | `name, scale, exps` | *(no description in the source)* |
| `lookup_unit` | `w` | Resolve one unit word. |
| `unit_tokens` | `s` | *(no description in the source)* |
| `coerce` | `x` | A bare Lua number appearing in arithmetic is treated as dimensionless. |
| `require_same` | `a, b, what` | *(no description in the source)* |

## What it reads

Nothing. It stands alone, which for an instrument means it can be run
against a project that does not load.

## Running it

It is a module. Something else loads it; there is nothing to run.

---

*Every function in this project is wrapped in a vimfold that opens with a
comment carrying its name, followed by prose, followed by the definition.
That was adopted so a long file collapses neatly in an editor. It is also,
by accident, a machine-readable statement of what a module offers, and it
is the only reason this page can exist — Lua itself says nothing about what
a file exports.*
