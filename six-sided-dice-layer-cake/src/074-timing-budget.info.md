# 074-timing-budget — info

*Generated by `096` from the declarations in `074-timing-budget.md`. Do not edit this page;
edit the blueprint and run the sweep again.*

**Does it close** — phase 10.
Described by `1005`.

## What it publishes

| symbol | unit | kind | value | meaning |
|---|---|---|---|---|
| `t_logic_mac` | ps | given | 470 ps | logic delay through one multiply-accumulate cell at the slow corner |
| `t_wire_mac` | ps | given | 55 ps | wire delay on the same path |
| `t_setup` | ps | given | 25 ps | setup time at the capturing flip-flop |
| `t_hold` | ps | given | 18 ps | hold time at the same |
| `t_ocv` | ps | given | 45 ps | on-chip variation allowance across the path |
| `t_logic_min` | ps | given | 80 ps | shortest logic delay on any path, which is what a hold failure is measured against |
| `k_temp_delay` | 1/K | given | 0.0018 1/K | fractional increase in delay per kelvin near the operating point |
| `k_freq_power` | 1 | given | 3 | how power scales with frequency near the top of the range |
| `t_path_face` | ps | derived | 616.47 ps | everything the face's critical path has to fit inside a cycle |
| `t_margin_face` | ps | derived | 97.8158 ps | what is left |
| `f_margin_face` | 1 | derived | 0.136942 | that as a share of a cycle |
| `t_path_hold` | ps | derived | 58.53 ps | the shortest path, less what could make it shorter still |
| `t_path_core` | ps | derived | 790.47 ps | the core array's path |
| `t_cycle_core_p` | ps | derived | 800 ps | and its cycle |
| `t_margin_core` | ps | derived | 9.53005 ps | what is left there |
| `t_path_at_face` | ps | derived | 790.47 ps | the core's path, for the question of whether it could run at the face clock |
| `f_boost_max` | 1 | derived | 1.03428 | how much faster the faces could run if the whole thermal margin were spent on clock rather than on removing a chiller |
| `f_face_boost` | GHz | derived | 1.44799 GHz | and what frequency that would be |

## What it consumes

| symbol | from | value | meaning |
|---|---|---|---|
| `T_j_mean` | `025` | 304.97 K | and the same for an average die in the middle of the coolant path |
| `T_j_peak` | `025` | 318.883 K | junction temperature at the hottest point of the hottest die on the worst-served face, with the coolant at its outlet temperature |
| `f_core` | `034` | 1.25 GHz | core clock, set by the array access time in 035 |
| `f_face` | `045` | 1.4 GHz | the face clock |
| `jit_budget` | `070` | 3.46995 ps | what 074 must allow for altogether |
| `margin_thermal` | `025` | 59.1173 K | how far the hottest transistor is from what the silicon is qualified to |
| `skew_die` | `071` | 12 ps | skew within one die's balanced tree, which is where the tight number is |
| `skew_intra` | `071` | 18 ps | skew a face's own timing closure must absorb, which is what 074 budgets |
| `t_access` | `035` | 0.75 ns | latency from a read being issued to the first bits arriving at the tier's port |
| `t_cycle_face` | `070` | 714.286 ps | the face cycle time everything in 074 is budgeted against |

## What consumes it

Change one of these and the blueprints beside it are what break.

| symbol | read by |
|---|---|
| `t_logic_mac` | `074` |
| `t_wire_mac` | `074` |
| `t_setup` | `074` |
| `t_hold` | `074` |
| `t_ocv` | `074` |
| `t_logic_min` | `074` |
| `k_temp_delay` | `074` |
| `k_freq_power` | `074` |
| `t_path_face` | `074` |
| `t_margin_face` | `074` |
| `f_margin_face` | `074` |
| `t_path_hold` | `074` |
| `t_path_core` | `074` |
| `t_cycle_core_p` | `074` |
| `t_path_at_face` | `074` |
| `f_boost_max` | `074` |

## What it asserts

| tag | relation | because |
|---|---|---|
| `C-074-1` | `t_path_face < t_cycle_face` | the face's critical path plus all its uncertainty must fit inside a cycle at the slow corner. The constraint the whole blueprint exists for |
| `C-074-2` | `f_margin_face > 0.02` | and with at least a fiftieth of a cycle left, because the logic delay is a `given` from a cell 045 has not laid out |
| `C-074-3` | `t_path_hold > t_hold` | the shortest path, after everything that could make it shorter, must still meet hold at the fast corner. A setup failure is a machine that will not run fast; a hold failure is a machine that does not work at any speed, and it gets worse at the corner the setup check does not visit |
| `C-074-4` | `t_path_core < t_cycle_core_p` | the core's path must fit its own cycle |
| `C-074-5` | `T_j_peak > T_j_mean` | the temperature used for the delay corner must be the peak rather than the die average. Trivially true as written, and it is here because the hot spot sits inside the multiplier array, which is also where the critical path is -- the hottest transistors are the slowest ones and they are the ones that matter |
| `C-074-6` | `t_path_at_face > t_cycle_face` | the core's path does not fit a face cycle, which answers 070's question: the two domains cannot be merged, and the crossing in 072 stays |

