# 034-core-organisation — info

*Generated by `096` from the declarations in `034-core-organisation.md`. Do not edit this page;
edit the blueprint and run the sweep again.*

**What the middle holds** — phase 5.
Described by `501`.

## What it publishes

| symbol | unit | kind | value | meaning |
|---|---|---|---|---|
| `w_tier_port` | bit | given | 10240 bit | bits one tier delivers per cycle, set by its macro count and the routing it can support across a forty millimetre die |
| `f_core` | GHz | given | 1.25 GHz | core clock, set by the array access time in 035 |
| `f_ecc_overhead` | 1 | derived | 0.037594 | share of raw capacity spent on check bits |
| `f_spare` | 1 | given | 0.02 | share held as spare rows and columns within a tier, blown at test |
| `f_redundant` | 1 | derived | 0.0416667 | share held as one whole spare tier, mapped in if another fails |
| `n_bank` | 1 | given | 256 | independent banks across the whole core |
| `n_bank_min` | 1 | given | 1000 | how many cycles' worth of read the smallest sensible bank holds |
| `C_tier` | GB | derived | 3.31658 GB | capacity of one tier. Written first with a division by a thousand to turn megabytes into gigabytes, which is exactly the manual conversion the notation exists to make unnecessary -- and which, being a dimensionless literal, silently made the core a thousand times too small |
| `C_core_raw` | GB | derived | 79.598 GB | raw capacity of the stack |
| `C_core_usable` | GB | derived | 71.9454 GB | what a model may actually use |
| `B_core` | bit/s | derived | 3.072e+14 bit/s | aggregate read bandwidth, every tier delivering at once |
| `B_face_max` | bit/s | derived | 3.072e+14 bit/s | what a single face may take when the others are idle. Equal to the aggregate, deliberately, and 037 has to deliver it |
| `B_face_even` | bit/s | derived | 5.12e+13 bit/s | and what it gets when all six are asking equally |
| `C_bank` | GB | derived | 0.31093 GB | capacity of one bank |
| `p_collide` | 1 | derived | 0.0308261 | chance that two of the eight clients -- six faces, the spout and the scrubber -- want the same bank in the same cycle, assuming their addresses are independent |
| `t_core_sweep` | s | derived | 0.0165829 s | how long it takes to read the whole core once at full bandwidth, which is what 040's scrub and 069b's memory mode are both measured against |

## What it consumes

| symbol | from | value | meaning |
|---|---|---|---|
| `A_core_side` | `012` | 1600 mm^2 | area of one side of the core block, and of one tier |
| `C_resident` | `059` | 55.1669 GB | what a model needs resident: weights and cache together |
| `E_core_bit` | `020` | 0.48 pJ/bit | energy to read one bit out of a core tier, array and tier routing together |
| `P_core_read` | `020` | 147.456 W | the core's array read energy at its full bandwidth |
| `d_areal` | `035` | 2.07286 MB/mm^2 | areal density. The unit conversion from bits per square micron to megabytes per square millimetre is the notation's job, not the author's, which is the second reason area per bit had to carry its bit |
| `n_ecc_check` | `040` | 10 bit | check bits on that line: nine to locate a single error among two hundred and fifty-six, one more to detect a second |
| `n_ecc_line` | `040` | 256 bit | data bits one correction line covers |
| `n_face` | `010` | 6 | compute faces, one per side of the cube |
| `n_tier` | `036` | 24 | memory tiers in the stack. Twenty-four rather than the thirty-two first sketched, because at the density 035 derives, thirty-two holds half again what is needed |

## What consumes it

Change one of these and the blueprints beside it are what break.

| symbol | read by |
|---|---|
| `w_tier_port` | `034`, `035`, `037`, `038`, `047` |
| `f_core` | `034`, `035`, `037`, `070`, `072`, `074` |
| `f_ecc_overhead` | `034`, `040` |
| `f_spare` | `034` |
| `f_redundant` | `034` |
| `n_bank` | `034` |
| `n_bank_min` | `034` |
| `C_tier` | `034` |
| `C_core_raw` | `034`, `035`, `040`, `073` |
| `C_core_usable` | `034`, `038`, `059`, `062`, `067`, `069a`, `069b`, `076a`, `078`, `080` |
| `B_core` | `020`, `034`, `037`, `039`, `040`, `051`, `053`, `055`, `059`, `060`, `061`, `062`, `073`, `079`, `080` |
| `B_face_max` | `034`, `055` |
| `B_face_even` | `037`, `044`, `047`, `048`, `052`, `060`, `061` |
| `C_bank` | `034` |
| `p_collide` | `034` |

## What it asserts

| tag | relation | because |
|---|---|---|
| `C-034-1` | `C_core_usable > C_resident` | usable capacity must exceed what the reference model needs resident, weights and cache together, from 078 |
| `C-034-2` | `B_face_max == B_core` | one face may take the whole of it. The single-face-takes-all requirement as a constraint rather than a paragraph, because 008 entry 5 depends on it and a crossbar that quietly delivered a sixth would leave every other blueprint still checking |
| `C-034-3` | `B_core * E_core_bit ~= P_core_read` | bandwidth times read energy is the core's read power in 020. A cross-check between two phases, and what it catches is one of the two being retuned alone |
| `C-034-4` | `p_collide < 0.05` | eight clients wanting the same bank at once must be rare, or 037's arbiter is arbitrating rather than switching |
| `C-034-5` | `C_bank > w_tier_port * n_bank_min` | a bank must be much larger than a single cycle's worth of read, or the banking is finer than the transfers and every transfer straddles two banks |
| `C-034-6` | `n_tier > 4` | a stack too short to lose a tier to redundancy without losing a quarter of the capacity is not a stack; this is what makes the redundant-tier scheme affordable |

## What it draws

- **from a tier's footprint to what a model can use [not-dimensioned]** — dimensions `A_core_side`, `d_areal`, `C_tier`, `n_tier`, `C_core_raw`, `f_ecc_overhead`, `f_spare`, `f_redundant`, `C_core_usable`

