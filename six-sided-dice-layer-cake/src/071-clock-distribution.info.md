# 071-clock-distribution — info

*Generated by `096` from the declarations in `071-clock-distribution.md`. Do not edit this page;
edit the blueprint and run the sweep again.*

**Getting the beat everywhere** — phase 10.
Described by `1002`.

## What it publishes

| symbol | unit | kind | value | meaning |
|---|---|---|---|---|
| `n_tree_level` | 1 | given | 3 | levels of distribution: within a die, across a face, between faces |
| `skew_die` | ps | given | 12 ps | skew within one die's balanced tree, which is where the tight number is |
| `skew_face` | ps | given | 6 ps | skew across the four dies of a face |
| `skew_proc` | ps | given | 20 ps | skew between two faces from process variation alone |
| `skew_temp_k` | ps/K | given | 0.4 ps/K | timing shift per kelvin of steady temperature difference between two faces |
| `dT_face_diff` | K | given | 12 K | steady temperature difference between a busy face and an idle one |
| `P_clocktree` | W | given | 22 W | all three levels together |
| `mesochronous` | 1 | given | 1 | whether faces share a frequency but not a phase. They do, and it is written as a value so that a blueprint assuming a common edge fails outright |
| `skew_temp` | ps | derived | 4.8 ps | the temperature term between two faces |
| `skew_supply` | ps | derived | 0.428571 ps | the supply term, each face having its own regulator |
| `skew_face_all` | ps | derived | 20.5724 ps | skew between two faces, the three independent sources in quadrature |
| `skew_intra` | ps | derived | 18 ps | skew a face's own timing closure must absorb, which is what 074 budgets |
| `L_arm` | mm | derived | 7 mm | length of one arm of the inter-face star; equal for all six by construction |
| `f_skew_cycle` | 1 | derived | 0.0252 | the intra-face skew as a share of a cycle |

## What it consumes

| symbol | from | value | meaning |
|---|---|---|---|
| `P_load` | `020` | 1609.28 W | power delivered to the point of load |
| `V_logic` | `029` | 0.75 V | logic and multiplier arrays |
| `dV_droop_logic` | `029` | 0.0225 V | how far the logic rail may fall during a load step, which is what sizes the decoupling in 031 |
| `psrr_mult` | `070` | 0.02 | fraction of a supply excursion that appears as a timing shift at the multiplier's output |
| `t_cross_face` | `072` | 2142.86 ps | latency a domain crossing adds, which lands in 053's stage budget |
| `t_cycle_face` | `070` | 714.286 ps | the face cycle time everything in 074 is budgeted against |
| `t_face` | `012` | 7 mm | one face assembly, outward surface to inward surface; the stack in 014 adds to just under this |

## What consumes it

Change one of these and the blueprints beside it are what break.

| symbol | read by |
|---|---|
| `skew_die` | `071`, `074` |
| `skew_face` | `071` |
| `skew_proc` | `071` |
| `skew_temp_k` | `071` |
| `dT_face_diff` | `071` |
| `P_clocktree` | `071` |
| `mesochronous` | `071` |
| `skew_temp` | `071` |
| `skew_supply` | `071` |
| `skew_face_all` | `071` |
| `skew_intra` | `071`, `074` |
| `L_arm` | `071` |
| `f_skew_cycle` | `071` |

## What it asserts

| tag | relation | because |
|---|---|---|
| `C-071-1` | `f_skew_cycle < 0.06` | skew a face must absorb in its own closure has to stay under a sixteenth of a cycle, which is what 074 allocates |
| `C-071-2` | `skew_face_all < t_cross_face` | skew between two faces must be small against a domain crossing's own latency, which is the loose bound mesochronous operation permits -- and it is two orders of magnitude looser than a common edge would have needed |
| `C-071-3` | `mesochronous == 1` | faces share a frequency and not a phase. Asserted as a value so that a blueprint deriving something from a common edge fails rather than being quietly wrong |
| `C-071-4` | `L_arm ~= t_face` | every arm of the star is one face thickness, which is what makes all six equal. It is the cube's geometry doing the work, and a change that moved the cage off centre would break it here |
| `C-071-5` | `P_clocktree < P_load / 50` | the whole distribution must cost under a fiftieth of the machine. Insisting on synchrony between faces is what would break this |
| `C-071-6` | `skew_temp < skew_proc` | the temperature term must be smaller than process variation, or the thermal design is what limits the clock |

## What it draws

- **three levels, and the middle one is unusual [not-dimensioned]** — no dimensions called out

