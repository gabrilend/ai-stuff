# 037-core-six-port-arbitration — info

*Generated by `096` from the declarations in `037-core-six-port-arbitration.md`. Do not edit this page;
edit the blueprint and run the sweep again.*

**Six askers, one store** — phase 5.
Described by `504`.

## What it publishes

| symbol | unit | kind | value | meaning |
|---|---|---|---|---|
| `n_port` | 1 | derived | 8 | clients on the switch: six faces, the spout and the scrubber |
| `w_xbar_slice` | bit | given | 1024 bit | width of one crossbar slice, the granularity the fabric is built in |
| `n_xbar_slice` | 1 | derived | 240 | slices needed to carry the whole core at once |
| `q_arb` | bit | given | 65536 bit | arbitration quantum: how much one client is given before the arbiter reconsiders |
| `a_xbar_slice` | mm^2 | measured | 0.021 mm^2 | area of one crossbar slice's worth of switch at this node, including its buffering |
| `e_xbar_bit` | pJ/bit | measured | 0.045 pJ/bit | energy to move one bit through the switch fabric |
| `B_bisect` | bit/s | derived | 3.072e+14 bit/s | bisection bandwidth of the fabric |
| `A_xbar` | mm^2 | derived | 40.32 mm^2 | area the switch occupies, which is what sets the cage thickness in 012 |
| `P_xbar` | W | derived | 13.824 W | power it takes at full traffic |
| `t_quantum` | s | derived | 1.28e-09 s | how long one client holds the switch before the arbiter reconsiders |
| `t_wait_face` | s | derived | 6.4e-09 s | worst case a face waits when all six are asking, which is what 053's stage budget must absorb |
| `t_wait_spout` | s | derived | 1.024e-08 s | worst case the spout waits, being low priority |
| `t_wait_scrub` | s | derived | 2.048e-08 s | and the scrubber, being lower |
| `A_cage_avail` | mm^2 | derived | 12696 mm^2 | silicon area available in the cage shell, taken as its six inner faces |

## What it consumes

| symbol | from | value | meaning |
|---|---|---|---|
| `B_core` | `034` | 3.072e+14 bit/s | aggregate read bandwidth, every tier delivering at once |
| `B_face_even` | `034` | 5.12e+13 bit/s | and what it gets when all six are asking equally |
| `L_cavity` | `012` | 46 mm | edge of the space the six faces enclose |
| `P_crossbar` | `020` | 13.82 W | the cage's switch fabric at full traffic. Thirty-nine was an estimate before 037 had a per-bit figure; the constraint that compares the two is what corrected it |
| `f_core` | `034` | 1.25 GHz | core clock, set by the array access time in 035 |
| `n_face` | `010` | 6 | compute faces, one per side of the cube |
| `n_tier` | `036` | 24 | memory tiers in the stack. Twenty-four rather than the thirty-two first sketched, because at the density 035 derives, thirty-two holds half again what is needed |
| `t_cage` | `012` | 3 mm | the switch shell lining the inside of the cavity, sized by the crossbar area in 037 |
| `t_scrub_period` | `040` | 3600 s | the same figure, under the name 037's starvation bound refers to |
| `t_stage` | `053` | unresolved | how long one face works before the sieve moves on |
| `w_tier_port` | `034` | 10240 bit | bits one tier delivers per cycle, set by its macro count and the routing it can support across a forty millimetre die |

## What consumes it

Change one of these and the blueprints beside it are what break.

| symbol | read by |
|---|---|
| `n_port` | `037`, `054` |
| `w_xbar_slice` | `037` |
| `n_xbar_slice` | `037` |
| `q_arb` | `037`, `052` |
| `a_xbar_slice` | `037` |
| `e_xbar_bit` | `037` |
| `B_bisect` | `037`, `055` |
| `A_xbar` | `037` |
| `P_xbar` | `037` |
| `t_quantum` | `037` |
| `t_wait_face` | `037`, `039`, `051`, `054` |
| `t_wait_spout` | `054` |
| `t_wait_scrub` | `037`, `054` |
| `A_cage_avail` | `037` |

## What it asserts

| tag | relation | because |
|---|---|---|
| `C-037-1` | `B_bisect >= B_core` | the fabric must carry everything the tiers can deliver, or the switch is the bottleneck rather than the memory and every performance number in 080 is wrong |
| `C-037-2` | `A_xbar < A_cage_avail` | the switch must fit in the cage. This is what sizes t_cage in 012, and if it fails the cavity shrinks and the core with it |
| `C-037-3` | `P_xbar ~= P_crossbar` | the power derived here must be the figure 020's budget assumed. Two routes, one number |
| `C-037-4` | `t_wait_face < t_stage / 100` | a face's worst-case wait must be a hundredth of a pipeline stage, so that arbitration never shows up in 080's model |
| `C-037-5` | `t_wait_scrub < t_scrub_period` | even the lowest-priority client must finish. Starvation freedom expressed as a bound rather than as a promise, which is the only form of it that can be checked |
| `C-037-6` | `q_arb > w_tier_port` | the quantum must be larger than a single cycle's read, or the arbiter reconsiders faster than the memory answers |

## What it draws

- **what the switch is actually doing [not-dimensioned]** — no dimensions called out

