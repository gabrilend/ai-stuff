# 062-spout-concept — info

*Generated by `096` from the declarations in `062-spout-concept.md`. Do not edit this page;
edit the blueprint and run the sweep again.*

**The idea, and what it costs** — phase 9.
Described by `901`.

## What it publishes

| symbol | unit | kind | value | meaning |
|---|---|---|---|---|
| `p_bond_fine` | um | given | 10 um | the finest pitch a permanent copper-to-copper bond reaches |
| `f_pane_overhead` | 1 | given | 0.2 | share of the fine zone's positions spent on power, ground, shielding and strobes |
| `e_pane_bit` | pJ/bit | measured | 0.01 pJ/bit | energy to move one bit across a ten micron bond, driver and receiver together |
| `f_spout_burst` | GHz | given | 1 GHz | rate a pane transfer runs at during a burst |
| `f_spout_sust` | MHz | given | 50 MHz | rate it may run at continuously without exceeding the steady port allocation. A hundred megahertz was the first figure and costs seventeen watts against a ten watt steady budget -- which is the difference between a burst the silicon absorbs and a load the coolant sees |
| `n_fine_col` | 1 | derived | 4800 | pad columns across the fine zone |
| `n_fine_pad` | 1 | derived | 2.304e+07 | positions in it |
| `n_pane_avail` | 1 | derived | 1.8432e+07 | conductors available for data |
| `n_pane_bit` | bit | derived | 1.67772e+07 bit | the pane, rounded down to a power of two so that its window aligns naturally in 038. It carries the unit because it is a quantity of bits rather than a count of things, and everything downstream of it is a size or a rate |
| `C_pane_mb` | MB | derived | 2.09715 MB | the pane, in the unit a person reads it in |
| `E_pane` | J | derived | 1.67772e-07 J | energy of one pane transfer |
| `n_pane_core` | 1 | derived | 34306.2 | panes needed to move the whole core |
| `t_core_out` | s | derived | 3.43062e-05 s | time to do it at the burst rate |
| `P_spout_burst` | W | derived | 167.772 W | power while a burst runs |
| `B_spout_burst` | bit/s | derived | 1.67772e+16 bit/s | the burst rate as a bandwidth |
| `B_spout_sust` | bit/s | derived | 8.38861e+14 bit/s | and the sustained one |
| `t_pane_fill` | s | derived | 5.46133e-08 s | how long the core takes to fill a pane |
| `t_pane_empty` | s | derived | 1e-09 s | how long the spout takes to empty it |
| `ratio_fill` | 1 | derived | 54.6133 | how much slower the memory is than the tube, which is the limit that is not the wires |
| `ratio_net` | 1 | derived | 41943 | how much faster this is than sending the same bytes over a network interface |
| `t_core_net` | s | derived | 1.43891 s | time to move the whole core over a fast network interface, for the comparison |
| `r_net_ref` | bit/s | measured | 4e+11 bit/s | a fast network interface, as the comparison everybody will make |

## What it consumes

| symbol | from | value | meaning |
|---|---|---|---|
| `A_fine` | `056` | 2304 mm^2 | area of the fine zone |
| `B_core` | `034` | 3.072e+14 bit/s | aggregate read bandwidth, every tier delivering at once |
| `C_core_usable` | `034` | 71.9454 GB | what a model may actually use |
| `E_burst_max` | `057` | 20 J | the most energy a transient may deposit in the face assemblies before it stops being absorbed by their thermal mass and starts being a load the coolant sees |
| `P_ports` | `020` | 10 W | six port fields during generation, when the storage lines are idle and the spout is occasional. This is a steady figure and the two things that break it are both bursts |
| `b1` | `046` | 1 bit | one bit. A width carries the unit so that a format's size can be added to a capacity; an exponent needs a pure number. Dividing by this is how a width becomes a count, and it is the only place in the project where a unit quantity is declared for that purpose |

## What consumes it

Change one of these and the blueprints beside it are what break.

| symbol | read by |
|---|---|
| `p_bond_fine` | `062`, `063`, `064` |
| `f_pane_overhead` | `062` |
| `e_pane_bit` | `062`, `064`, `069a` |
| `f_spout_burst` | `062`, `064`, `065`, `068` |
| `f_spout_sust` | `062`, `069` |
| `n_fine_col` | `062` |
| `n_fine_pad` | `062`, `063` |
| `n_pane_avail` | `062` |
| `n_pane_bit` | `038`, `062`, `063`, `064`, `065`, `067`, `068`, `069`, `069a`, `069b` |
| `C_pane_mb` | `062`, `065`, `069a` |
| `E_pane` | `026`, `062`, `068` |
| `n_pane_core` | `026`, `062`, `068`, `069` |
| `t_core_out` | `062`, `066`, `069a` |
| `P_spout_burst` | `062` |
| `B_spout_burst` | `067`, `069a` |
| `B_spout_sust` | `062`, `069a` |
| `t_pane_fill` | `062`, `069b` |
| `t_pane_empty` | `062`, `067`, `069b` |
| `ratio_fill` | `062` |
| `ratio_net` | `062` |
| `t_core_net` | `062`, `068` |
| `r_net_ref` | `062`, `067` |

## What it asserts

| tag | relation | because |
|---|---|---|
| `C-062-1` | `n_pane_bit / b1 <= n_pane_avail` | the pane must fit in the conductors the fine zone actually has after power, ground and strobes are taken out |
| `C-062-2` | `ratio_fill > 10` | the memory must be an order of magnitude slower to fill a pane than the tube is to empty it. This is the limit that is not the wires, asserted so that a reader who counts conductors meets it as a number |
| `C-062-3` | `P_spout_burst * t_core_out < E_burst_max` | pushing the whole core out must deposit less energy than 026's thermal masses absorb, which is what makes this an energy budget rather than a power one |
| `C-062-4` | `B_spout_sust * e_pane_bit < P_ports` | the sustained rate must fit inside the steady port allocation, since sustained means the coolant does see it |
| `C-062-5` | `ratio_net > 1000` | moving the whole core this way must be three orders of magnitude faster than a network interface, which is the only comparison that makes sixteen million wires worth their manufacturing risk |
| `C-062-6` | `C_pane_mb ~= n_pane_bit` | the pane expressed in megabytes and in bits must be the same quantity. Trivially true now that both carry the unit, and it was not before: the first version divided by eight million by hand and produced a dimensionless number that no other blueprint could use |

## What it draws

- **what one edge does [not-dimensioned]** — dimensions `C_pane`

