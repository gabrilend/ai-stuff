# 079-batching-and-occupancy — info

*Generated by `096` from the declarations in `079-batching-and-occupancy.md`. Do not edit this page;
edit the blueprint and run the sweep again.*

**The number twenty-eight** — phase 11.
Described by `1105`.

## What it publishes

| symbol | unit | kind | value | meaning |
|---|---|---|---|---|
| `eta_array_util` | 1 | given | 0.82 | share of cycles a multiplier has an operand at the design microbatch, which is below one because a tile reload is not overlapped perfectly |
| `n_micro_min` | 1 | derived | 6 | microbatches needed to keep every stage busy |
| `t_step_weights` | s | derived | 0.000947302 s | time for one step's weight traffic, which does not depend on batch |
| `t_step_kv` | s | derived | 0.000489335 s | and its cache traffic, which does |
| `t_step_mem` | s | derived | 0.00143664 s | the memory side of a step |
| `ops_achieved` | flop/s | derived | 3.6113e+15 flop/s | arithmetic the machine actually delivers rather than its peak |
| `t_step_arith` | s | derived | 0.00109398 s | the arithmetic side at the design batch |
| `batch_cross` | 1 | derived | 24.2458 | the batch at which the two are equal, ignoring the cache |
| `batch_cross_kv` | 1 | derived | 43.8676 | and including it. It comes out *higher*, not lower: the cache adds to the memory side in proportion to batch, so the arithmetic line has further to climb before it crosses |
| `f_batch_err` | 1 | derived | 0.13408 | how far the batch the machine is provisioned for sits from the crossover it derives |
| `micro_size` | 1 | derived | 4.66667 | sequences in one microbatch at the design batch |
| `tok_per_s_1` | 1/s | derived | 1036.51 1/s | tokens a second, one sequence |
| `tok_per_s_agg` | 1/s | derived | 19489.9 1/s | and aggregate at the design batch |
| `gain_batch` | 1 | derived | 18.8035 | what batching buys, which is the number an operator cares about most |

## What it consumes

| symbol | from | value | meaning |
|---|---|---|---|
| `B_core` | `034` | 3.072e+14 bit/s | aggregate read bandwidth, every tier delivering at once |
| `B_kv_tok` | `076` | 18790.5 MB | and across the whole batch, which is a different number and is the one 079 uses |
| `C_weights` | `078` | 36.3764 GB | the weights, resident, at the format in 046 |
| `batch_design` | `047` | 28 | the batch the machine is provisioned for; 079 derives the crossover independently and C-079-1 requires the two to agree |
| `flop_token` | `078` | 1.41096e+11 flop | operations to generate one token: two per weight |
| `n_stage` | `010` | 6 | pipeline stages a token falls through, one per face |
| `ops_machine` | `045` | 4.40402e+15 flop/s | and the whole machine |
| `t_token` | `053` | 0.000964779 s | time for one token of one sequence, bandwidth-bound: every weight once, plus that sequence's cache |

## What consumes it

Change one of these and the blueprints beside it are what break.

| symbol | read by |
|---|---|
| `eta_array_util` | `053`, `079` |
| `n_micro_min` | `079` |
| `t_step_weights` | `079` |
| `t_step_kv` | `079` |
| `t_step_mem` | `079` |
| `ops_achieved` | `079`, `080` |
| `t_step_arith` | `079` |
| `batch_cross` | `079` |
| `batch_cross_kv` | `079` |
| `f_batch_err` | `079` |
| `micro_size` | `079` |
| `tok_per_s_1` | `079` |
| `tok_per_s_agg` | `079` |
| `gain_batch` | `079` |

## What it asserts

| tag | relation | because |
|---|---|---|
| `C-079-1` | `f_batch_err < 0.15` | the batch the machine is provisioned for must be within fifteen per cent of the crossover it independently derives. 047 chose the provisioning and this derives the crossover from bandwidth and arithmetic; the two arriving at the same number by different routes is what says the machine is balanced rather than accidentally sized |
| `C-079-2` | `micro_size >= 1` | there must be at least one sequence per microbatch at the design batch, or the pipeline cannot be filled and the compute-bound region is unreachable |
| `C-079-3` | `n_micro_min == n_stage` | one microbatch per stage is the minimum, which is 053's central rule restated where the batch arithmetic lives |
| `C-079-4` | `gain_batch > 10` | batching must buy more than an order of magnitude, or the whole compute-bound regime is not worth provisioning for |
| `C-079-5` | `eta_array_util < 1` | achieved arithmetic must be below peak. Asserted in the confirming direction, because a performance model built on peak is the commonest way to be wrong about a machine like this by twenty per cent |
| `C-079-6` | `batch_cross_kv >= batch_cross` | including the cache must raise the crossover. Written the other way round first, on the assumption that more memory traffic means the memory wall arrives sooner -- but cache traffic scales with batch, so it lifts both lines and the arithmetic one has further to climb |

## What it draws

- **the two lines that cross [not-dimensioned]** — no dimensions called out

