# 064-spout-driver-and-receiver — info

*Generated by `096` from the declarations in `064-spout-driver-and-receiver.md`. Do not edit this page;
edit the blueprint and run the sweep again.*

**One wire, and the circuit at each end** — phase 9.
Described by `903`.

## What it publishes

| symbol | unit | kind | value | meaning |
|---|---|---|---|---|
| `a_driver` | um^2 | measured | 14 um^2 | area of one driver: an inverter sized for a femtofarad |
| `a_receiver` | um^2 | measured | 16 um^2 | area of one receiver: an inverter and a small latch |
| `c_bond` | fF | measured | 1 fF | capacitance one bonded conductor presents |
| `f_circuit_over` | 1 | measured | 13.89 | how much more a bit costs than moving the bond's own charge. The bond is a fourteenth of it; the rest is the two inverters switching, the local strobe distribution and the receiver's latch |
| `L_bond` | um | given | 10 um | length of the bond itself |
| `L_reach_bond` | um | given | 50 um | the furthest this circuit is valid over, beyond which a channel exists and 067 applies |
| `v_bounce_max` | V | given | 0.06 V | supply bounce the receiver still resolves a bit through |
| `n_stagger` | 1 | given | 16 | groups the tiles are fired in, one after another |
| `t_stagger` | ps | given | 20 ps | interval between two stagger groups |
| `f_cond_gain` | 1 | given | 0.5 | share of the worst-case switching that data conditioning removes |
| `a_conductor` | um^2 | derived | 100 um^2 | area one conductor has, pad and circuit together |
| `a_circuit` | um^2 | derived | 30 um^2 | area the circuit needs |
| `f_circuit_area` | 1 | derived | 0.3 | how much of the pitch the circuit uses |
| `E_bit_derived` | pJ/bit | derived | 0.0100008 pJ/bit | energy to move one bit: the bond's own charge, times what the circuits at each end add |
| `I_ssn_raw` | A | derived | 20.1327 A | current if every conductor switched the same way on the same edge |
| `I_ssn_mit` | A | derived | 0.629146 A | and after conditioning and stagger |
| `v_bounce` | V | derived | 1.25829e-06 V | the bounce that produces |
| `z_supply` | ohm | given | 2e-06 ohm | impedance the local supply presents to the array at the switching frequency |
| `f_gnd_min` | 1 | derived | 0.15 | the least share of positions that must be ground and shielding for the bounce above to be what it is |
| `t_stagger_all` | ps | derived | 320 ps | how long the whole stagger sequence takes, which 065 has to absorb |

## What it consumes

| symbol | from | value | meaning |
|---|---|---|---|
| `V_port` | `029` | 1.2 V | port field transceivers |
| `b1` | `046` | 1 bit | one bit. A width carries the unit so that a format's size can be added to a capacity; an exponent needs a pure number. Dividing by this is how a width becomes a count, and it is the only place in the project where a unit quantity is declared for that purpose |
| `e_pane_bit` | `062` | 0.01 pJ/bit | energy to move one bit across a ten micron bond, driver and receiver together |
| `f_spout_burst` | `062` | 1 GHz | rate a pane transfer runs at during a burst |
| `n_pane_bit` | `062` | 1.67772e+07 bit | the pane, rounded down to a power of two so that its window aligns naturally in 038. It carries the unit because it is a quantity of bits rather than a count of things, and everything downstream of it is a size or a rate |
| `p_bond_fine` | `062` | 10 um | the finest pitch a permanent copper-to-copper bond reaches |
| `t_skew_window` | `065` | 500 ps | the arrival window the receiver must tolerate, stated as a contract the far end may rely on |

## What consumes it

Change one of these and the blueprints beside it are what break.

| symbol | read by |
|---|---|
| `a_driver` | `064` |
| `a_receiver` | `064` |
| `c_bond` | `064` |
| `f_circuit_over` | `064` |
| `L_bond` | `064` |
| `L_reach_bond` | `064` |
| `v_bounce_max` | `064` |
| `n_stagger` | `064` |
| `t_stagger` | `064` |
| `f_cond_gain` | `064` |
| `a_conductor` | `064` |
| `a_circuit` | `064` |
| `E_bit_derived` | `064` |
| `I_ssn_raw` | `064` |
| `I_ssn_mit` | `064` |
| `v_bounce` | `064` |
| `z_supply` | `064` |
| `f_gnd_min` | `063` |
| `t_stagger_all` | `064`, `065` |

## What it asserts

| tag | relation | because |
|---|---|---|
| `C-064-1` | `a_circuit < a_conductor * 0.4` | the circuit must fit in well under half the pitch, since the pad and its keep-out take the rest. This is the constraint that eliminates every technique a serial link uses |
| `C-064-2` | `E_bit_derived ~= e_pane_bit` | the energy derived from the capacitance and the swing must be the figure 062 budgets. Two routes, one number |
| `C-064-3` | `v_bounce < v_bounce_max` | supply bounce at the worst-case switching pattern, after conditioning and stagger, must leave the receiver able to resolve a bit. A pane of zeroes followed by a pane of ones is not a rare pattern |
| `C-064-4` | `L_bond < L_reach_bond` | the bond must be inside the reach where equalisation and clock recovery can be omitted |
| `C-064-5` | `t_stagger_all < t_skew_window` | the whole stagger sequence must fit inside the arrival window 065 already tolerates, so that this mitigation costs nothing that was not already spent |
| `C-064-6` | `I_ssn_mit < I_ssn_raw / 10` | the mitigations together must take an order of magnitude off the worst case |

