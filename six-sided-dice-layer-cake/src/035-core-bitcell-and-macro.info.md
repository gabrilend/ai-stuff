# 035-core-bitcell-and-macro — info

*Generated by `096` from the declarations in `035-core-bitcell-and-macro.md`. Do not edit this page;
edit the blueprint and run the sweep again.*

**The cell, and the array around it** — phase 5.
Described by `502`.

## What it publishes

| symbol | unit | kind | value | meaning |
|---|---|---|---|---|
| `a_cell` | um^2/bit | measured | 0.0199 um^2/bit | area of one six-transistor static memory cell at the chosen node. The unit is area per bit rather than area, which sounds pedantic until a density derived from it is compared against something and turns out to be a reciprocal area with no information in it |
| `eta_array` | 1 | measured | 0.55 | share of a memory array that is cells rather than decoders, sense amplifiers, wordline drivers and redundancy |
| `eta_tier` | 1 | given | 0.6 | share of a tier's area that is array rather than through-stack vias, test structures, correction routing and the seal ring |
| `t_access` | ns | measured | 0.75 ns | latency from a read being issued to the first bits arriving at the tier's port |
| `n_macro_tier` | 1 | given | 4096 | memory macros on one tier |
| `w_macro` | bit | derived | 10240 bit | bits a tier's macros deliver together per cycle; equal to the port width because the port is what they are sized to fill |
| `SNM_cell` | V | measured | 0.155 V | static noise margin of the cell at the low voltage, high temperature, slow process corner |
| `SNM_required` | V | given | 0.075 V | the least static noise margin that is considered safe against the soft error rate in 040 |
| `a_bit_array` | um^2/bit | derived | 0.0361818 um^2/bit | area one bit occupies inside an array |
| `a_bit_tier` | um^2/bit | derived | 0.060303 um^2/bit | and on a tier, once everything a tier needs besides array is counted |
| `d_areal` | MB/mm^2 | derived | 2.07286 MB/mm^2 | areal density. The unit conversion from bits per square micron to megabytes per square millimetre is the notation's job, not the author's, which is the second reason area per bit had to carry its bit |
| `d_areal_ref` | MB/mm^2 | measured | 1.78 MB/mm^2 | density a shipping stacked cache die reaches at an older node with a comparable arrangement, as a sanity comparison rather than a target |
| `t_cycle_core` | ns | derived | 0.8 ns | the core's cycle time, which the access time must fit inside |
| `E_core_write` | pJ/bit | measured | 0.55 pJ/bit | energy to write one bit into a core tier |
| `E_core_ret` | pJ/(bit*s) | measured | 20 pJ/(bit*s) | retention leakage per bit at the operating temperature: about twenty picowatts a cell, which is what makes an idle machine cost anything at all. Per bit per second, so it is a power per bit and not an energy |
| `P_core_leak_d` | W | derived | 12.7357 W | retention power of the whole stack, derived from the per-bit figure |
| `dV_read_need` | V | derived | 0.075 V | how far the array rail must sit above the logic rail for the cell to keep its margin while being read |

## What it consumes

| symbol | from | value | meaning |
|---|---|---|---|
| `C_core_raw` | `034` | 79.598 GB | raw capacity of the stack |
| `E_core_bit` | `020` | 0.48 pJ/bit | energy to read one bit out of a core tier, array and tier routing together |
| `P_core_leak` | `020` | 12.74 W | retention leakage of all twenty-four tiers at the operating temperature, from the per-cell figure 035 carries |
| `dV_read_marg` | `029` | 0.08 V | the least the array rail must exceed the logic rail by for a static memory cell to stay stable while it is read, at the worst process corner and the highest temperature |
| `f_core` | `034` | 1.25 GHz | core clock, set by the array access time in 035 |
| `w_tier_port` | `034` | 10240 bit | bits one tier delivers per cycle, set by its macro count and the routing it can support across a forty millimetre die |

## What consumes it

Change one of these and the blueprints beside it are what break.

| symbol | read by |
|---|---|
| `a_cell` | `035` |
| `eta_array` | `035` |
| `eta_tier` | `035` |
| `t_access` | `035`, `039` |
| `n_macro_tier` | `035` |
| `SNM_cell` | `035` |
| `SNM_required` | `035` |
| `a_bit_array` | `035` |
| `a_bit_tier` | `035` |
| `d_areal` | `034`, `035` |
| `d_areal_ref` | `035` |
| `t_cycle_core` | `035`, `036` |
| `E_core_write` | `035` |
| `E_core_ret` | `035` |
| `P_core_leak_d` | `035` |

## What it asserts

| tag | relation | because |
|---|---|---|
| `C-035-1` | `t_access < t_cycle_core * 2` | the array must deliver first data within a couple of cycles, or the latency in 052 grows and 048's prefetch has to run further ahead |
| `C-035-2` | `SNM_cell > SNM_required` | the cell must keep a usable noise margin at the worst corner. All three worst conditions at once, because that is when it actually fails and a check at any one of them is a check of nothing |
| `C-035-3` | `d_areal < d_areal_ref * 2` | the density claimed must be within a factor of two of what a shipping part with the same arrangement achieves at an older node. Static memory has nearly stopped scaling, so a claim far beyond this would be a claim about a process nobody has |
| `C-035-4` | `P_core_leak_d ~= P_core_leak` | retention power derived from the per-bit figure must agree with the figure 020 assumed. Two routes to the same watts |
| `C-035-5` | `eta_array * eta_tier > 0.25` | the two efficiencies together must leave at least a quarter of the tier as cells. Below that the tier is mostly overhead and the dedicated-array argument has stopped being true |
| `C-035-6` | `E_core_write > E_core_bit` | writing costs more than reading, which is true of static memory and catches the two being swapped |

## What it draws

- **from a bitcell to a megabyte per square millimetre [not-dimensioned]** — dimensions `a_cell`, `eta_array`, `a_bit_array`, `eta_tier`, `a_bit_tier`, `d_areal`

