# 078-model-capacity — info

*Generated by `096` from the declarations in `078-model-capacity.md`. Do not edit this page;
edit the blueprint and run the sweep again.*

**What fits** — phase 11.
Described by `1104`.

## What it publishes

| symbol | unit | kind | value | meaning |
|---|---|---|---|---|
| `n_layer` | 1 | given | 80 | transformer layers |
| `d_model` | 1 | given | 8192 | the residual stream's width |
| `n_head` | 1 | given | 64 | attention heads |
| `n_kv_head` | 1 | given | 8 | key and value heads, fewer than query heads so the cache is smaller |
| `d_head` | 1 | given | 128 | width of one head |
| `d_ff` | 1 | given | 28672 | the feedforward's inner width |
| `n_vocab` | 1 | given | 128000 | vocabulary entries |
| `n_ctx` | 1 | given | 4096 | context length the machine is provisioned for |
| `p_attn` | 1 | derived | 1.50995e+08 | parameters in one layer's attention: queries, keys, values and the projection back |
| `p_ffn` | 1 | derived | 7.04643e+08 | parameters in one gated feedforward: two up, one down |
| `p_layer` | 1 | derived | 8.55638e+08 | parameters in one transformer layer |
| `p_embed` | 1 | derived | 1.04858e+09 | the embedding table |
| `p_head` | 1 | derived | 1.04858e+09 | the output projection, the same size |
| `n_param` | 1 | derived | 7.05482e+10 | the whole model |
| `n_reduce_max` | 1 | derived | 28672 | the longest reduction anywhere in a forward pass, which 077 sizes the accumulator against |
| `C_weights` | GB | derived | 36.3764 GB | the weights, resident, at the format in 046 |
| `C_layer_weights` | MB | derived | 441.188 MB | one transformer layer's share |
| `C_layer_max` | MB | derived | 540.672 MB | the largest single thing a face must hold: the output projection, which is bigger than any layer |
| `C_activation` | MB | derived | 0.016384 MB | one token's activation vector, which is what crosses between stages |
| `C_kv_seq` | MB | derived | 671.089 MB | the cache one sequence needs at full context |
| `C_kv` | GB | derived | 18.7905 GB | and the whole batch's |
| `C_checkpoint` | GB | given | 0 GB | activation checkpoints; zero unless 076a's training is in use |
| `C_adapter` | GB | given | 0 GB | adapter and optimiser state; the same |
| `flop_token` | flop | derived | 1.41096e+11 flop | operations to generate one token: two per weight |
| `f_kv_of_res` | 1 | derived | 0.340612 | the cache's share of residency, which is what makes capacity a surface |
| `n_ctx_at_full` | 1 | derived | 7753.43 | the longest context that fits at this model and batch |

## What it consumes

| symbol | from | value | meaning |
|---|---|---|---|
| `C_core_usable` | `034` | 71.9454 GB | what a model may actually use |
| `C_face_slice` | `047` | 1157.79 MB | and of a whole face's |
| `C_resident` | `059` | 55.1669 GB | what a model needs resident: weights and cache together |
| `batch_design` | `047` | 28 | the batch the machine is provisioned for; 079 derives the crossover independently and C-079-1 requires the two to agree |
| `n_flop_mac` | `045` | 2 flop | operations one multiply-accumulate cell performs per cycle: a multiply and an add. It carries the unit so that an operation rate is a rate of operations rather than a bare reciprocal second, which is the distinction that keeps a throughput from being added to a clock |
| `w_act` | `046` | 16 bit | activations in the residual stream |
| `w_kv` | `046` | 8 bit | key and value cache entries |
| `w_weight_eff` | `046` | 4.125 bit | bits per weight including the amortised scale, which is the number that decides how long a token takes |

## What consumes it

Change one of these and the blueprints beside it are what break.

| symbol | read by |
|---|---|
| `n_layer` | `075`, `076`, `076a`, `077`, `078` |
| `d_model` | `076`, `076a`, `078` |
| `n_head` | `076`, `078` |
| `n_kv_head` | `076`, `078` |
| `d_head` | `058`, `076`, `078` |
| `d_ff` | `076`, `076a`, `078` |
| `n_vocab` | `078` |
| `n_ctx` | `058`, `059`, `076`, `076a`, `078` |
| `p_attn` | `078` |
| `p_ffn` | `078` |
| `p_layer` | `053`, `078` |
| `p_embed` | `078` |
| `p_head` | `078` |
| `n_param` | `046`, `076a`, `078` |
| `n_reduce_max` | `045`, `077`, `078` |
| `C_weights` | `038`, `046`, `050`, `052`, `053`, `055`, `057`, `058`, `059`, `061`, `069b`, `075`, `076`, `076a`, `078`, `079`, `080` |
| `C_layer_weights` | `041`, `047`, `048`, `060`, `061`, `075`, `076`, `078` |
| `C_layer_max` | `075`, `078` |
| `C_activation` | `053`, `076`, `076a` |
| `C_kv_seq` | `078` |
| `C_kv` | `038`, `059`, `078` |
| `C_checkpoint` | `038` |
| `C_adapter` | `038` |
| `flop_token` | `053`, `076a`, `079`, `080` |
| `f_kv_of_res` | `078` |
| `n_ctx_at_full` | `078` |

## What it asserts

| tag | relation | because |
|---|---|---|
| `C-078-1` | `C_resident < C_core_usable` | the reference model at its reference context and batch must fit |
| `C-078-2` | `C_face_slice > C_layer_max + C_layer_weights` | a face must hold the largest thing it will ever compute and the next thing behind it. The largest is the output projection rather than a transformer layer, which is the case a constraint written against an average layer would have missed |
| `C-078-3` | `f_kv_of_res > 0.1` | the cache must be a real part of residency at the reference context, or the surface has collapsed to a line and 059's degradation to a shorter context buys nothing |
| `C-078-4` | `n_ctx_at_full > n_ctx` | there must be room to grow the context beyond the reference, which is the graceful degradation 059 offers in the other direction |
| `C-078-5` | `n_head >= n_kv_head` | there are at least as many query heads as key heads, which is what makes the cache smaller than it would otherwise be |
| `C-078-6` | `d_head * n_head == d_model` | the heads must tile the residual stream exactly |
| `C-078-7` | `n_reduce_max >= d_model` | the longest reduction is at least the residual width, which catches a feedforward narrower than the stream it feeds |

## What it draws

- **the shape everything is anchored to [not-dimensioned]** — dimensions `n_layer`, `d_model`, `n_head`, `n_kv_head`, `d_ff`, `n_vocab`, `d_model`

