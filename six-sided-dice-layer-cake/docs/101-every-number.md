# Every number in the machine

*Generated by `097`. One row per symbol: what it is called, what it is
measured in, where it came from, what it comes to, and why it exists.*

## 010 — The frame of reference

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_face` | 1 | given | 6 | — | compute faces, one per side of the cube |
| `n_corner` | 1 | given | 8 | — | corners of the cube, each a coolant manifold block |
| `n_edge` | 1 | given | 12 | — | edges, each carrying a supply and a return channel |
| `n_edge_per_corner` | 1 | given | 3 | — | edges meeting at one corner |
| `n_stage` | 1 | derived | 6 | `n_face` | pipeline stages a token falls through, one per face |
| `n_corner_in` | 1 | derived | 4 | `n_corner / 2` | corners where coolant enters, the even-parity set |
| `n_corner_out` | 1 | derived | 4 | `n_corner / 2` | corners where coolant leaves, the odd-parity set |
| `n_face_pair` | 1 | derived | 3 | `n_face / 2` | pairs of opposite faces |

## 011 — Material properties

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `k_si` | W/(m*K) | measured | 110 W/(m*K) | — | thermal conductivity of silicon at 350 K, a third below the room-temperature figure |
| `rho_si` | kg/m^3 | measured | 2329 kg/m^3 | — | density of silicon at 300 K |
| `cp_si` | J/(kg*K) | measured | 705 J/(kg*K) | — | specific heat capacity of silicon at 300 K |
| `cte_si` | ppm/K | measured | 2.6 ppm/K | — | linear thermal expansion of silicon, 300 K to 400 K |
| `E_si` | GPa | measured | 130 GPa | — | Young's modulus of silicon, averaged over orientation |
| `sigma_si_frac` | MPa | measured | 150 MPa | — | fracture stress of thinned silicon with an ordinary sawn or laser-diced edge, where microcracks from the cut are what fail |
| `sigma_si_plas` | MPa | measured | 350 MPa | — | the same for a plasma-diced edge, which etches rather than cuts and leaves no crack population; 018 requires this and it is a process requirement rather than a preference |
| `T_si_max` | K | measured | 378 K | — | highest junction temperature the silicon is qualified to, 105 degrees |
| `k_cu_bulk` | W/(m*K) | measured | 398 W/(m*K) | — | thermal conductivity of bulk annealed copper at 350 K |
| `k_cu_plated` | W/(m*K) | measured | 350 W/(m*K) | — | the same for electroplated copper in a via, where the grains are smaller |
| `k_cu_film` | W/(m*K) | measured | 260 W/(m*K) | — | the same for a thin film, where grain size approaches the electron mean free path |
| `rho_cu` | kg/m^3 | measured | 8960 kg/m^3 | — | density of copper |
| `cp_cu` | J/(kg*K) | measured | 385 J/(kg*K) | — | specific heat capacity of copper |
| `cte_cu` | ppm/K | measured | 16.5 ppm/K | — | linear thermal expansion of copper, 300 K to 400 K |
| `E_cu` | GPa | measured | 117 GPa | — | Young's modulus of copper |
| `res_cu` | ohm*m | measured | 1.9e-08 ohm*m | — | electrical resistivity of copper at 350 K |
| `j_em_cu` | mA/um^2 | measured | 1 mA/um^2 | — | electromigration current density limit for copper at 350 K over a ten year life |
| `k_cumo` | W/(m*K) | measured | 190 W/(m*K) | — | thermal conductivity of a fifteen per cent copper, eighty-five per cent molybdenum composite |
| `rho_cumo` | kg/m^3 | measured | 1e+04 kg/m^3 | — | density of the same |
| `cp_cumo` | J/(kg*K) | measured | 250 J/(kg*K) | — | specific heat capacity of the same |
| `cte_cumo` | ppm/K | measured | 7 ppm/K | — | linear thermal expansion of the same; chosen for this number and no other |
| `E_cumo` | GPa | measured | 280 GPa | — | Young's modulus of the same |
| `k_w` | W/(m*K) | measured | 174 W/(m*K) | — | thermal conductivity of tungsten, for the through-silicon vias |
| `res_w` | ohm*m | measured | 5.6e-08 ohm*m | — | electrical resistivity of tungsten at 350 K |
| `cte_glass` | ppm/K | measured | 3.2 ppm/K | — | linear thermal expansion of the interposer's glass core, chosen to sit near silicon |
| `k_glass` | W/(m*K) | measured | 1.1 W/(m*K) | — | thermal conductivity of the same, which is poor and does not matter because heat leaves the other way |
| `rho_ss` | kg/m^3 | measured | 7900 kg/m^3 | — | density of stainless steel |
| `cp_ss` | J/(kg*K) | measured | 500 J/(kg*K) | — | specific heat capacity of the same, which is what decides how long the steel in the rails and corners holds its heat once the power is off |
| `k_ss` | W/(m*K) | measured | 16 W/(m*K) | — | thermal conductivity of stainless steel |
| `cte_ss` | ppm/K | measured | 17.3 ppm/K | — | linear thermal expansion of stainless steel |
| `E_ss` | GPa | measured | 193 GPa | — | Young's modulus of stainless steel |
| `sigma_ss_y` | MPa | measured | 290 MPa | — | yield stress of annealed stainless steel |
| `rho_water` | kg/m^3 | measured | 989 kg/m^3 | — | density of water at 320 K, the mean coolant temperature |
| `cp_water` | J/(kg*K) | measured | 4180 J/(kg*K) | — | specific heat capacity of water at 320 K |
| `k_water` | W/(m*K) | measured | 0.63 W/(m*K) | — | thermal conductivity of water at 320 K |
| `mu_water` | Pa*s | measured | 0.000577 Pa*s | — | dynamic viscosity of water at 320 K, a third below its value at 293 K |
| `T_water_frz` | K | measured | 273 K | — | freezing point of untreated water, which is why 1202 says how a cube travels |
| `rho_fluoro` | kg/m^3 | measured | 1600 kg/m^3 | — | density of a perfluorinated coolant at 320 K |
| `cp_fluoro` | J/(kg*K) | measured | 1100 J/(kg*K) | — | specific heat capacity of the same |
| `k_fluoro` | W/(m*K) | measured | 0.065 W/(m*K) | — | thermal conductivity of the same, a tenth of water's |
| `mu_fluoro` | Pa*s | measured | 0.0012 Pa*s | — | dynamic viscosity of the same |
| `T_fluoro_frz` | K | measured | 195 K | — | freezing point of the same, which is the one property where it beats water |
| `c_light` | m/s | measured | 2.998e+08 m/s | — | the speed of light in vacuum. Here for the same reason the permittivity is: a literal in this notation is dimensionless, so a physical constant is a symbol |
| `eps_0` | F/m | measured | 8.854e-12 F/m | — | permittivity of free space. It is here rather than written into a derivation because every literal in this notation is dimensionless, so a physical constant has to be a symbol like any other quantity |
| `eps_ox` | 1 | measured | 3.9 | — | relative permittivity of the oxide liner around a through-silicon via |
| `k_diamond` | W/(m*K) | measured | 2000 W/(m*K) | — | thermal conductivity of diamond; nothing here is made of it, and it is the ceiling any other conductivity must sit under |
| `Pr_water` | 1 | derived | 3.828 | `mu_water * cp_water / k_water` | Prandtl number of water at 320 K, computed from the three properties above |
| `Pr_fluoro` | 1 | derived | 20.31 | `mu_fluoro * cp_fluoro / k_fluoro` | Prandtl number of the fluorocarbon at 320 K |
| `cte_ratio` | 1 | derived | 6.346 | `cte_cu / cte_si` | how much faster copper expands than silicon; the number 018 exists because of |
| `cte_ratio_mo` | 1 | derived | 2.692 | `cte_cumo / cte_si` | the same for the molybdenum composite, which is why the core uses it |

## 012 — Master dimensions

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `L_cube` | mm | given | 60 mm | — | outer edge length of the finished cube; follows from the four dimensions below it in the chain |
| `t_face` | mm | given | 7 mm | — | one face assembly, outward surface to inward surface; the stack in 014 adds to just under this |
| `t_cage` | mm | given | 3 mm | — | the switch shell lining the inside of the cavity, sized by the crossbar area in 037 |
| `w_rail` | mm | given | 4 mm | — | width of an edge rail, taken off each edge of each face plate; sized by the duct area 024 needs |
| `L_die` | mm | given | 24 mm | — | edge of one compute die; two thirds of a reticle field, and half its area is the slice in 047 |
| `w_street` | mm | given | 1 mm | — | gap between two dies on a face, for placement tolerance and the seal ring |
| `t_tier_si` | mm | given | 0.05 mm | — | thickness of one thinned memory tier; as thin as a tier can be handled |
| `t_lamina` | mm | given | 1.617 mm | — | thickness of one cooling lamina between two tiers. It is what is left of the core's height once twenty-four tiers are laid in it, and the tier count came out of 034's capacity chain rather than being chosen |
| `w_uchan` | mm | given | 0.15 mm | — | width of one microchannel in a face cold plate; this is the number that sets the heat transfer coefficient |
| `h_uchan` | mm | given | 1 mm | — | depth of the same channel, limited by fin efficiency rather than by etching |
| `w_ufin` | mm | given | 0.15 mm | — | wall between two microchannels, set by the plate's pressure rating in 017 |
| `L_cavity` | mm | derived | 46 mm | `L_cube - 2*t_face` | edge of the space the six faces enclose |
| `L_core` | mm | derived | 40 mm | `L_cavity - 2*t_cage` | edge of the memory block the cage encloses |
| `L_plate` | mm | derived | 52 mm | `L_cube - 2*w_rail` | edge of a face plate, once the edge rails are taken off |
| `L_dieblock` | mm | derived | 49 mm | `2*L_die + w_street` | edge of the four-die array on a face |
| `w_seal` | mm | derived | 1.5 mm | `(L_plate - L_dieblock) / 2` | seal ring left around the die block on each side |
| `t_tier_pitch` | mm | derived | 1.667 mm | `t_tier_si + t_lamina` | one repeating unit of the core stack |
| `p_uchan` | mm | derived | 0.3 mm | `w_uchan + w_ufin` | pitch of the microchannel field |
| `n_uchan` | 1 | derived | 173 | `floor(L_plate / p_uchan)` | microchannels across one face cold plate |
| `ar_uchan` | 1 | derived | 6.667 | `h_uchan / w_uchan` | aspect ratio of one microchannel |
| `A_plate` | mm^2 | derived | 2704 mm^2 | `L_plate^2` | area of one face plate |
| `A_die` | mm^2 | derived | 576 mm^2 | `L_die^2` | area of one compute die |
| `A_core_side` | mm^2 | derived | 1600 mm^2 | `L_core^2` | area of one side of the core block, and of one tier |
| `V_cube` | mm^3 | derived | 2.16e+05 mm^3 | `L_cube^3` | volume of the whole object |
| `n_die` | 1 | derived | 24 | `4 * n_face` | compute dies in the machine |
| `A_die_total` | mm^2 | derived | 1.382e+04 mm^2 | `n_die * A_die` | total compute die area, which 083 turns into a yield problem |

## 013 — The cube envelope

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `tol_L_cube` | mm | given | 0.05 mm | — | tolerance on the cube's outer edge, achievable by grinding the assembled rails |
| `tol_L_plate` | mm | given | 0.025 mm | — | tolerance on a face plate's edge |
| `flat_plate` | mm | given | 0.05 mm | — | flatness of a face plate over its full width, at any temperature in the operating range. Fifteen microns was tried first, which is what the process achieves cold; 018 then found the assembly bows forty-five when hot, and a flatness figure that only holds at one temperature is not one |
| `tol_stack_n` | 1 | given | 4 | — | face plates in the loop the tolerance stack closes around |
| `A_exterior` | mm^2 | derived | 2.16e+04 mm^2 | `n_face * L_cube^2` | outer surface area of the cube, ignoring the corner chamfers |
| `tol_loop` | mm | derived | 0.3 mm | `tol_stack_n * (tol_L_plate + flat_plate)` | worst-case accumulation around one great circle, where every plate errs the same way |
| `t_coldplate` | mm | given | 2 mm | — | thickness of a face cold plate, base and channels and cover together |
| `t_interposer` | mm | given | 1.5 mm | — | thickness of a face interposer, glass core with its planes |
| `t_die` | mm | given | 0.1 mm | — | thickness of a compute die after thinning |
| `rho_glass` | kg/m^3 | measured | 2500 kg/m^3 | — | density of the interposer's glass core |
| `f_solid_rail` | 1 | given | 0.6 | — | fraction of an edge rail's square section that is metal rather than channel |
| `f_solid_plate` | 1 | given | 0.75 | — | fraction of a cold plate that is silicon rather than channel |
| `L_corner` | mm | given | 12 mm | — | edge of a corner manifold block, set by the two chambers that have to fit inside it (015) |
| `f_solid_corner` | 1 | given | 0.55 | — | fraction of a corner block that is metal rather than chamber |
| `rho_cage` | kg/m^3 | given | 3000 kg/m^3 | — | mean density of the cage shell, silicon and copper and void together |
| `m_ports` | kg | given | 0.15 kg | — | port fields, regulators and connectors on all six faces, weighed as an assembly |
| `m_laminae` | kg | derived | 0.6209 kg | `n_tier * A_core_side * t_lamina * rho_cumo` | the cooling plates inside the core, one between every pair of memory tiers, which are most of the machine's mass |
| `m_tiers` | kg | derived | 0.004472 kg | `n_tier * A_core_side * t_tier_si * rho_si` | the memory tiers between them; 036 derives how many there are from the bitcell density rather than choosing a round number |
| `m_coldplate` | kg | derived | 0.05668 kg | `n_face * A_plate * t_coldplate * f_solid_plate * rho_si` | six silicon cold plates, less the channels etched out of them |
| `m_dies` | kg | derived | 0.00322 kg | `n_die * A_die * t_die * rho_si` | twenty-four compute dies |
| `m_interposer` | kg | derived | 0.06084 kg | `n_face * A_plate * t_interposer * rho_glass` | six face interposers |
| `m_rails` | kg | derived | 0.04732 kg | `n_edge * w_rail * w_rail * L_plate * f_solid_rail * rho_ss` | twelve edge rails |
| `m_corners` | kg | derived | 0.06007 kg | `n_corner * L_corner^3 * f_solid_corner * rho_ss` | eight corner manifold blocks |
| `m_cage` | kg | derived | 0.1 kg | `(L_cavity^3 - L_core^3) * rho_cage` | the switch shell filling the space between the cavity wall and the core |
| `m_coolant` | kg | derived | 0.02284 kg | `V_coolant * rho_water` | the fluid standing in the machine when it is running |
| `m_cube` | kg | derived | 1.126 kg | `m_laminae + m_tiers + m_coldplate + m_dies + m_interposer + m_rails + m_corners + m_cage + m_ports + m_coolant` | the finished object, wet |
| `rho_mean` | kg/m^3 | derived | 5215 kg/m^3 | `m_cube / V_cube` | mean density of the whole machine |

## 014 — One face, in section

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `t_radial_pad` | mm | given | 0.02 mm | — | copper pillar field on the inward surface, meeting the cage |
| `t_microbump` | mm | given | 0.04 mm | — | solder microbump array between the dies and the interposer |
| `t_bond` | mm | given | 0.01 mm | — | copper-to-copper hybrid bond from the die backs to the cold plate |
| `t_regulator` | mm | given | 1.5 mm | — | the voltage regulator tier and the port field substrate together |
| `t_land` | mm | given | 0.3 mm | — | the plated land array a connector mates to |
| `h_plenum` | mm | given | 1.13 mm | — | height of the coolant distribution plenum across the cold plate's width; it gave seventy microns to the seal when the cord had to grow |
| `t_seal_travel` | mm | given | 0.4 mm | — | compression travel allowed for the face-to-rail seal, set by 017's cord after the bow in 018 forced the flatness allowance up |
| `n_island` | 1 | given | 16 | — | via islands per cold plate, where channels stop and conductors pass |
| `L_island` | mm | given | 3 mm | — | edge of one square via island |
| `p_island_pad` | mm | given | 0.2 mm | — | pad pitch inside a via island |
| `t_stack` | mm | derived | 5.47 mm | `t_radial_pad + t_interposer + t_microbump + t_die + t_bond + t_coldplate + t_regulator + t_land` | everything solid in a face assembly, inward surface to outward |
| `t_face_used` | mm | derived | 7 mm | `t_stack + h_plenum + t_seal_travel` | the whole face thickness accounted for |
| `A_island` | mm^2 | derived | 9 mm^2 | `L_island^2` | area of one via island |
| `A_island_all` | mm^2 | derived | 144 mm^2 | `n_island * A_island` | area taken out of the channel field by all sixteen |
| `n_island_pad` | 1 | derived | 3600 | `n_island * floor(L_island / p_island_pad)^2` | conductors that can cross the cold plate, in total |
| `f_island_area` | 1 | derived | 0.05325 | `A_island_all / A_plate` | fraction of the cold plate given over to feedthroughs |
| `n_island_seal` | 1 | derived | 96 | `n_island * n_face` | seal features created by this decision, one ring per island per plate |

## 015 — The corner manifold block

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `A_chamber` | mm^2 | given | 14 mm^2 | — | cross-section of one chamber inside a corner block; two of these and their walls are what set the block's size |
| `t_chamber_wall` | mm | given | 1.6 mm | — | the wall separating the supply chamber from the return chamber |
| `t_outer_wall` | mm | given | 1.2 mm | — | wall between a chamber and the outside of the block |
| `d_port` | mm | given | 6 mm | — | bore of the external coolant fitting on the four ported blocks |
| `F_corner_rating` | N | given | 4000 N | — | load one corner block will carry at a mount point without yielding, from its section and the steel in 011 |
| `K_tee` | 1 | given | 1.1 | — | loss coefficient for a three-way division at this geometry and Reynolds number |
| `n_seal_corner` | 1 | given | 9 | — | seal lines where three rails and three plates meet one block |
| `V_chamber` | mm^3 | derived | 168 mm^3 | `A_chamber * L_corner` | volume of one chamber |
| `V_corner_wet` | mm^3 | derived | 2688 mm^3 | `2 * V_chamber * n_corner` | fluid standing in all eight corner blocks |
| `Q_corner_in` | m^3/s | derived | 1.466e-05 m^3/s | `Q_total / n_corner_in` | volumetric flow entering one inlet block |
| `v_corner` | m/s | derived | 1.047 m/s | `Q_corner_in / A_chamber` | velocity in an inlet block's supply chamber |
| `dp_corner` | Pa | derived | 596.5 Pa | `K_tee * rho_water * v_corner^2 / 2` | pressure lost dividing three ways in one block |
| `f_corner_loss` | 1 | derived | 0.03161 | `dp_corner / dp_loop` | that loss as a fraction of the whole loop's |
| `p_burst_wall` | Pa | derived | 3.535e+07 Pa | `2 * sigma_ss_y * t_chamber_wall^2 / (3 * A_chamber)` | pressure a flat wall of this thickness spanning this chamber will take before yielding |
| `m_corner_one` | kg | derived | 0.007508 kg | `L_corner^3 * f_solid_corner * rho_ss` | mass of one block |

## 016 — The edge rail

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `w_rail_chan` | mm | given | 3.2 mm | — | width of one channel in a rail, across the rail's square section |
| `h_rail_chan` | mm | given | 1.35 mm | — | height of the same; the two channels are stacked and this is what is left after the walls |
| `t_rail_web` | mm | given | 0.5 mm | — | metal between the supply channel and the return channel |
| `t_rail_wall` | mm | given | 0.4 mm | — | metal between a channel and the outside of the rail |
| `K_rail_ends` | 1 | given | 1.5 | — | entrance and exit loss coefficients for one rail, summed |
| `A_rail_chan` | mm^2 | derived | 4.32 mm^2 | `w_rail_chan * h_rail_chan` | cross-section of one channel in a rail; there are two, one supply and one return |
| `L_rail` | mm | derived | 36 mm | `L_cube - 2 * L_corner` | length of a rail between the two corner blocks it joins |
| `D_rail` | mm | derived | 1.899 mm | `2 * w_rail_chan * h_rail_chan / (w_rail_chan + h_rail_chan)` | hydraulic diameter of a rectangular rail channel |
| `n_rail_chan` | 1 | derived | 24 | `2 * n_edge` | rail channels in the machine, supply and return on every edge |
| `Q_rail` | m^3/s | derived | 9.774e-06 m^3/s | `Q_total / n_face` | volumetric flow along one supply rail: a rail feeds one face's field, and there are six of those |
| `v_rail` | m/s | derived | 2.262 m/s | `Q_rail / A_rail_chan` | velocity in a rail channel |
| `Re_rail` | 1 | derived | 7364 | `rho_water * v_rail * D_rail / mu_water` | Reynolds number in a rail; expected to be transitional, which is the awkward regime |
| `f_rail` | 1 | derived | 0.03411 | `0.316 / Re_rail^0.25` | Darcy friction factor from the Blasius correlation, valid to about Re of one hundred thousand |
| `dp_rail` | Pa | derived | 5434 Pa | `(f_rail * L_rail / D_rail + K_rail_ends) * rho_water * v_rail^2 / 2` | pressure lost along one rail including its ends |
| `f_rail_loss` | 1 | derived | 0.2879 | `dp_rail / dp_loop` | that loss as a fraction of the whole loop's |
| `V_rail_wet` | mm^3 | derived | 3732 mm^3 | `n_rail_chan * A_rail_chan * L_rail` | fluid standing in all twenty-four rail channels |
| `I_rail` | mm^4 | derived | 9.452 mm^4 | `w_rail^4 / 12 - 2 * A_rail_chan^2 / pi` | second moment of area of a rail about a face-parallel axis, the square section less its two bores |
| `m_rail_one` | kg | derived | 0.00273 kg | `w_rail^2 * L_rail * f_solid_rail * rho_ss` | mass of one rail |
| `p_burst_web` | Pa | derived | 1.119e+07 Pa | `2 * sigma_ss_y * t_rail_web^2 / (3 * w_rail_chan * h_rail_chan)` | pressure the web between a rail's two channels will take as a flat plate before yielding |

## 017 — The seals

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `p_work` | bar | given | 2 bar | — | working pressure of the coolant loop, pump head plus static |
| `mult_proof` | 1 | given | 3 | — | proof pressure as a multiple of working; an ordinary figure, held as a symbol so the whole set can be raised at once |
| `mult_burst` | 1 | given | 5 | — | burst pressure as the same |
| `d_cord` | mm | given | 2.5 mm | — | diameter of the elastomer cord in a compression groove. One millimetre was tried first and fell fifteen microns short of the tolerance loop; the bow that 018 then found pushed the flatness allowance from fifteen microns to fifty and the cord with it |
| `squeeze_min` | 1 | given | 0.15 | — | least fractional squeeze at which the cord seals |
| `squeeze_max` | 1 | given | 0.3 | — | most it tolerates before compression set over the cycle count is unacceptable |
| `leak_per_seal` | mbar*L/s | measured | 1e-09 mbar*L/s | — | helium leak rate per joint at proof pressure for a good elastomer seal in a groove. A leak rate quoted this way is a pressure-volume throughput and therefore has the dimensions of power, which is worth knowing before dividing it by anything |
| `comp_set` | 1 | measured | 0.2 | — | fraction of its squeeze an elastomer of this class loses permanently over a hundred thousand cycles at temperature |
| `w_seal_min` | mm | given | 1.2 mm | — | least width of face plate rim a groove and its land need |
| `n_seal_plate` | 1 | derived | 24 | `4 * n_face` | face-plate-to-rail joints |
| `n_seal_rail` | 1 | derived | 24 | `2 * n_edge` | rail-to-corner joints |
| `n_seal_cover` | 1 | derived | 6 | `n_face` | cold plate cover bonds |
| `n_seal_fitting` | 1 | derived | 8 | `n_corner` | external fittings |
| `n_seal_total` | 1 | derived | 166 | `n_seal_plate + n_seal_rail + n_corner + n_island_seal + n_seal_cover + n_seal_fitting` | every joint in the machine that keeps coolant on one side of itself |
| `p_proof` | Pa | derived | 6e+05 Pa | `p_work * mult_proof` | pressure every wall and every seal must hold without leaking |
| `p_burst` | Pa | derived | 1e+06 Pa | `p_work * mult_burst` | pressure at which a wall is permitted to fail |
| `seal_compression_range` | mm | derived | 0.375 mm | `d_cord * (squeeze_max - squeeze_min)` | the band of gap variation a groove can take up while still sealing |
| `seal_end_of_life` | mm | derived | 0.4688 mm | `d_cord * squeeze_min / (1 - comp_set)` | squeeze that must be present when new, so that after a hundred thousand cycles of set there is still enough left to seal |
| `leak_total` | mbar*L/s | derived | 1.66e-07 mbar*L/s | `n_seal_total * leak_per_seal` | throughput of every joint in the machine added together |
| `Q_leak_seal` | m^3/s | derived | 5e-16 m^3/s | `leak_per_seal / p_work` | volumetric loss at one joint at working pressure; a throughput divided by the pressure driving it is a volume flow |
| `Q_leak_total` | m^3/s | derived | 8.3e-14 m^3/s | `n_seal_total * Q_leak_seal` | and for all hundred and sixty-six of them |
| `L_seal_line` | mm | derived | 1248 mm | `n_seal_plate * L_plate` | length of compression seal line around the face plates alone |

## 018 — What heating it does to it

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `dT_assembly` | K | given | 130 K | — | swing from the hybrid bonding temperature down to room, taken once |
| `dT_power` | K | given | 60 K | — | swing from cold to operating, taken a hundred thousand times |
| `dT_load` | K | given | 8 K | — | local swing at a die when its engine goes idle to full and back |
| `n_cyc_power` | 1 | given | 1e+05 | — | power cycles over the machine's life, from 086 |
| `n_cyc_load` | 1 | given | 4e+12 | — | load steps over the same, at thirteen layers a token and a thousand tokens a second |
| `bow_coeff` | ppm/K | measured | 12 ppm/K | — | effective expansion mismatch of the face stack about its own neutral axis, six times the layer-to-layer difference, which is what sets its curvature |
| `strain_tier` | 1 | derived | 0.000264 | `(cte_cumo - cte_si) * dT_power` | strain at a tier-to-lamina interface over a power cycle |
| `strain_glass` | 1 | derived | 3.6e-05 | `(cte_glass - cte_si) * dT_power` | the same at a die-to-interposer interface |
| `strain_plate` | 1 | derived | 0.000882 | `(cte_ss - cte_si) * dT_power` | the same at a plate-to-rail joint, which is a seal and not a bond |
| `strain_cu_alt` | 1 | derived | 0.000834 | `(cte_cu - cte_si) * dT_power` | what the tier interface would be if the laminae were copper; the number that chose the material |
| `disp_tier` | mm | derived | 0.01056 mm | `strain_tier * L_core` | relative motion at a tier interface, corner to corner |
| `disp_plate` | mm | derived | 0.04586 mm | `strain_plate * L_plate` | the same at a plate-to-rail joint |
| `disp_cu_alt` | mm | derived | 0.03336 mm | `strain_cu_alt * L_core` | and what copper would have moved |
| `sigma_tier` | Pa | derived | 3.432e+07 Pa | `E_si * strain_tier` | stress in the silicon at a tier interface, taking the bond as rigid, which is the pessimistic case and the cheap one to compute |
| `sigma_glass` | Pa | derived | 4.68e+06 Pa | `E_si * strain_glass` | the same at a die-to-interposer interface |
| `sigma_cu_alt` | Pa | derived | 1.084e+08 Pa | `E_si * strain_cu_alt` | and what copper laminae would have induced |
| `sigma_assembly` | Pa | derived | 7.436e+07 Pa | `E_si * (cte_cumo - cte_si) * dT_assembly` | residual stress frozen into a tier interface at bonding, before the machine is ever switched on |
| `sigma_cu_assy` | Pa | derived | 2.349e+08 Pa | `E_si * (cte_cu - cte_si) * dT_assembly` | residual a copper lamina would have frozen in at bonding |
| `sigma_cu_total` | Pa | derived | 3.433e+08 Pa | `sigma_cu_alt + sigma_cu_assy` | everything a copper lamina would put into the silicon, operating and residual together |
| `margin_tier` | 1 | derived | 3.22 | `sigma_si_plas / (sigma_tier + sigma_assembly)` | how many times the fracture stress of a plasma-diced edge exceeds what a tier interface actually carries, residual included |
| `margin_cu_alt` | 1 | derived | 1.019 | `sigma_si_plas / sigma_cu_total` | the same margin copper laminae would have left |
| `bow_face` | mm | derived | 0.04449 mm | `bow_coeff * dT_power / t_stack * L_plate^2 / 8` | mid-span bow of a face assembly over a power cycle, taken as a circular arc of curvature bow_coeff times the swing over the stack thickness |

## 019 — Mounting, and taking it away

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_mount` | 1 | given | 4 | — | mount points, on the four corner blocks of one chosen face |
| `n_mount_rigid` | 1 | given | 3 | — | of those, how many are rigid; the fourth is compliant so the frame does not over-constrain the cube |
| `g_shock` | 1 | given | 50 | — | shock the mounted assembly must survive in transit, in multiples of gravity |
| `g_accel` | m/s^2 | measured | 9.81 m/s^2 | — | standard gravity |
| `d_bolt` | mm | given | 3 mm | — | bolt diameter at each mount point; four millimetres needs exactly the whole corner block and leaves nothing around it |
| `V_coupling` | mm^3 | measured | 100 mm^3 | — | fluid lost when one self-sealing coupling is parted, from the coupling's own data |
| `n_service` | 1 | given | 5 | — | service events -- cube replacements -- expected over the life of an installation |
| `cte_frame` | ppm/K | derived | 17.3 ppm/K | `cte_ss` | the frame is steel, and so is most of the cube's exterior |
| `dT_frame` | K | given | 40 K | — | temperature difference the frame and the cube can be at, worst case |
| `T_touch` | K | given | 318 K | — | the warmest surface a person may put a bare hand on. Forty-five degrees Celsius, which is the ordinary limit for a metal surface somebody has to grip rather than brush past |
| `n_tau_cool` | 1 | given | 3 | — | thermal time constants to wait before the cube is called cool. Three leaves five per cent of the excess, and the fourth would buy one per cent for another third of the wait |
| `t_stop` | s | given | 60 s | — | stopping the machine in an orderly way: finish the token in flight, drain the six pipeline stages, park the model, stop the clock |
| `t_valve` | s | given | 120 s | — | closing the isolation valves either side of the cube and letting the loop pressure fall to nothing, twice -- once out and once in |
| `t_couple` | s | given | 45 s | — | parting or making one self-sealing coolant coupling by hand, including wiping the face |
| `t_bolt` | s | given | 90 s | — | releasing or torquing one mount point, including checking the torque |
| `t_lift` | s | given | 60 s | — | lifting the cube out of the frame and the replacement into it. A sixty-millimetre cube weighing a little over a kilogram wet is a one-hand object, and the time is care rather than effort |
| `t_align` | s | given | 180 s | — | seating the replacement against the three rigid mounts and setting the compliant one, which is where the time goes if it goes anywhere |
| `t_purge` | s | given | 600 s | — | filling and purging the new cube, until no air returns from the outlet corners |
| `m_mounted` | kg | derived | 1.126 kg | `m_cube` | mass the frame carries |
| `F_mount_static` | N | derived | 2.762 N | `m_mounted * g_accel / n_mount` | static load at one mount point |
| `F_mount_shock` | N | derived | 138.1 N | `m_mounted * g_accel * g_shock / n_mount` | load at one mount point under transit shock |
| `disp_frame` | mm | derived | 0 mm | `(cte_frame - cte_ss) * dT_frame * L_cube` | differential motion between frame and cube over the worst temperature difference |
| `travel_compliant` | mm | derived | 0.3322 mm | `4 * (cte_frame + cte_ss) * dT_frame * L_cube` | travel the compliant mount must allow, taken generously because the frame's own material is not settled |
| `V_spill_event` | mm^3 | derived | 800 mm^3 | `2 * n_corner_in * V_coupling` | fluid lost in one service event, all eight couplings parted |
| `V_spill_life` | mm^3 | derived | 4000 mm^3 | `V_spill_event * n_service` | fluid lost over the installation's life |
| `n_orient` | 1 | derived | 6 | `n_face` | mounting orientations to check, one per face that could be the mounted one |
| `C_cube` | J/K | derived | 495.3 J/K | `(m_tiers + m_coldplate + m_dies) * cp_si + m_laminae * cp_cumo + (m_rails + m_corners + m_ports) * cp_ss + m_coolant * cp_water + m_cage * cp_si` | heat the whole machine holds per kelvin, summed over what it is made of. The cage is taken as silicon because it is a switch shell of dies, and the interposer is left out as glass with a tenth the mass of anything else here |
| `R_cube_cool` | K/W | derived | 0.01104 K/W | `(T_j_peak - T_coolant_in) / P_heat` | the cube's whole thermal resistance to the coolant, read backwards out of the steady state: the temperature it sits at above the inlet, divided by the heat it was rejecting to get there |
| `tau_cube` | s | derived | 5.47 s | `C_cube * R_cube_cool` | how long the machine takes to fall to a third of its excess temperature with the power off and the pump still running. Not the engine's time constant in 026, which is the array alone and is a thousand times shorter -- this is the whole object |
| `t_cool_hold` | s | derived | 16.41 s | `n_tau_cool * tau_cube` | how long to hold with the pump running before opening anything. The pump outlives the power to the dies for exactly this reason |
| `n_couple` | 1 | derived | 16 | `2 * n_corner` | coolant couplings to part and then make: one supply and one return at every corner |
| `t_swap` | s | derived | 3196 s | `t_stop + t_cool_hold + t_valve + 2 * n_couple * t_couple + 2 * n_mount * t_bolt + t_lift + t_align + t_purge` | the mechanical exchange, end to end: stop, cool, isolate, part sixteen couplings, undo four bolts, lift out, lift in, seat, do up four bolts, make sixteen couplings, fill and purge |
| `t_service` | s | derived | 1.1e+04 s | `t_swap + t_bringup` | the whole service event as an operator experiences it, from the machine still running to the replacement passing rung ten. 085 owns the second half and this blueprint owns the first |
| `t_service_h` | hr | derived | 3.055 hr | `t_service` | the same, in the unit somebody plans a shift around |
| `t_shift` | hr | given | 8 hr | — | a working shift, which a service event has to fit inside or it becomes a two-day job and the installation is down overnight |
| `f_swap_of_svc` | 1 | derived | 0.2907 | `t_swap / t_service` | the share of a service event that is hands on the machine rather than the machine testing itself |

## 020 — The heat budget

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `E_op` | pJ/flop | measured | 0.25 pJ/flop | — | switching energy per operation in a multiplier cell at this node and voltage |
| `P_scalar_die` | W | given | 3 W | — | scalar core, sequencer, clock tree and control on one die, which barely varies with what it is doing |
| `E_slice_bit` | pJ/bit | measured | 0.15 pJ/bit | — | energy to read one bit out of a face slice, array and local routing together |
| `leak_ref` | W/mm^2 | measured | 0.003 W/mm^2 | — | logic leakage per unit die area at 300 K and the nominal supply |
| `dT_leak_dbl` | K | measured | 10 K | — | temperature rise that doubles leakage |
| `T_ref_leak` | K | given | 300 K | — | temperature the leakage figure above is quoted at |
| `T_j_assumed` | K | given | 318.9 K | — | junction temperature the leakage term is evaluated at; the answer of a fixed-point iteration that C-020-1 checks against what 025 derives |
| `E_core_bit` | pJ/bit | measured | 0.48 pJ/bit | — | energy to read one bit out of a core tier, array and tier routing together |
| `P_core_leak` | W | given | 12.74 W | — | retention leakage of all twenty-four tiers at the operating temperature, from the per-cell figure 035 carries |
| `E_link_bit` | pJ/bit | measured | 0.1 pJ/bit | — | energy to move one bit across a radial link, driver and receiver together |
| `P_crossbar` | W | given | 13.82 W | — | the cage's switch fabric at full traffic. Thirty-nine was an estimate before 037 had a per-bit figure; the constraint that compares the two is what corrected it |
| `P_ports` | W | given | 10 W | — | six port fields during generation, when the storage lines are idle and the spout is occasional. This is a steady figure and the two things that break it are both bursts |
| `P_port_burst` | W | given | 80 W | — | what the port fields may draw transiently: a model load runs the storage lines flat out for tens of milliseconds, and the spout's pane costs more again for microseconds. Neither is in the steady budget and 026 is where both are absorbed |
| `eta_conv_1` | 1 | measured | 0.96 | — | efficiency of the first stage, forty-eight volts to five |
| `eta_conv_2` | 1 | measured | 0.9 | — | efficiency of the second, five volts to the point of load |
| `eta_dist` | 1 | measured | 0.985 | — | resistive efficiency of the planes and grids between them |
| `P_slice_die` | W | derived | 2.826 W | `E_slice_bit * B_operand_die` | what a die's own slice reads cost, at the rate 045's array consumes them |
| `leak_factor` | 1 | derived | 3.706 | `2^((T_j_assumed - T_ref_leak) / dT_leak_dbl)` | how much worse leakage is at the operating temperature than at the quoted one |
| `P_leak_die` | W | derived | 6.405 W | `leak_ref * A_die * leak_factor` | leakage of one die at that temperature |
| `P_die` | W | derived | 58.11 W | `P_engine_die + P_scalar_die + P_slice_die + P_leak_die` | everything one compute die dissipates at the design point |
| `P_dies` | W | derived | 1395 W | `n_die * P_die` | all twenty-four compute dies |
| `P_core_read` | W | derived | 147.5 W | `E_core_bit * B_core` | the core's array read energy at its full bandwidth |
| `P_core` | W | derived | 160.2 W | `P_core_read + P_core_leak` | the whole memory block |
| `P_link` | W | derived | 30.72 W | `E_link_bit * B_core` | six radial links carrying everything the core delivers |
| `P_cage` | W | derived | 44.54 W | `P_link + P_crossbar` | the switch shell |
| `P_load` | W | derived | 1609 W | `P_dies + P_core + P_cage + P_ports` | power delivered to the point of load |
| `eta_conv` | 1 | derived | 0.851 | `eta_conv_1 * eta_conv_2 * eta_dist` | end-to-end conversion and distribution efficiency |
| `P_input` | W | derived | 1891 W | `P_load / eta_conv` | power drawn from the forty-eight volt supply |
| `P_conv_loss` | W | derived | 281.7 W | `P_input - P_load` | what turning voltage into other voltage costs, deposited on the face interposers |
| `P_heat` | W | derived | 1891 W | `P_input` | total heat to remove. It is the input power, because everything drawn from a supply leaves as heat, and naming it separately is what lets the plumbing be checked against the electrics |
| `P_density` | W/m^3 | derived | 8.754e+06 W/m^3 | `P_heat / V_cube` | heat per unit volume of the finished object |
| `P_idle` | W | derived | 248.4 W | `n_die * (P_scalar_die + P_leak_die) + P_core_leak + P_ports` | what the machine costs to have switched on with a model resident and nothing being asked of it |

## 021 — The working fluid

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `fluid_is_water` | 1 | given | 1 | — | the selection switch: one for water, zero for the dielectric. Nothing between the two means anything |
| `rho_fluid` | kg/m^3 | derived | 989 kg/m^3 | `fluid_is_water * rho_water + (1 - fluid_is_water) * rho_fluoro` | density of whichever fluid is selected |
| `cp_fluid` | J/(kg*K) | derived | 4180 J/(kg*K) | `fluid_is_water * cp_water  + (1 - fluid_is_water) * cp_fluoro` | specific heat capacity of the same |
| `k_fluid` | W/(m*K) | derived | 0.63 W/(m*K) | `fluid_is_water * k_water   + (1 - fluid_is_water) * k_fluoro` | thermal conductivity of the same |
| `mu_fluid` | Pa*s | derived | 0.000577 Pa*s | `fluid_is_water * mu_water  + (1 - fluid_is_water) * mu_fluoro` | dynamic viscosity of the same |
| `T_frz_fluid` | K | derived | 273 K | `fluid_is_water * T_water_frz + (1 - fluid_is_water) * T_fluoro_frz` | freezing point of the same |
| `T_fluid_min` | K | derived | 288 K | `ships_dry * T_serv_min + (1 - ships_dry) * T_ship_min` | the coldest the fluid itself ever gets: the service minimum if the cube travels empty, and the transit minimum if it does not |
| `Pr_fluid` | 1 | derived | 3.828 | `mu_fluid * cp_fluid / k_fluid` | Prandtl number of the selected fluid |
| `dT_rise` | K | given | 7.8 K | — | design temperature rise of the coolant across the cube; a choice, and the one that sets the flow |
| `T_coolant_in` | K | given | 298 K | — | temperature the coolant enters at. This is the parameter that decides whether the loop in 027 needs a refrigeration plant or a radiator and a fan |
| `v_erosion_max` | m/s | measured | 4 m/s | — | velocity above which flow erosion of the wetted materials shortens the life in 086 |
| `T_ship_min` | K | given | 253 K | — | lowest temperature a cube may see in transit |
| `T_serv_min` | K | given | 288 K | — | lowest temperature a cube sees once installed and filled |
| `ships_dry` | 1 | given | 1 | — | whether a cube travels with its coolant removed. One means it does, and 082 has to say so in the procedure |
| `mdot_water` | kg/s | derived | 0.058 kg/s | `P_heat / (cp_water * dT_rise)` | mass flow water would need for this heat at this rise |
| `mdot_fluoro` | kg/s | derived | 0.2204 kg/s | `P_heat / (cp_fluoro * dT_rise)` | and what the dielectric would need |
| `Q_water` | m^3/s | derived | 5.864e-05 m^3/s | `mdot_water / rho_water` | volumetric flow of water |
| `Q_fluoro` | m^3/s | derived | 0.0001377 m^3/s | `mdot_fluoro / rho_fluoro` | and of the dielectric |
| `flow_penalty` | 1 | derived | 2.349 | `Q_fluoro / Q_water` | how much more of the dielectric has to be moved, which is the headline cost of the substitution |
| `k_penalty` | 1 | derived | 9.692 | `k_water / k_fluoro` | how much worse its conduction is, which sets the convection coefficient in 022 |
| `mdot_design` | kg/s | derived | 0.058 kg/s | `P_heat / (cp_fluid * dT_rise)` | mass flow the selected fluid actually needs |

## 022 — The field that does the work

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `alpha_uchan` | 1 | derived | 0.15 | `w_uchan / h_uchan` | aspect ratio of a channel, short side over long, which is what the Nusselt polynomial is in |
| `Nu_4side` | 1 | derived | 6.22 | `8.235 * (1 - 2.0421*alpha_uchan + 3.0853*alpha_uchan^2 - 2.4765*alpha_uchan^3 + 1.0578*alpha_uchan^4 - 0.1861*alpha_uchan^5)` | Nusselt number for fully developed laminar flow in a rectangular duct at constant heat flux with all four walls heated |
| `f_3side` | 1 | given | 0.85 | — | correction for heat arriving on three sides rather than four, the cover being unheated |
| `Nu_uchan` | 1 | derived | 5.287 | `Nu_4side * f_3side` | the Nusselt number this field actually runs at |
| `D_uchan` | mm | derived | 0.2609 mm | `2 * w_uchan * h_uchan / (w_uchan + h_uchan)` | hydraulic diameter of one channel |
| `h_conv` | W/(m^2*K) | derived | 1.277e+04 W/(m^2*K) | `Nu_uchan * k_fluid / D_uchan` | convection coefficient at the channel wall |
| `m_fin` | 1/m | derived | 1244 1/m | `sqrt(2 * h_conv / (k_si * w_ufin))` | fin parameter for a silicon wall of this thickness at this coefficient |
| `mL_fin` | 1 | derived | 1.244 | `m_fin * h_uchan` | the dimensionless group the efficiency is a function of |
| `eta_fin` | 1 | derived | 0.6805 | `(exp(2*mL_fin) - 1) / (mL_fin * (exp(2*mL_fin) + 1))` | efficiency of one fin, written as the hyperbolic tangent expanded, since the notation has no tanh |
| `f_fin_area` | 1 | derived | 0.9302 | `2 * h_uchan / (2 * h_uchan + w_uchan)` | share of the heated perimeter that is fin rather than base |
| `eta_surface` | 1 | derived | 0.7028 | `1 - f_fin_area * (1 - eta_fin)` | overall surface efficiency, base and fins together |
| `ar_uchan_max` | 1 | given | 8 | — | aspect ratio past which fin efficiency falls below what is worth the extra silicon; this is what set h_uchan in 012 |
| `per_uchan` | mm | derived | 2.15 mm | `2 * h_uchan + w_uchan` | heated perimeter of one channel |
| `A_wet_chan` | mm^2 | derived | 111.8 mm^2 | `per_uchan * L_plate` | heated area of one channel over its length |
| `A_wet_face` | mm^2 | derived | 1.934e+04 mm^2 | `n_uchan * A_wet_chan` | heated area of one face's field before deration |
| `f_island_wet` | 1 | derived | 0.05336 | `n_island * L_island * (L_island / p_uchan) * per_uchan / A_wet_face` | share of the heated area the sixteen via islands interrupt |
| `A_wet_total` | mm^2 | derived | 1.099e+05 mm^2 | `n_face * A_wet_face * (1 - f_island_wet)` | heated area of all six fields, derated |
| `UA_face` | W/K | derived | 164.3 W/K | `h_conv * A_wet_total * eta_surface / n_face` | conductance of one face's field |
| `UA_total` | W/K | derived | 985.8 W/K | `h_conv * A_wet_total * eta_surface` | conductance of all six together |
| `dT_conv` | K | derived | 1.918 K | `P_heat / UA_total` | the temperature the coolant sits below the channel walls |
| `A_wet_rails` | mm^2 | derived | 3931 mm^2 | `n_edge * 2 * (w_rail_chan + h_rail_chan) * L_rail` | heated area the rail channels present, if they were the cooling |
| `h_conv_rail` | W/(m^2*K) | derived | 1198 W/(m^2*K) | `3.61 * k_fluid / D_rail` | convection coefficient in a rail, at the laminar Nusselt number for a duct this shape |
| `UA_rails` | W/K | derived | 4.708 W/K | `h_conv_rail * A_wet_rails` | what the corners and edges alone would remove per kelvin |
| `gain_field` | 1 | derived | 209.4 | `UA_total / UA_rails` | how much better the fields are than the plumbing that feeds them |

## 023 — The tetrahedra in the corners

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_net` | 1 | given | 2 | — | independent networks, supply and return, sharing the twelve edges |
| `n_edge_fed` | 1 | derived | 12 | `n_edge` | supply channels carrying flow toward a load; all twelve, which is the property the parity choice delivers |
| `n_edge_dead` | 1 | solved | 0 | — | supply channels joining two fed corners and therefore carrying nothing, counted by reading the twelve edges rather than asserted -- from 102. Zero under the parity choice and four under the choose-one-face alternative |
| `hops_to_feed` | 1 | given | 1 | — | greatest number of edges from any point of the supply network to a feed point |
| `n_face_per_rail` | 1 | given | 1 | — | face fields a single supply rail feeds, once opposite faces are run perpendicular so no rail pair carries two full loads |
| `n_edge_crossing` | 1 | solved | 12 | — | edges whose two ends have opposite parity, found by taking each edge in turn and comparing the parity of its ends -- from 102. This is the bipartition as a count of things checked rather than a count of things assumed |
| `n_edge_listed` | 1 | solved | 12 | — | edges written out in 010's prose that match an edge of the cube built from its definition -- from 102. The document and the object have never before been compared |
| `n_tetra_equal` | 1 | solved | 6 | — | pairwise distances between the four fed corners that come to the face diagonal, out of six -- from 102 |
| `n_assign_tried` | 1 | solved | 512 | — | arrangements of the six faces onto the twelve rails that obey the perpendicularity rule, before the no-sharing rules are applied -- from 102 |
| `n_assign_legal` | 1 | solved | 64 | — | of those, the ones where no supply channel feeds two faces and no return channel drains two -- from 102 |
| `n_assign_even` | 1 | solved | 16 | — | of those, the ones that distribute the coolant exactly evenly between the six faces -- from 102, and the surprise of the phase |
| `n_perp_pair` | 1 | solved | 3 | — | opposite-face pairs whose channels run perpendicular under the chosen assignment -- from 102. Should be every pair, which is what makes it worth counting |
| `d_tetra` | mm | derived | 84.85 mm | `L_cube * sqrt(2)` | distance between any two fed corners: the cube's face diagonal, six times over |
| `n_tetra_edge` | 1 | given | 6 | — | pairwise distances between the four fed corners, all equal |
| `Q_per_inlet` | m^3/s | derived | 1.466e-05 m^3/s | `Q_total / n_corner_in` | flow entering one fed corner |
| `Q_per_supply` | m^3/s | derived | 4.887e-06 m^3/s | `Q_per_inlet / n_edge_per_corner` | flow leaving that corner down one of its three supply channels |

## 024 — Where the flow actually goes

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `f_wet_max` | 1 | given | 0.15 | — | the most of the cube's volume that may be standing fluid |
| `eta_pump` | 1 | measured | 0.3 | — | wire-to-water efficiency of a pump of this size and duty; small pumps are poor and this is a realistic figure rather than a hopeful one |
| `K_plenum` | 1 | given | 2.2 | — | loss coefficient for entering and leaving a plenum and turning into the channel field, summed over both ends |
| `Q_total` | m^3/s | derived | 5.864e-05 m^3/s | `mdot_design / rho_fluid` | volumetric flow through the whole machine |
| `Q_face` | m^3/s | derived | 9.774e-06 m^3/s | `Q_total / n_face` | through one face's field |
| `Q_uchan` | m^3/s | derived | 5.65e-08 m^3/s | `Q_face / n_uchan` | through one microchannel |
| `A_uchan` | mm^2 | derived | 0.15 mm^2 | `w_uchan * h_uchan` | cross-section of one microchannel |
| `v_uchan` | m/s | derived | 0.3766 m/s | `Q_uchan / A_uchan` | velocity in one microchannel |
| `Re_uchan` | 1 | derived | 168.4 | `rho_fluid * v_uchan * D_uchan / mu_fluid` | Reynolds number there; laminar by a wide margin, which every correlation in 022 depends on |
| `fRe_uchan` | 1 | derived | 20.05 | `24 * (1 - 1.3553*alpha_uchan + 1.9467*alpha_uchan^2 - 1.7012*alpha_uchan^3 + 0.9564*alpha_uchan^4 - 0.2537*alpha_uchan^5)` | the Fanning friction factor times Reynolds number for laminar flow in a rectangular duct, which is a constant for a given shape and is why laminar pressure drop is linear in velocity |
| `f_uchan` | 1 | derived | 0.4761 | `4 * fRe_uchan / Re_uchan` | Darcy friction factor in a microchannel |
| `dp_field` | Pa | derived | 6657 Pa | `f_uchan * (L_plate / D_uchan) * rho_fluid * v_uchan^2 / 2` | pressure lost crossing one face's field |
| `dp_plenum` | Pa | derived | 154.3 Pa | `K_plenum * rho_fluid * v_uchan^2 / 2` | entering and leaving the plenum at both ends |
| `dp_loop` | Pa | derived | 1.887e+04 Pa | `dp_field + dp_plenum + 2 * dp_rail + 2 * dp_corner` | the whole circuit inside the cube, one path from a fed corner to a drained one |
| `f_field_loss` | 1 | derived | 0.3528 | `dp_field / dp_loop` | the load's share of the loss, which is what says whether this is a manifold feeding a load or two comparable restrictions in series |
| `V_field_wet` | mm^3 | derived | 8096 mm^3 | `n_face * n_uchan * A_uchan * L_plate` | fluid standing in the six microchannel fields |
| `V_plenum_wet` | mm^3 | derived | 2820 mm^3 | `n_face * 2 * h_plenum * L_plate * w_rail` | and in the twelve plenums that feed and drain them |
| `V_core_wet` | mm^3 | derived | 5760 mm^3 | `n_tier * A_core_side * t_lamina * f_void_lam` | and in the twenty-four cooling laminae inside the core, at the void fraction 036 derives from their channel geometry |
| `V_coolant` | mm^3 | derived | 2.31e+04 mm^3 | `V_field_wet + V_plenum_wet + V_rail_wet + V_corner_wet + V_core_wet` | all of it, which is what 013 weighs and 027 has to make up when it leaks |
| `P_hydraulic` | W | derived | 1.107 W | `Q_total * dp_loop` | work a second the fluid needs |
| `P_pump` | W | derived | 3.689 W | `P_hydraulic / eta_pump` | electrical power the pump draws, which is outside the cube and outside 020's budget |
| `f_pump_of_heat` | 1 | derived | 0.001951 | `P_pump / P_heat` | what moving the coolant costs against what it carries |
| `f_served_min` | 1 | given | 0.85 | — | least share of the mean flow any face may receive and still have 025's worst case be the one being computed. A limit rather than a result, named so it is not a bare number sitting in a constraint |
| `n_node_net` | 1 | solved | 29 | — | nodes in the hydraulic network the solve runs on -- from 102: eight supply corners, eight return corners, six supply plenums, six return plenums and the inlet header, with the outlet header as the reference every pressure is measured against |
| `n_branch_net` | 1 | solved | 50 | — | branches joining them -- from 102: four inlet fittings, four outlets, six faces, and thirty-six rail segments, because a rail carrying a plenum is two rails with a tap between them |
| `f_worst_served` | 1 | solved | 1 | — | share of the mean flow the worst-served face receives under the assignment 023 chose -- from 102. Exactly one, and exactly rather than nearly: the chosen arrangement has a threefold symmetry that makes all six faces the same face, so no arithmetic can separate them |
| `f_best_served` | 1 | solved | 1 | — | and the best-served face, which is the same number for the same reason -- from 102. Declared separately because the two being equal is the result, and a result written once looks like an assumption |
| `f_worst_any` | 1 | solved | 0.9449 | — | share of the mean the worst-served face receives under the least even of the sixty-four legal assignments -- from 102. This is the number the thermal chain is built on, not the one above, because a builder who wires the plumbing legally but not optimally must still get a machine that works |
| `dp_network` | Pa | solved | 1.139e+04 Pa | — | pressure from the pump's outlet to its inlet at the design flow, from the network solve rather than from summing one path -- from 102 |
| `dT_conv_worst` | K | derived | 2.03 K | `dT_conv / f_worst_any` | the convection rise at the worst-served face, which is what 025 must use rather than the mean |
| `f_path_over_net` | 1 | derived | 1.656 | `dp_loop / dp_network` | how much the single-path sum overstates the real circuit. Above one, and the amount is the manifold delivering to each plenum from both of its ends at once |

## 025 — Junction to room, every term

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `dT_margin_min` | K | given | 20 K | — | the least margin the hottest transistor must keep against its limit, given that the hot spot term rests on a floorplan that could still move |
| `dT_conv_max` | K | given | 4 K | — | the most 022's convection term may take of the budget between the coolant and the silicon |
| `T_room` | K | given | 295 K | — | air the radiator rejects into |
| `R_die` | K/W | derived | 6.576e-05 K/W | `t_die / (k_si * A_die_total)` | one-dimensional conduction through all twenty-four dies in parallel |
| `R_plate_base` | K/W | derived | 0.0005603 K/W | `(t_coldplate - h_uchan) / (k_si * n_face * A_plate)` | through the base of the six cold plates |
| `dT_die` | K | derived | 0.09171 K | `P_dies * R_die` | the drop across the dies |
| `dT_plate` | K | derived | 1.06 K | `P_heat * R_plate_base` | the drop across the plate bases |
| `A_wet_engine` | mm^2 | derived | 428.5 mm^2 | `f_engine_area * A_die * A_wet_face / A_plate` | heated channel area lying above one die's multiplier array |
| `dT_conv_local` | K | derived | 11.93 K | `f_engine_power * P_die / (h_conv * eta_surface * A_wet_engine)` | convection rise directly over the array, where a tenth of the area carries seventy per cent of the heat |
| `dT_hotspot` | K | derived | 9.901 K | `dT_conv_local - dT_conv_worst` | how much hotter the array is than the face average; this is what 005 calls the hot spot term |
| `dT_fluid_mean` | K | derived | 3.9 K | `dT_rise / 2` | the coolant's own rise at the mean point along its path |
| `T_j_peak` | K | derived | 318.9 K | `T_coolant_in + dT_rise + dT_conv_worst + dT_hotspot + dT_plate + dT_die` | junction temperature at the hottest point of the hottest die on the worst-served face, with the coolant at its outlet temperature |
| `T_j_mean` | K | derived | 305 K | `T_coolant_in + dT_fluid_mean + dT_conv + dT_plate + dT_die` | and the same for an average die in the middle of the coolant path |
| `margin_thermal` | K | derived | 59.12 K | `T_si_max - T_j_peak` | how far the hottest transistor is from what the silicon is qualified to |
| `fix_point_err` | 1 | derived | 5.42e-05 | `abs(T_j_assumed - T_j_peak) / T_j_assumed` | how far 020's assumed leakage temperature is from what this chain produces |
| `s_hotspot` | 1 | derived | 0.4741 | `dT_hotspot / (T_j_peak - T_coolant_in)` | the hot spot's share of everything above the inlet |
| `s_conv` | 1 | derived | 0.09721 | `dT_conv_worst / (T_j_peak - T_coolant_in)` | the channel wall's share |
| `s_fluid` | 1 | derived | 0.3735 | `dT_rise / (T_j_peak - T_coolant_in)` | the coolant's own share |
| `s_solid` | 1 | derived | 0.05513 | `(dT_plate + dT_die) / (T_j_peak - T_coolant_in)` | everything conducted through solid |

## 026 — What happens in the first microsecond

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `dT_walk_max` | K | given | 1 K | — | the most the walking hot spot may swing over one pipeline stage before the face ordering in 010 would have to be taken seriously |
| `dT_spout_max` | K | given | 0.1 K | — | the most a whole-core output burst may warm the face it leaves through before it stops being an energy the silicon absorbs and starts being a load the coolant sees |
| `dT_throttle` | K | given | 15 K | — | how far below the silicon's limit the throttle acts |
| `dT_halt` | K | given | 5 K | — | how far below it the machine stops issuing |
| `t_sensor` | s | measured | 0.0002 s | — | response time of an on-die temperature sensor, its own settling included |
| `t_throttle` | s | given | 1e-05 s | — | time from the throttle threshold being crossed to engine activity falling |
| `C_engine` | J/K | derived | 0.009836 J/K | `f_engine_area * A_die * t_die * rho_si * cp_si` | thermal mass of the silicon directly under one die's multiplier array |
| `C_face` | J/K | derived | 7.038 J/K | `(A_plate * t_coldplate * f_solid_plate * rho_si + 4 * A_die * t_die * rho_si) * cp_si` | thermal mass of one face assembly, cold plate and dies |
| `C_core` | J/K | derived | 158.4 J/K | `n_tier * A_core_side * (t_lamina * rho_cumo * cp_cumo + t_tier_si * rho_si * cp_si)` | thermal mass of the core stack |
| `R_engine` | K/W | derived | 0.2601 K/W | `dT_conv_local / (f_engine_power * P_die)` | thermal resistance from the array to the fluid, from 025's local term |
| `tau_engine` | s | derived | 0.002558 s | `R_engine * C_engine` | how long the array takes to reach its steady temperature. The number that decides whether any of the fast transients matter |
| `tau_loop` | s | derived | 3.804 s | `V_loop / Q_total` | transport delay round the external circuit |
| `dT_walk` | K | derived | 0.6224 K | `dT_hotspot * t_stage / tau_engine` | temperature excursion of the walking hot spot over one stage, which is the whole of 010's argument reduced to a number |
| `E_spout_burst` | J | derived | 0.005756 J | `E_pane * n_pane_core` | energy to push the entire core through the output tube |
| `dT_spout` | K | derived | 0.0008178 K | `E_spout_burst / C_face` | what that burst does to the temperature of the face it leaves through |
| `t_to_halt` | s | derived | 1.209 s | `C_face * (T_si_max - dT_halt - T_j_peak) * n_face / P_heat` | how long the machine has, from the design operating point, if all cooling stops at once |
| `T_throttle` | K | derived | 363 K | `T_si_max - dT_throttle` | threshold at which the clock comes down |
| `T_halt` | K | derived | 373 K | `T_si_max - dT_halt` | threshold at which the faces stop |
| `t_lag_budget` | s | derived | 0.2233 s | `(T_halt - T_throttle) * C_face * n_face / P_heat` | how long the machine takes to cross from one threshold to the other at full power with no cooling, which is what the sensors must be faster than |

## 027 — Everything outside the cube

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `T_in_max` | K | given | 333 K | — | the warmest coolant the seals, the elastomer and the materials in 011 tolerate at the working pressure |
| `dT_margin_dry` | K | given | 15 K | — | the least junction margin that must remain once the chiller is removed and the inlet floats to whatever an air-cooled radiator gives |
| `UA_rad` | W/K | given | 60 W/K | — | conductance of an air-cooled radiator of a size that fits beside this machine, with its fan running |
| `d_filter` | um | given | 25 um | — | absolute rating of the filter; the largest particle it passes |
| `dp_filter_new` | Pa | given | 3000 Pa | — | pressure across a clean filter element |
| `dp_filter_end` | Pa | given | 1.5e+04 Pa | — | pressure across one loaded enough to be changed |
| `V_loop_ext` | mm^3 | given | 2e+05 mm^3 | — | fluid in the pump, radiator, reservoir and tubing, outside the cube |
| `V_reservoir` | mm^3 | given | 5e+04 mm^3 | — | reservoir capacity |
| `t_interlock` | s | given | 0.1 s | — | from flow loss detected to power removed |
| `t_service_int` | s | given | 3.15e+07 s | — | interval between services, one year |
| `n_pump` | 1 | given | 2 | — | pumps, one running and one held |
| `beta_water` | 1/K | measured | 0.0004 1/K | — | volumetric thermal expansion of water near the operating temperature |
| `V_loop` | mm^3 | derived | 2.231e+05 mm^3 | `V_coolant + V_loop_ext` | fluid in the whole circuit |
| `dT_rad` | K | derived | 31.52 K | `P_heat / UA_rad` | temperature the coolant must sit above the room for the radiator to reject the heat |
| `T_in_no_chill` | K | derived | 326.5 K | `T_room + dT_rad` | the coldest inlet an air-cooled radiator alone can produce |
| `dT_chiller` | K | derived | 28.52 K | `T_in_no_chill - T_coolant_in` | how much colder the requested inlet is than an air-cooled radiator alone can produce. Positive means a refrigeration plant, and how far positive is how much of one. Written as a temperature rather than as a boolean because this notation has no conditionals and a quantity says more than a flag would |
| `T_j_no_chill` | K | derived | 347.4 K | `T_j_peak - T_coolant_in + T_in_no_chill` | junction temperature if the chiller is removed and the inlet allowed to float to what the radiator gives |
| `margin_no_chill` | K | derived | 30.6 K | `T_si_max - T_j_no_chill` | and how much margin is left after doing that |
| `V_expansion` | mm^3 | derived | 2784 mm^3 | `V_loop * beta_water * dT_rise * 4` | fluid volume change over the operating range, taken over four times the design rise to cover a cold start |
| `Q_leak_max` | m^3/s | derived | 1.372e-12 m^3/s | `V_makeup / t_service_int` | volumetric loss the reservoir can absorb over one service interval once thermal expansion and service spillage are allowed for |
| `V_makeup` | mm^3 | derived | 4.322e+04 mm^3 | `V_reservoir - V_expansion - V_spill_life` | reservoir capacity left over for leakage, once expansion and the spillage 019 loses at every coupling are taken out |
| `dp_ext` | Pa | derived | 2.7e+04 Pa | `dp_filter_end + dp_rad + dp_tube` | pressure the external circuit costs when the filter is at the end of its life |
| `dp_rad` | Pa | given | 8000 Pa | — | pressure across the radiator core at design flow |
| `dp_tube` | Pa | given | 4000 Pa | — | pressure across the tubing and fittings |
| `dp_system` | Pa | derived | 4.587e+04 Pa | `dp_loop + dp_ext` | the whole circuit, inside and out, at end-of-life filter loading |
| `P_pump_total` | W | derived | 8.967 W | `dp_system * Q_total / eta_pump` | electrical power the pump draws against the whole system |
| `t_to_halt_1pump` | K | derived | 7.8 K | `dT_rise` | the extra coolant rise on one pump: flow halves, so the fluid's own rise doubles, and nothing else in the chain changes |
| `V_leak_life` | mm^3 | derived | 2615 mm^3 | `Q_leak_total * t_service_int` | fluid actually lost to leakage over one service interval, at the rate 017's hundred and sixty-six joints add up to |

## 028 — The power budget

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `P_logic_load` | W | derived | 1341 W | `n_die * (P_engine_die + P_scalar_die + P_leak_die) + P_crossbar` | everything on the logic rail: engines, control, leakage and the switch fabric |
| `P_array_load` | W | derived | 228 W | `n_die * P_slice_die + P_core` | everything on the array rail: the face slices and the whole memory block |
| `P_link_load` | W | derived | 30.72 W | `P_link` | the six radial link drivers |
| `P_port_load` | W | derived | 10 W | `P_ports` | port fields, storage lines and the spout, averaged |
| `P_aux_load` | W | given | 1 W | — | sensors, telemetry and the interlock, which must run when nothing else does |
| `I_alarm` | A | given | 1000 A | — | a current large enough that reaching it would mean the delivery scheme had been abandoned; it exists to give C-028-5 something named to compare against |
| `I_logic` | A | derived | 1787 A | `P_logic_load / V_logic` | current on the logic rail across the whole machine |
| `I_array` | A | derived | 268.3 A | `P_array_load / V_array` | on the array rail |
| `I_link` | A | derived | 51.2 A | `P_link_load / V_link` | on the link rail |
| `I_port` | A | derived | 8.333 A | `P_port_load / V_port` | on the port rail |
| `I_aux` | A | derived | 0.303 A | `P_aux_load / V_aux` | on the auxiliary rail |
| `I_die_logic` | A | derived | 73.71 A | `(P_engine_die + P_scalar_die + P_leak_die) / V_logic` | logic current into one compute die, which is the number the power grid in 030 is sized by |
| `I_supply` | A | derived | 39.39 A | `P_input / V_supply` | current drawn from the external supply |
| `I_face_supply` | A | derived | 6.566 A | `I_supply / n_face` | and per face, which is what a port field connector has to carry |
| `I_core_inward` | A | derived | 188.5 A | `P_core / V_array` | current the memory block draws, arriving radially |
| `I_core_face` | A | derived | 31.41 A | `I_core_inward / n_face` | one face's share of it, sent inward through the same interface the data uses |
| `I_would_be` | A | derived | 2521 A | `P_input / V_logic` | what the supply current would be if power arrived at the voltage the transistors run at. Two and a half kiloamps: not a connector, a pair of busbars nobody could bolt to this object, and the reason the whole two-stage conversion exists |
| `conv_ratio` | 1 | derived | 64 | `V_supply / V_logic` | how far the voltage has to fall between the outside world and a gate |

## 029 — Five voltages, or four

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `V_supply` | V | given | 48 V | — | what arrives from outside. Chosen because it is the highest voltage that is still ordinary to distribute and connect without special handling |
| `V_mid` | V | given | 5 V | — | the intermediate rail on a face interposer, between the two conversion stages |
| `V_logic` | V | given | 0.75 V | — | logic and multiplier arrays |
| `V_array` | V | given | 0.85 V | — | static memory cells, in the slices and in the core |
| `V_link` | V | given | 0.6 V | — | radial link drivers, low swing by design |
| `V_port` | V | given | 1.2 V | — | port field transceivers |
| `V_aux` | V | given | 3.3 V | — | sensors, telemetry and the interlock |
| `tol_rail` | 1 | given | 0.05 | — | fractional tolerance band on every rail, worst case over the operating range |
| `droop_frac` | 1 | given | 0.03 | — | fraction of nominal a rail may droop transiently under a load step |
| `ripple_frac` | 1 | given | 0.01 | — | fraction of nominal a rail may carry as switching ripple |
| `dV_read_marg` | V | given | 0.08 V | — | the least the array rail must exceed the logic rail by for a static memory cell to stay stable while it is read, at the worst process corner and the highest temperature |
| `n_domain` | 1 | given | 5 | — | supply domains |
| `dV_droop_logic` | V | derived | 0.0225 V | `V_logic * droop_frac` | how far the logic rail may fall during a load step, which is what sizes the decoupling in 031 |
| `dV_ripple` | V | derived | 0.0075 V | `V_logic * ripple_frac` | switching ripple allowed on the same |
| `dV_band` | V | derived | 0.0375 V | `V_logic * tol_rail` | the whole tolerance band the rail must stay inside |
| `P_if_merged` | W | derived | 178.7 W | `P_logic_load * (V_array / V_logic - 1)` | what running the logic at the array's voltage would cost, which is the argument for keeping them apart |
| `f_merge_cost` | 1 | derived | 0.1111 | `P_if_merged / P_load` | that cost as a share of the machine |
| `E_swing_ratio` | 1 | derived | 0.64 | `(V_link / V_logic)^2` | the energy the link saves by swinging six hundred millivolts instead of the logic rail's, since energy goes as the square of the swing |
| `P_link_if_logic` | W | derived | 48 W | `P_link_load / E_swing_ratio` | what the link would cost at the logic swing |

## 030 — Where the copper goes

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `t_plane_mid` | mm | given | 0.07 mm | — | thickness of one interposer plane carrying the intermediate rail |
| `ir_frac` | 1 | given | 0.02 | — | fraction of nominal the static drop may take. Separate from the transient droop allowance in 029, because static drop is always present and spending the whole allowance on it would leave the rail out of specification the moment anything switched |
| `n_plane_mid` | 1 | given | 4 | — | such planes, two out and two back |
| `t_grid_metal` | um | given | 16 um | — | thickness of the thick metal stack a die's power grid is built in. Three microns was tried and put a hundred millivolts across the die at seventy amperes, which is four times the whole droop allowance |
| `f_grid_metal` | 1 | given | 0.75 | — | share of that stack given to power rather than signal |
| `L_reg_to_die` | mm | given | 1.2 mm | — | distance from a regulator's output to the microbumps it feeds. Regulators sit directly beneath the dies they serve, because this length multiplies the drop |
| `n_pillar_pwr` | 1 | derived | 2.116e+06 | `n_radial_pad * f_radial_power` | radial pillars carrying current inward rather than data |
| `i_pad_max` | mA | given | 5 mA | — | current one twenty-micron copper pillar carries, a fifth of what it would take, because 032 wants the margin |
| `R_island` | ohm | derived | 1.344e-06 ohm | `res_cu * t_coldplate / (n_island_pad * pi * (p_island_pad/4)^2)` | resistance of all the via island feedthroughs on one face in parallel |
| `dV_island` | V | derived | 8.824e-06 V | `I_face_supply * R_island` | drop across them at the supply voltage, where the current is small |
| `P_island_loss` | W | derived | 5.794e-05 W | `I_face_supply^2 * R_island` | heat those conductors make, deposited inside the channel field |
| `R_plane` | ohm | derived | 6.786e-05 ohm | `res_cu * L_plate / (n_plane_mid * t_plane_mid * L_plate)` | resistance of the interposer planes carrying the intermediate rail across a face |
| `dV_plane` | V | derived | 0.004277 V | `(P_input / n_face / V_mid) * R_plane` | drop across them |
| `A_grid` | mm^2 | derived | 0.288 mm^2 | `L_die * t_grid_metal * f_grid_metal` | cross-section of one die's power grid, taken across the die's width in the top metal |
| `R_grid` | ohm | derived | 7.917e-05 ohm | `res_cu * L_reg_to_die / A_grid` | resistance from a regulator's output to the far side of a die |
| `dV_grid` | V | derived | 0.005835 V | `I_die_logic * R_grid` | drop across it at the design current |
| `dV_total` | V | derived | 0.01011 V | `dV_grid + dV_plane` | accumulated static drop to the worst-placed transistor, at the logic rail |
| `R_to_load` | ohm | derived | 7.917e-05 ohm | `dV_grid / I_die_logic` | the resistance the last level actually presents |
| `dV_static_max` | V | derived | 0.015 V | `V_logic * ir_frac` | the most the static drop may be |
| `R_to_load_max` | ohm | derived | 0.0002035 ohm | `dV_static_max / I_die_logic` | the most the last level may present |
| `I_pillar_cap` | A | derived | 1.058e+04 A | `n_pillar_pwr * i_pad_max` | current the radial pillar array will carry inward |
| `f_pillar_used` | 1 | derived | 0.002969 | `I_core_face / I_pillar_cap` | how much of that capability the core's inward supply actually uses |

## 031 — Holding the rail up for ten nanoseconds

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `t_reg_resp` | s | given | 1e-08 s | — | how long an integrated regulator takes to respond to a load step |
| `c_trench` | nF/mm^2 | measured | 100 nF/mm^2 | — | capacitance per unit area of a deep trench capacitor array in the interposer |
| `n_ramp_cycle` | 1 | given | 64 | — | cycles over which 048 admits operands at the start of an operation |
| `f_antires` | 1 | given | 1.5 | — | how far above the target impedance a damped antiresonant peak sits. Three is what an undamped two-stage network gives, and it does not fit inside the rail's tolerance band -- so damping is a requirement here rather than a refinement |
| `I_step_die` | A | derived | 61.17 A | `P_engine_die / V_logic` | current one die's multiplier array demands when it goes from idle to full |
| `t_ramp` | s | derived | 4.571e-08 s | `n_ramp_cycle / f_face` | how long the sequencer takes to get there |
| `slew_raw` | A/s | derived | 8.563e+10 A/s | `I_step_die / (1 / f_face)` | current slew if the array started all at once, in one cycle |
| `slew_ramped` | A/s | derived | 1.338e+09 A/s | `I_step_die / t_ramp` | and with the ramp |
| `C_raw` | F | derived | 2.719e-05 F | `I_step_die * t_reg_resp / dV_droop_logic` | decoupling one die would need with no ramp: the whole step, held for the regulator's whole response |
| `C_ramped` | F | derived | 2.973e-06 F | `I_step_die * t_reg_resp^2 / (2 * t_ramp * dV_droop_logic)` | and with it: the deficit while the regulator catches a ramp is the area of a triangle rather than a rectangle |
| `ramp_gain` | 1 | derived | 9.143 | `C_raw / C_ramped` | what the sixty-four cycles buy |
| `A_trench_die` | mm^2 | derived | 29.73 mm^2 | `C_ramped / c_trench` | interposer area one die's decoupling occupies |
| `A_trench_all` | mm^2 | derived | 713.6 mm^2 | `n_die * A_trench_die` | and all of it |
| `f_trench_area` | 1 | derived | 0.05162 | `A_trench_die / A_die` | that area as a share of the die it sits under |
| `Z_target` | ohm | derived | 0.0003678 ohm | `dV_droop_logic / I_step_die` | impedance the network must stay under across frequency |
| `Z_peak` | ohm | derived | 0.0005518 ohm | `Z_target * f_antires` | where an unmitigated antiresonance would put it |
| `f_ramp_cost` | 1 | derived | 0.0005742 | `t_ramp / t_layer` | what the ramp costs as a share of the time a layer takes |
| `E_stored` | J | derived | 2.007e-05 J | `n_die * C_ramped * V_logic^2 / 2` | energy held in the decoupling across the machine, which 033 has to discharge safely |

## 032 — How thin a wire is allowed to be

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `I_die_max` | A | given | 120 A | — | the most current one die's power grid and microbump array will carry, from the cross-section below and the limit at temperature |
| `sp_hillock` | um | given | 0.4 um | — | least spacing between a power conductor and a signal conductor at the top metal level, so that a hillock cannot bridge them |
| `n_yr_life` | 1 | given | 10 | — | years the conductors must survive at the design current, from 086 |
| `T_em_quoted` | K | given | 319 K | — | temperature the current density limit in 011 is quoted at. It was three hundred and fifty, which is where the conductors were assumed to run before 025's chain closed; they run cooler, and a limit quoted hot is conservative rather than wrong -- but a limit quoted at the wrong temperature at all is how this goes wrong silently |
| `A_req_die` | um^2 | derived | 7.371e+04 um^2 | `I_die_logic / j_em_cu` | cross-section one die's logic current needs |
| `A_grid_avail` | um^2 | derived | 2.88e+05 um^2 | `L_die * t_grid_metal * f_grid_metal` | cross-section the top metal actually provides across the die's width |
| `m_grid` | 1 | derived | 3.907 | `A_grid_avail / A_req_die` | margin at the binding case |
| `A_req_island` | um^2 | derived | 6566 um^2 | `I_face_supply / j_em_cu` | cross-section the via islands need |
| `A_isl_avail` | um^2 | derived | 2.827e+13 um^2 | `n_island_pad * pi * (p_island_pad * 250)^2` | cross-section they provide, taking a pad as a quarter of its pitch in radius |
| `m_island` | 1 | derived | 4.306e+09 | `A_isl_avail / A_req_island` | margin there, which is the number that showed the islands are not the problem |
| `A_req_pillar` | um^2 | derived | 3.141e+04 um^2 | `I_core_face / j_em_cu` | cross-section the inward core supply needs at one radial interface |
| `A_pil_avail` | um^2 | derived | 6.648e+14 um^2 | `n_pillar_pwr * pi * (d_radial_pad * 500)^2` | cross-section the power pillars provide |
| `m_pillar` | 1 | derived | 2.116e+10 | `A_pil_avail / A_req_pillar` | margin there |
| `m_worst` | 1 | derived | 3.907 | `min(min(m_grid, m_island), m_pillar)` | the binding case across all three |
| `j_grid` | mA/um^2 | derived | 0.2559 mA/um^2 | `I_die_logic / A_grid_avail` | actual current density in the die power grid, which is the number a process engineer will ask for |

## 033 — Turning it on, and losing it

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `brownout_frac` | 1 | given | 0.9 | — | fraction of nominal at which the supply is declared to have failed |
| `t_detect` | s | given | 2e-05 s | — | from the supply crossing that threshold to the machine being told |
| `t_ramp_dom` | s | given | 0.001 s | — | how long one domain takes to come up, set by the inrush limit |
| `n_up_step` | 1 | derived | 5 | `n_domain` | steps in the power-up sequence, one per domain |
| `C_decouple_all` | F | derived | 0.0001214 F | `n_die * C_ramped + C_core_decouple` | every decoupling capacitor in the machine |
| `C_core_decouple` | F | given | 5e-05 F | — | decoupling on the array rail inside the core and the cage |
| `I_inrush` | A | derived | 0.09102 A | `C_decouple_all * V_logic / t_ramp_dom` | current drawn charging all of it over one domain's ramp |
| `t_powerup` | s | derived | 0.0055 s | `n_up_step * t_ramp_dom + t_lock` | from supplies valid to reset released |
| `t_lock` | s | given | 0.0005 s | — | time for the reference and the multipliers in 070 to settle |
| `E_holdup` | J | derived | 0.0001084 J | `C_array_holdup * V_array^2 / 2` | energy stored to keep the array rail alive while the logic collapses |
| `C_array_holdup` | F | given | 0.0003 F | — | bulk capacitance on the array rail whose only job is to keep the model alive through a supply sag. A real component chosen for its value, not a quantity derived from what it must achieve -- the point of the constraint below is to check a choice, and a value derived from the requirement would check nothing |
| `t_holdup` | s | derived | 4.753e-08 s | `C_array_holdup * V_array * tol_rail / I_array` | how long that actually lasts |
| `E_aux_holdup` | J | given | 0.005 J | — | energy reserved to keep the auxiliary domain alive long enough to write a fault record |
| `t_down_emerg` | s | given | 0.0001 s | — | emergency down-sequence, ordered but fast, used when 027's interlock cuts on coolant loss |
| `t_holdup_ref` | s | given | 0.001 s | — | a millisecond, as the reference the hold-up energy is judged small against |
| `t_powerup_max` | s | given | 0.1 s | — | the longest the supplies may take to reach the point where reset can be released; a person waiting for a machine notices a tenth of a second and not much less |

## 034 — What the middle holds

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `w_tier_port` | bit | given | 1.024e+04 bit | — | bits one tier delivers per cycle, set by its macro count and the routing it can support across a forty millimetre die |
| `f_core` | GHz | given | 1.25 GHz | — | core clock, set by the array access time in 035 |
| `f_ecc_overhead` | 1 | derived | 0.03759 | `n_ecc_check / (n_ecc_line + n_ecc_check)` | share of raw capacity spent on check bits |
| `f_spare` | 1 | given | 0.02 | — | share held as spare rows and columns within a tier, blown at test |
| `f_redundant` | 1 | derived | 0.04167 | `1 / n_tier` | share held as one whole spare tier, mapped in if another fails |
| `n_bank` | 1 | given | 256 | — | independent banks across the whole core |
| `n_bank_min` | 1 | given | 1000 | — | how many cycles' worth of read the smallest sensible bank holds |
| `C_tier` | GB | derived | 3.317 GB | `A_core_side * d_areal` | capacity of one tier. Written first with a division by a thousand to turn megabytes into gigabytes, which is exactly the manual conversion the notation exists to make unnecessary -- and which, being a dimensionless literal, silently made the core a thousand times too small |
| `C_core_raw` | GB | derived | 79.6 GB | `n_tier * C_tier` | raw capacity of the stack |
| `C_core_usable` | GB | derived | 71.95 GB | `C_core_raw * (1 - f_ecc_overhead) * (1 - f_spare) * (1 - f_redundant)` | what a model may actually use |
| `B_core` | bit/s | derived | 3.072e+14 bit/s | `n_tier * w_tier_port * f_core` | aggregate read bandwidth, every tier delivering at once |
| `B_face_max` | bit/s | derived | 3.072e+14 bit/s | `B_core` | what a single face may take when the others are idle. Equal to the aggregate, deliberately, and 037 has to deliver it |
| `B_face_even` | bit/s | derived | 5.12e+13 bit/s | `B_core / n_face` | and what it gets when all six are asking equally |
| `C_bank` | GB | derived | 0.3109 GB | `C_core_raw / n_bank` | capacity of one bank |
| `p_collide` | 1 | derived | 0.03083 | `1 - (1 - 1/n_bank)^(n_face + 2)` | chance that two of the eight clients -- six faces, the spout and the scrubber -- want the same bank in the same cycle, assuming their addresses are independent |
| `t_core_sweep` | s | derived | 0.01658 s | `C_core_raw * 8 / B_core` | how long it takes to read the whole core once at full bandwidth, which is what 040's scrub and 069b's memory mode are both measured against |

## 035 — The cell, and the array around it

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `a_cell` | um^2/bit | measured | 0.0199 um^2/bit | — | area of one six-transistor static memory cell at the chosen node. The unit is area per bit rather than area, which sounds pedantic until a density derived from it is compared against something and turns out to be a reciprocal area with no information in it |
| `eta_array` | 1 | measured | 0.55 | — | share of a memory array that is cells rather than decoders, sense amplifiers, wordline drivers and redundancy |
| `eta_tier` | 1 | given | 0.6 | — | share of a tier's area that is array rather than through-stack vias, test structures, correction routing and the seal ring |
| `t_access` | ns | measured | 0.75 ns | — | latency from a read being issued to the first bits arriving at the tier's port |
| `n_macro_tier` | 1 | given | 4096 | — | memory macros on one tier |
| `w_macro` | bit | derived | 1.024e+04 bit | `w_tier_port / n_macro_tier * n_macro_tier` | bits a tier's macros deliver together per cycle; equal to the port width because the port is what they are sized to fill |
| `SNM_cell` | V | measured | 0.155 V | — | static noise margin of the cell at the low voltage, high temperature, slow process corner |
| `SNM_required` | V | given | 0.075 V | — | the least static noise margin that is considered safe against the soft error rate in 040 |
| `a_bit_array` | um^2/bit | derived | 0.03618 um^2/bit | `a_cell / eta_array` | area one bit occupies inside an array |
| `a_bit_tier` | um^2/bit | derived | 0.0603 um^2/bit | `a_bit_array / eta_tier` | and on a tier, once everything a tier needs besides array is counted |
| `d_areal` | MB/mm^2 | derived | 2.073 MB/mm^2 | `1 / a_bit_tier` | areal density. The unit conversion from bits per square micron to megabytes per square millimetre is the notation's job, not the author's, which is the second reason area per bit had to carry its bit |
| `d_areal_ref` | MB/mm^2 | measured | 1.78 MB/mm^2 | — | density a shipping stacked cache die reaches at an older node with a comparable arrangement, as a sanity comparison rather than a target |
| `t_cycle_core` | ns | derived | 0.8 ns | `1 / f_core` | the core's cycle time, which the access time must fit inside |
| `E_core_write` | pJ/bit | measured | 0.55 pJ/bit | — | energy to write one bit into a core tier |
| `E_core_ret` | pJ/(bit*s) | measured | 20 pJ/(bit*s) | — | retention leakage per bit at the operating temperature: about twenty picowatts a cell, which is what makes an idle machine cost anything at all. Per bit per second, so it is a power per bit and not an energy |
| `P_core_leak_d` | W | derived | 12.74 W | `E_core_ret * C_core_raw` | retention power of the whole stack, derived from the per-bit figure |
| `dV_read_need` | V | derived | 0.075 V | `SNM_required * 2 - SNM_cell + dV_read_marg` | how far the array rail must sit above the logic rail for the cell to keep its margin while being read |

## 036 — Twenty-four layers, and what is between them

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_tier` | 1 | given | 24 | — | memory tiers in the stack. Twenty-four rather than the thirty-two first sketched, because at the density 035 derives, thirty-two holds half again what is needed |
| `f_si_volume` | 1 | derived | 0.02999 | `t_tier_si / t_tier_pitch` | share of the core's volume that is silicon rather than heat exchanger |
| `h_stack` | mm | derived | 40.01 mm | `n_tier * t_tier_pitch` | height of the stack, which must be the cavity's core dimension |
| `p_tsv` | um | given | 18 um | — | pitch of the through-stack via array |
| `d_tsv` | um | given | 7 um | — | diameter of one through-stack via. Three microns was tried, then five: a forty millimetre column is a resistance and a capacitance in series, its time constant goes as the reciprocal of the via's area, and five microns settled in two hundred and eleven picoseconds against a two hundred picosecond budget. Seven gives a hundred and eight |
| `n_tsv_col` | 1 | derived | 2222 | `floor(L_core / p_tsv)` | via columns across one edge of a tier |
| `n_tsv` | 1 | derived | 4.937e+06 | `n_tsv_col^2` | through-stack vias in the whole array |
| `f_tsv_area` | 1 | derived | 0.1188 | `n_tsv * pi * (d_tsv/2)^2 / A_core_side` | share of a tier's area the via array occupies, which is part of what 035's tier overhead pays for |
| `C_tsv` | F | derived | 7.899e-12 F | `2 * pi * eps_0 * eps_ox * L_core / ln(3)` | capacitance of one through-stack via against its oxide liner, taking the shield spacing as three times the via radius |
| `R_tsv` | ohm | derived | 19.75 ohm | `res_cu * L_core / (pi * (d_tsv/2)^2)` | resistance of the same. Copper rather than tungsten: tungsten is three times the resistivity and over forty millimetres that is the difference between settling inside a core cycle and not |
| `t_tsv_delay` | s | derived | 1.076e-10 s | `0.69 * R_tsv * C_tsv` | the time constant of a via running the whole height of the stack, which is what decides whether the end faces can be treated like the side faces |
| `P_tier` | W | derived | 6.675 W | `P_core / n_tier` | heat one tier makes |
| `w_lam_chan` | mm | given | 0.3 mm | — | width of a channel in a cooling lamina; wider than a face's because the heat here is a tenth as dense |
| `h_lam_chan` | mm | given | 0.3 mm | — | depth of the same. Half the lamina's thickness was tried first and made the core a quarter void, which is far more cooling than seven watts a tier needs and put nearly forty cubic centimetres of fluid inside the machine. The lamina is thick because the stack height demanded it, not because the heat did |
| `f_void_lam` | 1 | derived | 0.09276 | `(w_lam_chan / (2 * w_lam_chan)) * (h_lam_chan / t_lamina)` | share of a lamina that is channel rather than metal, from the channel geometry rather than assumed |
| `n_lam_chan` | 1 | derived | 66 | `floor(L_core / (w_lam_chan * 2))` | channels across one lamina, on a pitch of twice their width |
| `Q_lamina` | m^3/s | derived | 2.07e-07 m^3/s | `Q_core / n_tier` | flow through one lamina |
| `A_wet_lam` | mm^2 | derived | 3168 mm^2 | `n_lam_chan * 2 * (w_lam_chan + h_lam_chan) * L_core` | wetted area of one lamina |
| `dT_core` | K | derived | 0.2508 K | `P_tier / (h_conv_lam * A_wet_lam)` | temperature a tier sits above its coolant. Two hand-written conversions -- millimetres to metres in the coefficient and square millimetres to square metres here -- between them made this term a thousand times too large, which is what a checker is for |
| `h_conv_lam` | W/(m^2*K) | derived | 8400 W/(m^2*K) | `4.0 * k_fluid / (2 * w_lam_chan * h_lam_chan / (w_lam_chan + h_lam_chan))` | convection coefficient in a lamina channel, at the laminar Nusselt number for a duct of about this shape |
| `Q_core` | m^3/s | derived | 4.968e-06 m^3/s | `Q_total * P_core / P_heat` | the core's share of the machine's flow, in proportion to its share of the heat |
| `disp_tier_lam` | mm | derived | 0.01056 mm | `(cte_cumo - cte_si) * dT_power * L_core` | relative motion at one tier-to-lamina interface over a power cycle |

## 037 — Six askers, one store

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_port` | 1 | derived | 8 | `n_face + 2` | clients on the switch: six faces, the spout and the scrubber |
| `w_xbar_slice` | bit | given | 1024 bit | — | width of one crossbar slice, the granularity the fabric is built in |
| `n_xbar_slice` | 1 | derived | 240 | `w_tier_port * n_tier / w_xbar_slice` | slices needed to carry the whole core at once |
| `q_arb` | bit | given | 6.554e+04 bit | — | arbitration quantum: how much one client is given before the arbiter reconsiders |
| `a_xbar_slice` | mm^2 | measured | 0.021 mm^2 | — | area of one crossbar slice's worth of switch at this node, including its buffering |
| `e_xbar_bit` | pJ/bit | measured | 0.045 pJ/bit | — | energy to move one bit through the switch fabric |
| `B_bisect` | bit/s | derived | 3.072e+14 bit/s | `n_xbar_slice * w_xbar_slice * f_core` | bisection bandwidth of the fabric |
| `A_xbar` | mm^2 | derived | 40.32 mm^2 | `n_xbar_slice * a_xbar_slice * n_port` | area the switch occupies, which is what sets the cage thickness in 012 |
| `P_xbar` | W | derived | 13.82 W | `e_xbar_bit * B_core` | power it takes at full traffic |
| `t_quantum` | s | derived | 1.28e-09 s | `q_arb / B_face_even` | how long one client holds the switch before the arbiter reconsiders |
| `t_wait_face` | s | derived | 6.4e-09 s | `t_quantum * (n_face - 1)` | worst case a face waits when all six are asking, which is what 053's stage budget must absorb |
| `t_wait_spout` | s | derived | 1.024e-08 s | `t_quantum * n_port` | worst case the spout waits, being low priority |
| `t_wait_scrub` | s | derived | 2.048e-08 s | `t_quantum * n_port * 2` | and the scrubber, being lower |
| `A_cage_avail` | mm^2 | derived | 1.27e+04 mm^2 | `6 * L_cavity^2 * t_cage / t_cage` | silicon area available in the cage shell, taken as its six inner faces |

## 038 — What lives where

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `C_request` | MB | given | 1 MB | — | where a host puts a token identifier and takes one back; the machine's only compatibility surface |
| `C_control` | MB | given | 1 MB | — | control and status, including the pane window's aliasing register |
| `C_repair` | MB | given | 16 MB | — | the repair map and scrub state from 040 |
| `w_interleave` | bit | given | 3.277e+04 bit | — | address granularity at which consecutive addresses move to the next bank. Eight thousand was tried and is narrower than a single cycle's read from one tier, which would have meant every transfer straddling two banks |
| `C_pane` | MB | derived | 2.097 MB | `n_pane_bit` | the window the spout sees, from 062 |
| `n_region` | 1 | given | 9 | — | regions in the map |
| `C_staging` | MB | derived | 0.4588 MB | `n_stage * C_stage_buf` | the six forward staging buffers |
| `C_staging_r` | MB | derived | 0.4588 MB | `n_stage * C_stage_buf` | and the six reverse ones for training |
| `C_mapped` | GB | derived | 55.19 GB | `C_weights + C_kv + C_staging + C_staging_r + C_checkpoint + C_adapter + C_request + C_control + C_repair` | everything with an address |
| `C_free` | GB | derived | 16.76 GB | `C_core_usable - C_mapped` | what is left |
| `f_mapped` | 1 | derived | 0.7671 | `C_mapped / C_core_usable` | how full the map is at the reference model |
| `n_bank_stride` | 1 | derived | 3.2 | `w_interleave / w_tier_port` | cycles a single bank is held before the address moves on |

## 039 — The rule six faces rely on

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `t_release` | ns | given | 12 ns | — | latency of a release: the time from issuing it to every earlier write being complete |
| `t_acquire` | ns | given | 8 ns | — | latency of an acquire |
| `n_share_site` | 1 | given | 3 | — | places in this machine where two faces touch the same memory |
| `coherent` | 1 | given | 0 | — | whether hardware coherence exists. It does not, and this is written as a number so that a blueprint assuming otherwise fails rather than merely being wrong |
| `t_write_max` | ns | derived | 19.15 ns | `t_release + t_access + t_wait_face` | the longest a write can be in flight: its own release, the array's access, and the worst the arbiter can make it wait. 033's brownout hold-up is judged against this. Written first with a hand conversion from seconds to nanoseconds, which turned nineteen nanoseconds into six and a half seconds |
| `t_handoff` | ns | derived | 38.49 ns | `t_release + t_acquire + 2 * t_link_rt` | a full stage handoff: release, the flag crossing to the next face, acquire, and the buffer read |
| `t_pane_excl` | ns | derived | 54.61 ns | `C_pane / B_core` | how long the pane read excludes writes, which is the price of choosing exclusion |
| `f_pane_stall` | 1 | derived | 5.661e-05 | `t_pane_excl / t_token` | that price as a share of a token |

## 040 — Catching a bit that flipped

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `fit_per_bit` | 1/(bit*s) | measured | 2.78e-19 1/(bit*s) | — | soft error rate for static memory at sea level: a thousand failures per thousand million hours per megabit, converted once here so that nothing downstream has to multiply by three thousand six hundred and hope |
| `n_ecc_line` | bit | given | 256 bit | — | data bits one correction line covers |
| `n_ecc_check` | bit | given | 10 bit | — | check bits on that line: nine to locate a single error among two hundred and fifty-six, one more to detect a second |
| `t_scrub` | s | given | 3600 s | — | period over which the whole core is read and rewritten |
| `n_spare_row` | 1 | given | 64 | — | spare rows per tier, blown at test |
| `n_redundant` | 1 | given | 1 | — | whole tiers held in reserve |
| `n_ecc_min` | bit | given | 10 bit | — | the fewest check bits that can locate one error among two hundred and fifty-six and detect a second |
| `f_scrub_max` | 1 | given | 0.0001 | — | the most of the core's bandwidth the scrubber may take before it stops being invisible |
| `n_scrub_margin` | 1 | given | 100 | — | how many times over the scrub must complete between upsets |
| `lam_core` | 1/s | derived | 1.77e-07 1/s | `fit_per_bit * C_core_raw` | upsets a second anywhere in the core |
| `t_upset` | s | derived | 5.649e+06 s | `1 / lam_core` | mean time between them |
| `n_line` | 1 | derived | 2.487e+09 | `C_core_raw / n_ecc_line` | correction lines in the core |
| `lam_line` | 1/s | derived | 7.117e-17 1/s | `1 / (t_upset * n_line)` | upset rate in any one line |
| `t_double` | s | derived | 4.41e+19 s | `2 / (lam_core * lam_line * t_scrub)` | mean time to two upsets landing in the same line within one scrub period, which is the only way a correctable machine becomes an uncorrectable one |
| `B_scrub` | bit/s | derived | 1.769e+08 bit/s | `C_core_raw / t_scrub` | bandwidth the scrubber consumes |
| `f_scrub` | 1 | derived | 5.758e-07 | `B_scrub / B_core` | that as a share of the core's, which is what makes it invisible |
| `t_scrub_period` | s | derived | 3600 s | `t_scrub` | the same figure, under the name 037's starvation bound refers to |
| `f_ecc_line` | 1 | derived | 0.03759 | `n_ecc_check / (n_ecc_line + n_ecc_check)` | overhead of this code |
| `f_ecc_64` | 1 | derived | 0.1111 | `8 / 72` | what a sixty-four bit line would have cost, for the comparison |
| `runtime_repair` | 1 | given | 0 | — | whether a line can be retired and remapped while the machine runs. It cannot: the map would have to survive power loss and nothing in this cube is non-volatile |

## 041 — The plan of one die

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `f_area_slice` | 1 | given | 0.5 | — | share of a compute die given to its cache slice |
| `f_area_engine` | 1 | given | 0.104 | — | share given to the multiplier array and its accumulators |
| `f_area_expand` | 1 | given | 0.087 | — | share given to operand staging and the weight expansion path |
| `f_area_seq` | 1 | given | 0.026 | — | share given to the sequencer |
| `f_area_scalar` | 1 | given | 0.014 | — | share given to the scalar core |
| `f_area_link` | 1 | given | 0.069 | — | share given to the radial link termination on this die |
| `f_area_pwr` | 1 | given | 0.104 | — | share given to the power grid, clock and decoupling access |
| `f_area_margin` | 1 | given | 0.096 | — | routing and margin |
| `n_tile_engine` | 1 | given | 64 | — | tiles the multiplier array is broken into and scattered across the die |
| `L_tile_min` | mm | given | 0.5 mm | — | the smallest an engine tile may be before its own boundary costs more in routing than the spreading it buys |
| `util_design` | 1 | given | 1 | — | multiplier utilisation at the design operating point, which is the crossover batch where the engines are the wall |
| `f_area_sum` | 1 | derived | 1 | `f_area_slice + f_area_engine + f_area_expand + f_area_seq + f_area_scalar + f_area_link + f_area_pwr + f_area_margin` | everything on the die, which must be everything |
| `A_slice_die` | mm^2 | derived | 288 mm^2 | `f_area_slice * A_die` | area one die's slice occupies |
| `A_engine_die` | mm^2 | derived | 59.9 mm^2 | `f_area_engine * A_die` | area its multiplier array occupies |
| `L_tile` | mm | derived | 0.9675 mm | `sqrt(A_engine_die / n_tile_engine)` | edge of one engine tile |
| `p_tile` | mm | derived | 3 mm | `L_die / sqrt(n_tile_engine)` | spacing between engine tiles, which is how far heat travels to reach cold memory |
| `f_engine_power` | 1 | derived | 0.7895 | `P_engine_die / P_die` | share of a die's heat the array makes, which 025's local term needs |
| `f_engine_area` | 1 | derived | 0.104 | `f_area_engine` | and the share of its area, under the name 025 uses |
| `dT_lateral` | K | derived | 136.9 K | `f_engine_power * P_die / n_tile_engine * (p_tile - L_tile) / (k_si * L_tile * t_die)` | temperature drop for heat crossing the gap from one engine tile into the memory around it, which is what the scattering buys and what limits how much it buys |

## 042 — Why a face is four dies

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `w_reticle` | mm | measured | 26 mm | — | short side of a scanner's exposure field |
| `h_reticle` | mm | measured | 33 mm | — | long side of the same |
| `n_die_face` | 1 | given | 4 | — | compute dies on one face |
| `w_scribe` | mm | given | 0.08 mm | — | scribe lane between two dies on a wafer |
| `e_interdie` | pJ/bit | measured | 0.35 pJ/bit | — | energy to move one bit between two dies on the same face interposer |
| `f_interdie` | 1 | given | 0.02 | — | share of a face's operand traffic that crosses a die boundary despite the partitioning |
| `A_reticle` | mm^2 | derived | 858 mm^2 | `w_reticle * h_reticle` | area of one exposure field |
| `f_reticle_use` | 1 | derived | 0.6713 | `A_die / A_reticle` | how much of a field one die uses |
| `B_interdie` | bit/s | derived | 1.507e+12 bit/s | `f_interdie * B_operand_die * n_die_face` | traffic crossing die boundaries on one face |
| `P_interdie` | W | derived | 0.5276 W | `e_interdie * B_interdie` | what that costs |
| `w_link_split` | 1 | derived | 4 | `n_die_face` | how many ways the radial link is divided, one segment per die |
| `n_die_total` | 1 | derived | 24 | `n_die_face * n_face` | compute dies in the machine |

## 043 — The small instruction set

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_inst_scalar` | 1 | given | 25 | — | scalar instructions |
| `n_inst_seq` | 1 | given | 6 | — | sequencer control instructions |
| `n_inst_tensor` | 1 | given | 12 | — | tensor operations, reachable only through descriptors |
| `n_inst_sample` | 1 | given | 6 | — | sampling instructions |
| `w_inst` | bit | given | 32 bit | — | instruction encoding width |
| `n_reg` | 1 | given | 32 | — | scalar registers |
| `w_reg` | bit | given | 64 bit | — | width of one |
| `n_trap` | 1 | given | 2 | — | trap conditions the machine recognises |
| `n_exp_terms` | 1 | given | 6 | — | terms in the specified exponential's polynomial |
| `n_rsqrt_iter` | 1 | given | 3 | — | Newton iterations in the specified reciprocal square root |
| `n_inst` | 1 | derived | 49 | `n_inst_scalar + n_inst_seq + n_inst_tensor + n_inst_sample` | instructions altogether |
| `n_opcode` | 1 | derived | 128 | `2^7` | opcodes the encoding reserves, taking seven bits of the word |
| `f_opcode_used` | 1 | derived | 0.3828 | `n_inst / n_opcode` | how much of the opcode space is spent |
| `C_regfile` | bit | derived | 2048 bit | `n_reg * w_reg` | the scalar register file |
| `n_reduce_op` | 1 | derived | 18 | `n_inst_tensor + n_inst_sample` | operations that perform a reduction and therefore need a specified order and width |

## 044 — The small core that sets things up

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_pipe_stage` | 1 | given | 5 | — | pipeline stages |
| `n_scalar_inst` | 1 | given | 400 | — | scalar instructions executed per token per face, building thirteen layers of descriptor chain |
| `C_local_mem` | bit | given | 2.621e+05 bit | — | local memory on the scalar core for descriptor construction |
| `n_outstanding` | 1 | given | 128 | — | memory requests the core may have in flight. Sixteen was a round number and covers a seventh of a link round trip, which means the core stalls on six descriptor fields out of seven |
| `w_rng_state` | bit | given | 128 bit | — | width of the carried random state, per sequence |
| `w_rng_min` | bit | given | 64 bit | — | the least width at which two sequences will not collide over a long conversation |
| `n_core_face` | 1 | derived | 4 | `n_die_face` | scalar cores on a face, one per die |
| `t_scalar_tok` | s | derived | 2.857e-07 s | `n_scalar_inst / f_face` | time the core spends per token, at one instruction a cycle |
| `util_scalar` | 1 | derived | 0.001777 | `t_scalar_tok / t_stage` | how busy it is |
| `A_scalar_face` | mm^2 | derived | 32.26 mm^2 | `f_area_scalar * A_die * n_die_face` | area four scalar cores take on a face |
| `f_scalar_face` | 1 | derived | 0.014 | `A_scalar_face / (A_die * n_die_face)` | that as a share of a face, which is the cost of choosing four over one |
| `t_cover_link` | s | derived | 1.024e-08 s | `n_outstanding * w_transfer / B_face_even` | how long the outstanding requests cover, which must exceed a link round trip or the core stalls |

## 045 — Sixty-five thousand multipliers

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_mac_row` | 1 | given | 256 | — | multiplier cells across one row of the array |
| `f_face` | GHz | given | 1.4 GHz | — | the face clock |
| `a_mac` | um^2 | measured | 480 um^2 | — | area of one multiply-accumulate cell including its share of the accumulator |
| `w_acc` | bit | given | 32 bit | — | accumulator width |
| `n_lut_entry` | 1 | given | 16 | — | entries in the shared expansion table one group of weights indexes |
| `n_flop_mac` | flop | given | 2 flop | — | operations one multiply-accumulate cell performs per cycle: a multiply and an add. It carries the unit so that an operation rate is a rate of operations rather than a bare reciprocal second, which is the distinction that keeps a throughput from being added to a clock |
| `a_expand_lane` | um^2 | measured | 900 um^2 | — | area of one expansion lane: the table lookup and the scale multiply |
| `f_bwd_operand` | 1 | given | 2 | — | how much more operand bandwidth the backward pass needs, streaming differently instead of transposing the array |
| `n_mac` | 1 | derived | 6.554e+04 | `n_mac_row^2` | multiplier cells in one array |
| `A_mac_array` | mm^2 | derived | 31.46 mm^2 | `n_mac * a_mac` | area they occupy |
| `A_expand` | mm^2 | derived | 0.2304 mm^2 | `n_mac_row * a_expand_lane` | area the expansion path occupies, one lane per row |
| `ops_die` | flop/s | derived | 1.835e+14 flop/s | `n_flop_mac * n_mac * f_face` | operations a die can issue a second |
| `ops_machine` | flop/s | derived | 4.404e+15 flop/s | `ops_die * n_die` | and the whole machine |
| `P_engine_die` | W | derived | 45.88 W | `E_op * ops_die * util_design` | switching power of one die's multiplier array at the design utilisation |
| `n_reuse` | 1 | derived | 28 | `batch_design` | how many times a weight is used from the slice for each time it is read from the core |
| `I_step_engine` | A | derived | 61.17 A | `E_op * ops_die / V_logic` | current the array demands when every cell has an operand, which 031 sized the decoupling from |
| `B_operand_die` | bit/s | derived | 1.884e+13 bit/s | `(n_mac / batch_design * w_weight + n_mac_row * w_act) * f_face` | operand bandwidth from the slice: a weight tile reloaded every batch cycles at four bits each, plus a row of activations every cycle at sixteen |
| `acc_headroom` | 1 | derived | 4.571 | `2^(w_acc / b1 - 1) / (n_reduce_max * range_operand^2)` | how much margin the accumulator has against the largest reduction in the model, each product at most the square of an operand's range. Written first against one row of the array, which is not the reduction -- the array accumulates across many passes and the real length is the model's widest tensor |

## 046 — How numbers are written down

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `b1` | bit | given | 1 bit | — | one bit. A width carries the unit so that a format's size can be added to a capacity; an exponent needs a pure number. Dividing by this is how a width becomes a count, and it is the only place in the project where a unit quantity is declared for that purpose |
| `w_weight` | bit | given | 4 bit | — | bits per weight index |
| `n_group` | 1 | given | 128 | — | weights sharing one expansion table and one scale |
| `w_scale` | bit | given | 16 bit | — | width of a group's scale |
| `w_act` | bit | given | 16 bit | — | activations in the residual stream |
| `w_operand` | bit | given | 8 bit | — | engine operands |
| `w_kv` | bit | given | 8 bit | — | key and value cache entries |
| `w_logit` | bit | given | 32 bit | — | logits, on face five only |
| `n_round_mode` | 1 | given | 1 | — | rounding modes the machine has. One, so that no conversion anywhere can differ between two implementations |
| `special_vals` | 1 | given | 0 | — | whether denormals, infinities and not-a-number are propagated. They are not; producing one raises a trap |
| `w_weight_eff` | bit | derived | 4.125 bit | `w_weight + w_scale / n_group` | bits per weight including the amortised scale, which is the number that decides how long a token takes |
| `f_scale_cost` | 1 | derived | 0.0303 | `(w_scale / n_group) / w_weight_eff` | what the scale costs as a share of the weight read, which is the whole of open question F1 |
| `w_weight_8` | bit | derived | 8.125 bit | `w_operand + w_scale / n_group` | what eight-bit weights would cost, for the comparison |
| `speed_ratio` | 1 | derived | 1.97 | `w_weight_8 / w_weight_eff` | how much faster four bits makes the machine than eight, which is the trade being made |
| `range_operand` | 1 | derived | 128 | `2^(w_operand / b1 - 1)` | largest magnitude an engine operand can carry |
| `n_lut_bits` | bit | derived | 256 bit | `n_lut_entry * w_scale` | bits of expansion table one group carries, which the slice stores alongside the weights |

## 047 — The memory a face keeps to itself

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `eta_slice` | 1 | given | 0.16 | — | share of the slice region that is bitcell rather than sense amplifiers, correction, banking and shared routing. Lower than the core's tiers because a slice lives on a logic die |
| `n_slice_buf` | 1 | given | 2 | — | buffers: one being computed from, one being filled |
| `n_slice_bank` | 1 | given | 64 | — | banks per die's slice |
| `batch_design` | 1 | given | 28 | — | the batch the machine is provisioned for; 079 derives the crossover independently and C-079-1 requires the two to agree |
| `e_slice_bit` | pJ/bit | derived | 0.15 pJ/bit | `E_slice_bit` | read energy per bit, under the name 020 uses |
| `p_seq_conflict` | 1 | given | 0.02 | — | chance a sequencer's small read finds its bank busy with a burst |
| `a_bit_slice` | um^2/bit | derived | 0.1244 um^2/bit | `a_cell / eta_slice` | area one bit occupies in a face slice |
| `d_slice` | MB/mm^2 | derived | 1.005 MB/mm^2 | `1 / a_bit_slice` | areal density on a logic die |
| `C_slice_die` | MB | derived | 289.4 MB | `A_slice_die * d_slice` | capacity of one die's slice |
| `C_face_slice` | MB | derived | 1158 MB | `C_slice_die * n_die_face` | and of a whole face's |
| `C_slice_need` | MB | derived | 882.4 MB | `n_slice_buf * C_layer_weights` | what the two-buffer requirement actually demands |
| `m_slice` | 1 | derived | 1.312 | `C_face_slice / C_slice_need` | margin on the tightest constraint in the project, which is worth having as a number rather than as a pass |
| `B_slice_read` | bit/s | derived | 3.67e+15 bit/s | `n_slice_bank * w_tier_port * f_face * n_die_face` | rate a face's slice serves reads, all banks at once |
| `t_seq_wait` | s | derived | 1.429e-12 s | `p_seq_conflict * w_transfer / (B_slice_read / n_slice_bank)` | how long a sequencer's small read waits when it lands behind a burst |
| `d_slice_ratio` | 1 | derived | 2.062 | `d_areal / d_slice` | how much denser a dedicated tier is than a slice on a logic die |

## 048 — The thing that walks a layer

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `w_desc` | bit | given | 512 bit | — | width of one descriptor |
| `w_desc_min` | bit | given | 256 bit | — | the least width that carries two addresses, two shapes, an operation and a link at the core's address width |
| `n_desc_layer` | 1 | given | 34 | — | descriptors in one layer's chain: a dozen operations, several of them per head group |
| `n_seq_state` | 1 | given | 12 | — | states in the sequencer's machine |
| `n_ramp_cycle_s` | 1 | derived | 64 | `n_ramp_cycle` | cycles the operand admission ramp takes, under the name 031 uses |
| `n_small_read` | 1 | given | 8 | — | small control reads the sequencer makes per layer, which is the count 040 needed to choose its correction granularity |
| `t_prefetch_lead` | s | derived | 6.894e-05 s | `t_link_rt + C_layer_weights / B_face_even` | how far ahead the prefetch must start: a link round trip plus the time to move a whole layer |
| `n_interdie_sync` | 1 | given | 1 | — | inter-die crossings per layer for the four sequencers to agree |
| `C_chain_layer` | bit | derived | 1.741e+04 bit | `w_desc * n_desc_layer` | one layer's chain |
| `C_chain_face` | bit | derived | 2.437e+05 bit | `C_chain_layer * n_layer_face` | all of a face's chains, built once at load |
| `n_small_tok` | 1 | derived | 112 | `n_small_read * n_layer_face` | small reads per token per face, which 040 asserts against its line width |
| `t_layer` | s | derived | 7.961e-05 s | `t_layer_comp` | how long one layer takes at the design batch. It is the arithmetic time and not the transfer time, because below the crossover a prefetch and the compute it hides behind are the same memory traffic and cannot overlap at all -- double buffering earns itself above the crossover, where the arithmetic is the wall and the transfer can run underneath it |
| `f_prefetch_lead` | 1 | derived | 0.866 | `t_prefetch_lead / t_layer` | how much of a layer's time the prefetch must run ahead by |

## 049 — How anything finds out what a face is doing

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_sensor_die` | 1 | given | 16 | — | temperature sensors on one compute die. Eight was a round number chosen before 041 scattered the array into sixty-four tiles; at eight, three quarters of the hot regions have no sensor near them |
| `t_sensor_resp` | s | given | 0.0002 s | — | response time of one, its own settling included |
| `w_counter` | bit | given | 48 bit | — | width of a performance counter |
| `w_reg_ctrl` | bit | given | 64 bit | — | width of one control or status register |
| `n_counter` | 1 | given | 12 | — | performance counters per face |
| `n_fault_bit` | 1 | given | 24 | — | sticky fault bits per face |
| `n_reg_ctrl` | 1 | given | 64 | — | control and status registers per face |
| `n_model_term` | 1 | given | 7 | — | terms in 080's performance model, each of which must have a counter |
| `t_session` | s | given | 3600 s | — | a working session: the span over which a measurement has to mean something, and therefore the span a counter must not wrap in |
| `n_sensor_face` | 1 | derived | 64 | `n_sensor_die * n_die_face` | sensors on one face |
| `sensor_per_tile` | 1 | derived | 0.25 | `n_sensor_die / n_tile_engine` | sensors per engine tile, which is what decides whether the hot region is actually being watched |
| `t_counter_wrap` | s | derived | 2.011e+05 s | `2^(w_counter / b1) / f_face` | how long a counter runs at full rate before it wraps |
| `C_ctrl` | bit | derived | 4096 bit | `n_reg_ctrl * w_reg_ctrl` | the register file |
| `t_sensor_lag` | s | derived | 0.0004558 s | `t_sensor_resp + tau_engine / 10` | total lag from the hot region changing to the reading changing: the sensor's own settling plus the time for the change to reach it through a tenth of the engine's thermal constant |

## 050 — Six spokes and no rim

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_link` | 1 | derived | 6 | `n_face` | radial links, one per face |
| `L_link` | mm | derived | 7 mm | `t_face` | length of one, face to cage |
| `L_face_adj` | mm | derived | 60 mm | `L_cube` | the wire an adjacent-face link would have needed |
| `L_face_opp` | mm | derived | 120 mm | `2 * L_cube` | and an opposite-face one |
| `n_pair` | 1 | derived | 15 | `n_face * (n_face - 1) / 2` | face pairs a mesh would have had to connect |
| `n_closure` | 1 | derived | 2 | `2` | distinct link designs a mesh would have needed, one per distance |
| `ratio_reach` | 1 | derived | 17.14 | `L_face_opp / L_link` | how much further a face-to-face wire would have had to reach |
| `f_handoff` | 1 | derived | 1.261e-05 | `C_handoff / (C_weights / n_face)` | that as a share of the weight traffic one stage moves, which is the number that says a mesh would be carrying nothing |

## 051 — Seven millimetres of wire

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `d_radial_pad` | um | given | 20 um | — | pitch of the radial interface's pad array |
| `f_radial_power` | 1 | given | 0.4 | — | share of positions given to power and ground, which 030 needs for the core's inward supply |
| `f_radial_spare` | 1 | given | 0.05 | — | share held as spares against bonds that fail |
| `r_pad_bit` | bit/s | given | 2e+09 bit/s | — | bits a second one conductor carries: a low swing over a short reach, with no equalisation |
| `f_medium_link` | 1 | given | 0.5 | — | how fast a signal travels in this medium, as a fraction of the speed of light |
| `L_reach_max` | mm | given | 12 mm | — | the furthest this circuit is valid over. Beyond it, equalisation and clock recovery come back and it becomes 067's problem |
| `V_link_min` | V | given | 0.45 V | — | the least swing that keeps the receiver's noise margin across this interface, which 029 reads |
| `w_tile_link` | 1 | given | 4096 | — | conductors in one source-synchronous tile |
| `e_link_bit_d` | pJ/bit | measured | 0.1 pJ/bit | — | energy to move one bit across the interface, driver and receiver together |
| `L_radial` | mm | derived | 46 mm | `L_cavity` | edge of the square patch where a face meets the cage |
| `n_radial_col` | 1 | derived | 2300 | `floor(L_radial / d_radial_pad)` | pad columns across it |
| `n_radial_pad` | 1 | derived | 5.29e+06 | `n_radial_col^2` | positions in the array |
| `n_radial_sig` | 1 | derived | 2.909e+06 | `n_radial_pad * (1 - f_radial_power - f_radial_spare)` | conductors carrying data |
| `n_radial_spare` | 1 | derived | 2.645e+05 | `n_radial_pad * f_radial_spare` | conductors held in reserve |
| `B_link_pads` | bit/s | derived | 5.819e+15 bit/s | `n_radial_sig * r_pad_bit` | what the pad count alone would permit |
| `P_link_pads` | W | derived | 581.9 W | `e_link_bit_d * B_link_pads` | and what that would cost, which is the number that shows power binds |
| `B_link_used` | bit/s | derived | 3.072e+14 bit/s | `B_core` | what the link is actually provisioned for: everything the memory behind it can deliver |
| `P_link_used` | W | derived | 30.72 W | `e_link_bit_d * B_link_used` | and what that costs on one link at full rate |
| `bind_ratio` | 1 | derived | 18.94 | `B_link_pads / B_link_used` | how much more the pads would have allowed than the power budget does |
| `n_tile_link` | 1 | derived | 710.3 | `n_radial_sig / w_tile_link` | source-synchronous tiles across the interface |
| `t_flight` | s | derived | 4.67e-11 s | `L_link / (c_light * f_medium_link)` | one-way flight time over the link |
| `t_link_rt` | s | derived | 9.243e-09 s | `2 * t_flight + t_access + t_wait_face + t_proto` | round trip from a face issuing a read to the first data arriving: two flights, the array's access, the worst arbitration wait, and the protocol's own overhead |
| `t_proto` | s | given | 2e-09 s | — | fixed protocol overhead in a round trip: framing, decode and the crossing into the cage's clock domain |

## 052 — What a transfer looks like

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `w_header` | bit | given | 128 bit | — | header on one transfer: operation, address, sequence and credit accounting |
| `n_txn_type` | 1 | given | 5 | — | transaction types: read, write, staging release, control read, control write |
| `w_transfer` | bit | given | 4096 bit | — | payload of one transfer. Sixteen of 040's correction lines, an eighth of 038's interleave, and large enough that the header is three per cent |
| `n_credit` | 1 | given | 256 | — | outstanding transfers one port may have |
| `f_overhead` | 1 | derived | 0.0303 | `w_header / (w_header + w_transfer)` | share of the link spent on headers |
| `n_line_txfr` | 1 | derived | 16 | `w_transfer / n_ecc_line` | correction lines in one transfer |
| `bdp_face` | bit | derived | 4.733e+05 bit | `B_face_even * t_link_rt` | bandwidth-delay product for one face at its even share |
| `n_credit_need` | 1 | derived | 115.5 | `bdp_face / w_transfer` | credits needed to cover it |
| `B_eff_face` | bit/s | derived | 5.12e+13 bit/s | `min(B_face_even, n_credit * w_transfer / t_link_rt)` | what a face actually achieves, given its credits |
| `f_credit_loss` | 1 | derived | 0 | `1 - B_eff_face / B_face_even` | bandwidth lost to running out of credits, which is a loss with no symptom |
| `n_line_per_token` | 1 | derived | 1.895e+08 | `C_weights / n_ecc_line / n_face` | correction lines one face touches per token, which 048's small reads are judged rare against |

## 053 — The schedule the machine exists to run

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_microbatch` | 1 | given | 6 | — | microbatches in flight above the crossover, one per stage |
| `f_poll_backoff` | 1 | given | 0.01 | — | share of a stage a polling face spends actually issuing reads, the rest being back-off |
| `tol_stage` | 1 | given | 0.05 | — | how unequal the six stages may be before the slowest visibly sets the rate |
| `f_end_rate` | 1 | given | 0.02 | — | share of steps in which some sequence in the batch produces an end marker |
| `t_token` | s | derived | 0.0009648 s | `(C_weights + B_kv_seq) / B_core` | time for one token of one sequence, bandwidth-bound: every weight once, plus that sequence's cache |
| `t_step` | s | derived | 0.001437 s | `(C_weights + B_kv_tok) / B_core` | and for one step of a whole batch, where the weights are read once for everybody and the cache is read for each |
| `t_layer_comp` | s | derived | 7.961e-05 s | `batch_design * 2 * p_layer * n_flop_mac / 2 / ops_face` | how long one layer's arithmetic takes at the design batch, which is what a prefetch actually has to hide behind |
| `ops_face` | flop/s | derived | 6.019e+14 flop/s | `ops_die * n_die_face * eta_array_util` | arithmetic one face actually delivers |
| `t_stage` | s | derived | 0.0001608 s | `t_token / n_stage` | how long one face works before the sieve moves on |
| `t_token_comp` | s | derived | 0.0008971 s | `batch_design * flop_token / ops_machine` | and what one step would take if arithmetic were the wall |
| `C_stage_buf` | MB | derived | 0.07646 MB | `C_activation * batch_design / n_microbatch` | one staging buffer: a microbatch of activation vectors |
| `C_stage_min` | MB | derived | 0.01638 MB | `C_activation` | the least a staging buffer can be and still hold one sequence's activations |
| `B_poll` | bit/s | derived | 2.659e+10 bit/s | `n_face * f_poll_backoff * w_transfer / t_link_rt` | bandwidth six polling faces consume |
| `f_poll_cost` | 1 | derived | 8.655e-05 | `B_poll / B_core` | that as a share of the core's |
| `n_fill` | 1 | derived | 5 | `n_stage - 1` | microbatches that enter an empty pipeline before it is full |
| `f_fill_cost` | 1 | derived | 0.009901 | `n_fill / (n_fill + n_token_typical)` | what filling and draining costs on a generation of typical length |
| `n_token_typical` | 1 | given | 500 | — | tokens in a generation long enough to be worth measuring |
| `f_bubble_cost` | 1 | derived | 0.003333 | `f_end_rate / n_stage` | what letting an ended sequence's bubble propagate costs |

## 054 — Proving it cannot stop

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_channel` | 1 | given | 2 | — | independent channels: request and response, sharing no queue anywhere |
| `n_ring_break` | 1 | given | 1 | — | stages at which the ring is broken by a stage that never blocks |
| `f_timeout_marg` | 1 | given | 100 | — | how many times the proven worst case a timeout must be set at |
| `t_wait_worst` | s | derived | 2.048e-08 s | `max(max(t_wait_face, t_wait_spout), t_wait_scrub)` | the longest any client can wait, across all three classes |
| `t_timeout_bar` | s | derived | 3.849e-06 s | `t_handoff * f_timeout_marg` | timeout on a staging barrier |
| `t_timeout_txn` | s | derived | 9.243e-07 s | `t_link_rt * f_timeout_marg` | timeout on a transfer |
| `n_resource` | 1 | derived | 16 | `n_channel + n_stage + n_port` | resources a client can hold while waiting for another, which is the size of the graph the argument has to cover |
| `f_wait_stage` | 1 | derived | 0.0001274 | `t_wait_worst / t_stage` | the worst wait as a share of a pipeline stage |

## 055 — The arithmetic the claim rests on

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `B_stage_hand` | bit/s | derived | 3.804e+09 bit/s | `C_handoff / t_stage` | staging handoff traffic, one microbatch of activations per stage per step |
| `B_seq_small` | bit/s | derived | 4.755e+08 bit/s | `n_small_tok * w_transfer / t_token` | the sequencer's small control reads |
| `B_spout_avg` | bit/s | derived | 1.678e+10 bit/s | `C_pane / t_spout_period` | the spout, averaged over how often a pane is actually taken |
| `t_spout_period` | s | given | 0.001 s | — | how often a pane is taken in ordinary operation, which is a use assumption rather than a hardware property |
| `B_other` | bit/s | derived | 4.782e+10 bit/s | `B_stage_hand + B_seq_small + B_spout_avg + B_scrub + B_poll` | everything that is not weight traffic |
| `f_other` | 1 | derived | 0.0001557 | `B_other / B_core` | that as a share of the core |
| `m_bisect` | 1 | derived | 1 | `B_bisect / B_core` | the crossbar's margin over the memory |
| `m_link` | 1 | derived | 18.94 | `B_link_pads / B_core` | the link's, counting pads rather than power |
| `m_slice_bw` | 1 | derived | 194.8 | `B_slice_read / B_operand_die` | the slice's margin over what one die's engine consumes |
| `B_face_single` | bit/s | derived | 3.072e+14 bit/s | `B_face_max` | what one face gets with the others idle, which must be the aggregate |
| `f_single` | 1 | derived | 1 | `B_face_single / B_core` | that as a share, which is the claim reduced to a number that must be one |
| `t_token_bw` | s | derived | 0.0009648 s | `(C_weights + B_kv_seq) / B_core` | time per token from this chain: every weight once and one sequence's cache. 061 and 080 must agree with it |

## 056 — One outward surface, six times

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `p_port_coarse` | um | given | 250 um | — | pitch of the perimeter zone, a detachable land grid |
| `w_perimeter` | mm | given | 2 mm | — | width of the perimeter zone on each side. Four millimetres was the first sketch and it costs the fine zone a quarter of its area, which halves the pane in 062 -- and the perimeter needs a few hundred conductors against the several thousand positions two millimetres already gives it |
| `n_pair_line` | 1 | given | 64 | — | differential pairs one storage line uses |
| `n_pad_power` | 1 | given | 96 | — | pads carrying the face's supply and its return |
| `n_pad_control` | 1 | given | 32 | — | pads for management, telemetry and the interlock |
| `i_pad_port` | A | given | 0.3 A | — | current one perimeter pad carries |
| `A_port` | mm^2 | derived | 2704 mm^2 | `A_plate` | the whole outward surface |
| `A_perimeter` | mm^2 | derived | 400 mm^2 | `A_plate - (L_plate - 2*w_perimeter)^2` | area of the perimeter zone |
| `A_fine` | mm^2 | derived | 2304 mm^2 | `(L_plate - 2*w_perimeter)^2` | area of the fine zone |
| `n_pad_perim` | 1 | derived | 6400 | `A_perimeter / p_port_coarse^2` | positions in the perimeter zone |
| `n_port_conductor` | 1 | derived | 256 | `2*n_pair_line + n_pad_power + n_pad_control` | conductors that must reach the interposer through 014's via islands |
| `I_port_max` | A | derived | 14.4 A | `n_pad_power / 2 * i_pad_port` | current the power pads will carry, half of them being return |
| `f_fine_unused` | 1 | derived | 0.8333 | `(n_face - 1) / n_face` | share of the fine zones that will never be bonded to anything, which is the price of one part number |
| `n_pop` | 1 | given | 6 | — | populations this field supports |

## 057 — The line to the drives

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_line_pop` | 1 | given | 5 | — | storage lines populated on a cube that also has an output tube |
| `n_drive_line` | 1 | given | 16 | — | drives behind one line |
| `r_drive` | bit/s | measured | 1.28e+11 bit/s | — | bits a second one drive sustains on a long sequential read |
| `r_pair_line` | bit/s | given | 3.2e+10 bit/s | — | bits a second one differential pair carries on this link |
| `L_cable` | mm | given | 1000 mm | — | reach from the port field to the drive shelf |
| `e_line_bit` | pJ/bit | measured | 5 pJ/bit | — | energy to move one bit over the line, both ends included |
| `B_line` | bit/s | derived | 2.048e+12 bit/s | `n_pair_line * r_pair_line` | what one line's pairs carry |
| `B_drives` | bit/s | derived | 2.048e+12 bit/s | `n_drive_line * r_drive` | what the drives behind it supply |
| `B_feed` | bit/s | derived | 1.024e+13 bit/s | `n_line_pop * min(B_line, B_drives)` | aggregate into the machine |
| `t_load` | s | derived | 0.02842 s | `C_weights / B_feed` | time to fill the core |
| `t_load_relay` | s | derived | 0.0341 s | `t_load * n_face / n_line_pop` | and with the sixth slice relayed through the core over one of the five |
| `P_line` | W | derived | 51.2 W | `e_line_bit * B_feed` | what the lines cost while they are running, which is only during a load |
| `n_drive_total` | 1 | derived | 80 | `n_drive_line * n_line_pop` | drives an installation has to provide |
| `t_load_min` | s | derived | 0.4547 s | `C_weights / (n_line_pop * r_drive)` | load time with a single drive on each line, which is the other end of the deployment table |
| `E_burst_max` | J | given | 20 J | — | the most energy a transient may deposit in the face assemblies before it stops being absorbed by their thermal mass and starts being a load the coolant sees |
| `t_load_slow` | s | given | 60 s | — | the longest a load may take at the sparsest sensible deployment, one drive per line. A minute is what somebody will wait once per power cycle |
| `t_load_max` | s | given | 0.1 s | — | the longest filling the core may take. A tenth of a second is what somebody starting a machine will not notice; the design comes in well under it |
| `L_reach_line` | mm | given | 3000 mm | — | reach of the adopted serial standard at this rate, from its own specification |

## 058 — How a model is laid out on the media

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `w_header_med` | bit | given | 3.277e+04 bit | — | the header: model shape, tensor table, format identifiers and version |
| `n_tensor_layer` | 1 | given | 9 | — | tensors in one layer, in walk order |
| `w_hash` | bit | given | 256 bit | — | hash width per slice |
| `p_align_med` | bit | derived | 3.277e+04 bit | `max(w_transfer, w_interleave)` | alignment every tensor start must satisfy, being the larger of a transfer and a bank stride |
| `f_pad_med` | 1 | given | 0.004 | — | share of the file that is padding to that alignment |
| `C_rotation` | MB | derived | 1.049 MB | `n_ctx * d_head * w_act` | the carried rotation table: a cosine and a sine per position per pair |
| `C_media` | GB | derived | 36.52 GB | `C_weights + C_rotation + w_header_med + n_face * w_hash + C_weights * f_pad_med` | the whole file |
| `C_slice_med` | GB | derived | 6.087 GB | `C_media / n_face` | one slice's region |
| `f_sequential` | 1 | derived | 0.996 | `1 - f_pad_med` | share of a slice read that is one contiguous run, which is the whole point of the format |
| `n_seek` | 1 | derived | 6 | `n_face` | seeks in a whole load: one per slice region, and no more |

## 059 — What stays, and what streams

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `ratio_core_media` | 1 | derived | 30 | `B_core / B_feed` | how much faster the core is than the media, which is the height of the cliff |
| `C_resident` | GB | derived | 55.17 GB | `C_weights + C_kv` | what a model needs resident: weights and cache together |
| `f_resident` | 1 | derived | 0.7668 | `C_resident / C_core_usable` | how full the core is at the reference model, context and batch |
| `C_headroom` | GB | derived | 16.78 GB | `C_core_usable - C_resident` | what is left |
| `n_ctx_max` | 1 | derived | 7753 | `n_ctx * (C_core_usable - C_weights) / C_kv` | the longest context that would fit at the reference model and batch, which is the graceful degradation offered instead of streaming |
| `f_stream_penalty` | 1 | derived | 30 | `ratio_core_media` | how much slower a streamed weight is than a resident one |
| `t_token_stream10` | s | derived | 0.003763 s | `t_token * (0.9 + 0.1 * ratio_core_media)` | time per token if a tenth of the model had to stream, which is the curve a reader wants before choosing to allow it |
| `n_slice_policy` | 1 | given | 0 | — | replacement policies in the slice. There are none: the walk order is known in advance, so there is nothing to choose between |

## 060 — Fetching the next layer while computing this one

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `f_stall_prob` | 1 | given | 0.01 | — | share of layers whose prefetch arrives late at the design operating point |
| `n_stall_cycle` | 1 | given | 2000 | — | cycles the engine waits when one does |
| `t_transfer_layer` | s | derived | 6.894e-05 s | `C_layer_weights / B_face_even` | time to move one layer's weights at a face's contended share |
| `t_lead` | s | derived | 6.894e-05 s | `t_link_rt + t_transfer_layer` | how far ahead the prefetch must start |
| `t_lead_unc` | s | derived | 1.15e-05 s | `t_link_rt + C_layer_weights / B_core` | and what it would be uncontended, which is the number that would have been used had nobody thought about the other five faces |
| `f_lead_layer` | 1 | derived | 0.866 | `t_lead / t_layer` | the lead as a share of the arithmetic it has to hide behind |
| `t_stall_token` | s | derived | 2e-07 s | `f_stall_prob * n_layer_face * n_stall_cycle / f_face` | stall time per token per face |
| `f_stall_token` | 1 | derived | 0.0002073 | `t_stall_token / t_token` | that as a share of a token |
| `B_concurrent` | bit/s | derived | 1.266e+14 bit/s | `B_operand_die * n_die_face + B_face_even` | what the slice must serve and absorb at the same time: the engine reading one buffer while the link fills the other |

## 061 — Does a face ever starve

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `B_eat_face` | bit/s | derived | 7.537e+13 bit/s | `B_operand_die * n_die_face` | what one face's four engines consume at full utilisation |
| `m_slice_feed` | 1 | derived | 48.7 | `B_slice_read / B_eat_face` | the slice's margin over that |
| `m_core_feed` | 1 | derived | 1.155 | `B_face_even / (C_layer_weights / t_layer)` | the core's margin over what a face must pull per layer at its contended share |
| `B_scrub_face` | bit/s | derived | 2.948e+07 bit/s | `B_scrub / n_face` | the scrubber's share falling on one face's traffic |
| `m_scrub` | 1 | derived | 1.155 | `(B_face_even - B_scrub_face) / (C_layer_weights / t_layer)` | the core's margin with the scrubber running, which is the check 040 needs and cannot do for itself |
| `f_starve_below` | 1 | derived | 0.3207 | `1 - (B_face_even / B_eat_face)` | how starved an engine is below the crossover, which is the intended state and is reported rather than constrained |
| `t_token_feed` | s | derived | 0.0009648 s | `(C_weights + B_kv_seq) / B_core` | time per token from this chain, which 055 and 080 must agree with |

## 062 — The idea, and what it costs

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `p_bond_fine` | um | given | 10 um | — | the finest pitch a permanent copper-to-copper bond reaches |
| `f_pane_overhead` | 1 | given | 0.2 | — | share of the fine zone's positions spent on power, ground, shielding and strobes |
| `e_pane_bit` | pJ/bit | measured | 0.01 pJ/bit | — | energy to move one bit across a ten micron bond, driver and receiver together |
| `f_spout_burst` | GHz | given | 1 GHz | — | rate a pane transfer runs at during a burst |
| `f_spout_sust` | MHz | given | 50 MHz | — | rate it may run at continuously without exceeding the steady port allocation. A hundred megahertz was the first figure and costs seventeen watts against a ten watt steady budget -- which is the difference between a burst the silicon absorbs and a load the coolant sees |
| `n_fine_col` | 1 | derived | 4800 | `floor(sqrt(A_fine) / p_bond_fine)` | pad columns across the fine zone |
| `n_fine_pad` | 1 | derived | 2.304e+07 | `n_fine_col^2` | positions in it |
| `n_pane_avail` | 1 | derived | 1.843e+07 | `n_fine_pad * (1 - f_pane_overhead)` | conductors available for data |
| `n_pane_bit` | bit | derived | 1.678e+07 bit | `b1 * 2^floor(log(n_pane_avail) / log(2))` | the pane, rounded down to a power of two so that its window aligns naturally in 038. It carries the unit because it is a quantity of bits rather than a count of things, and everything downstream of it is a size or a rate |
| `C_pane_mb` | MB | derived | 2.097 MB | `n_pane_bit` | the pane, in the unit a person reads it in |
| `E_pane` | J | derived | 1.678e-07 J | `e_pane_bit * n_pane_bit` | energy of one pane transfer |
| `n_pane_core` | 1 | derived | 3.431e+04 | `C_core_usable / n_pane_bit` | panes needed to move the whole core |
| `t_core_out` | s | derived | 3.431e-05 s | `n_pane_core / f_spout_burst` | time to do it at the burst rate |
| `P_spout_burst` | W | derived | 167.8 W | `E_pane * f_spout_burst` | power while a burst runs |
| `B_spout_burst` | bit/s | derived | 1.678e+16 bit/s | `n_pane_bit * f_spout_burst` | the burst rate as a bandwidth |
| `B_spout_sust` | bit/s | derived | 8.389e+14 bit/s | `n_pane_bit * f_spout_sust` | and the sustained one |
| `t_pane_fill` | s | derived | 5.461e-08 s | `n_pane_bit / B_core` | how long the core takes to fill a pane |
| `t_pane_empty` | s | derived | 1e-09 s | `1 / f_spout_burst` | how long the spout takes to empty it |
| `ratio_fill` | 1 | derived | 54.61 | `t_pane_fill / t_pane_empty` | how much slower the memory is than the tube, which is the limit that is not the wires |
| `ratio_net` | 1 | derived | 4.194e+04 | `t_core_net / t_core_out` | how much faster this is than sending the same bytes over a network interface |
| `t_core_net` | s | derived | 1.439 s | `C_core_usable / r_net_ref` | time to move the whole core over a fast network interface, for the comparison |
| `r_net_ref` | bit/s | measured | 4e+11 bit/s | — | a fast network interface, as the comparison everybody will make |

## 063 — Sixteen million pads on a square

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `f_pane_spare` | 1 | given | 0.03 | — | share of the fine zone's positions held as spares against bonds that fail |
| `w_tile_pad` | 1 | given | 64 | — | pads across one source-synchronous tile |
| `f_gnd_ratio` | 1 | given | 0.2 | — | share of positions given to power, ground, shielding and strobes, from 064's switching analysis |
| `n_pane_tile` | 1 | derived | 4096 | `n_pane_bit / b1 / w_tile_pad^2` | tiles the pane is divided into |
| `L_tile_pad` | um | derived | 640 um | `w_tile_pad * p_bond_fine` | edge of one tile |
| `n_strobe` | 1 | derived | 8192 | `n_pane_tile * 2` | strobe conductors: one pair per tile |
| `n_spare_pane` | 1 | derived | 6.912e+05 | `n_fine_pad * f_pane_spare` | conductors held in reserve |
| `n_accounted` | 1 | derived | 2.208e+07 | `n_pane_bit / b1 + n_strobe + n_spare_pane + n_fine_pad * f_gnd_ratio` | every position, by purpose |
| `A_spout_need` | mm^2 | derived | 2304 mm^2 | `n_fine_pad * p_bond_fine^2` | fine zone area the widest grade needs, which 056 must provide |
| `n_bond_total` | 1 | derived | 2.304e+07 | `n_fine_pad` | bonds made in one operation at the very end of assembly, which 083 has to survive |

## 064 — One wire, and the circuit at each end

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `a_driver` | um^2 | measured | 14 um^2 | — | area of one driver: an inverter sized for a femtofarad |
| `a_receiver` | um^2 | measured | 16 um^2 | — | area of one receiver: an inverter and a small latch |
| `c_bond` | fF | measured | 1 fF | — | capacitance one bonded conductor presents |
| `f_circuit_over` | 1 | measured | 13.89 | — | how much more a bit costs than moving the bond's own charge. The bond is a fourteenth of it; the rest is the two inverters switching, the local strobe distribution and the receiver's latch |
| `L_bond` | um | given | 10 um | — | length of the bond itself |
| `L_reach_bond` | um | given | 50 um | — | the furthest this circuit is valid over, beyond which a channel exists and 067 applies |
| `v_bounce_max` | V | given | 0.06 V | — | supply bounce the receiver still resolves a bit through |
| `n_stagger` | 1 | given | 16 | — | groups the tiles are fired in, one after another |
| `t_stagger` | ps | given | 20 ps | — | interval between two stagger groups |
| `f_cond_gain` | 1 | given | 0.5 | — | share of the worst-case switching that data conditioning removes |
| `a_conductor` | um^2 | derived | 100 um^2 | `p_bond_fine^2` | area one conductor has, pad and circuit together |
| `a_circuit` | um^2 | derived | 30 um^2 | `a_driver + a_receiver` | area the circuit needs |
| `f_circuit_area` | 1 | derived | 0.3 | `a_circuit / a_conductor` | how much of the pitch the circuit uses |
| `E_bit_derived` | pJ/bit | derived | 0.01 pJ/bit | `c_bond * V_port^2 / 2 / b1 * f_circuit_over` | energy to move one bit: the bond's own charge, times what the circuits at each end add |
| `I_ssn_raw` | A | derived | 20.13 A | `n_pane_bit / b1 * c_bond * V_port * f_spout_burst` | current if every conductor switched the same way on the same edge |
| `I_ssn_mit` | A | derived | 0.6291 A | `I_ssn_raw * f_cond_gain / n_stagger` | and after conditioning and stagger |
| `v_bounce` | V | derived | 1.258e-06 V | `I_ssn_mit * z_supply` | the bounce that produces |
| `z_supply` | ohm | given | 2e-06 ohm | — | impedance the local supply presents to the array at the switching frequency |
| `f_gnd_min` | 1 | derived | 0.15 | `0.15` | the least share of positions that must be ground and shielding for the bounce above to be what it is |
| `t_stagger_all` | ps | derived | 320 ps | `n_stagger * t_stagger` | how long the whole stagger sequence takes, which 065 has to absorb |

## 065 — Getting sixteen million edges to agree

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `v_prop_die` | 1 | given | 0.33 | — | how fast a signal crosses a die, as a fraction of the speed of light |
| `t_skew_intra` | ps | given | 8 ps | — | skew budget within one tile |
| `t_skew_mismatch` | ps | given | 40 ps | — | distribution network mismatch between tiles, from process and supply variation |
| `t_skew_window` | ps | given | 500 ps | — | the arrival window the receiver must tolerate, stated as a contract the far end may rely on |
| `t_cross_die` | s | derived | 5.256e-10 s | `L_plate / (c_light * v_prop_die)` | time for a signal to cross a face, which is the naive single-edge skew |
| `f_cycle_naive` | 1 | derived | 0.5256 | `t_cross_die * f_spout_burst` | that as a fraction of a cycle at the burst rate, which is the number that kills the single-edge approach |
| `L_tile_skew` | um | derived | 791.5 um | `t_skew_intra * c_light * v_prop_die` | the largest tile whose corner-to-corner skew fits the intra-tile budget |
| `t_window_used` | ps | derived | 360 ps | `t_skew_mismatch + t_stagger_all` | the window actually consumed: distribution mismatch plus 064's stagger |
| `n_strobe_cost` | 1 | derived | 0.0004883 | `n_strobe / (n_pane_bit / b1)` | what the per-tile strobes cost, as a share of the conductors |
| `d_buffer_tile` | 1 | given | 4 | — | pane transfers of buffering the receiver holds per tile, to absorb the window |
| `C_rx_buffer` | MB | derived | 8.389 MB | `n_pane_bit * d_buffer_tile` | receiver buffering in total, which lands in 069a |

## 066 — The bonded grade

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `T_bond` | K | given | 483 K | — | temperature the copper-to-copper bond is made at. Five hundred and twenty-three was the first figure and is hotter than the bond that put the faces on, which would reflow it -- surface activation before bonding is what brings this down, and it is a process requirement rather than a preference |
| `F_bond` | kg | given | 2000 kg | — | force applied across the array during bonding |
| `t_bond_dwell` | s | given | 1800 s | — | how long it is held |
| `tol_align_xy` | um | given | 0.5 um | — | in-plane placement tolerance the pitch demands |
| `tol_flat_bond` | um | given | 0.2 um | — | flatness both surfaces must hold across the array |
| `n_rework` | 1 | given | 0 | — | rework attempts available. There are none: this is the last operation and both objects are finished |
| `y_array_raw` | 1 | derived | 0.316 | `y_bond_hybrid^n_bond_total` | probability every bond in the array is good with no spares at all |
| `n_bad_expect` | 1 | derived | 1.152 | `n_bond_total * (1 - y_bond_hybrid)` | bonds expected to fail in one array |
| `y_array_spare` | 1 | derived | 1 | `1 - n_bad_expect / n_spare_pane` | roughly, the chance the spares cover the failures -- a crude bound rather than a distribution, and marked as such |
| `p_area_bond` | MPa | derived | 8.516 MPa | `F_bond * g_accel / A_fine` | pressure the bonding force puts on the array |
| `ratio_align` | 1 | derived | 250 | `flat_plate / tol_flat_bond` | how much looser 013's face flatness is than this bond needs, which is the number that says where the difficulty is |
| `t_core_out_b` | s | derived | 3.431e-05 s | `t_core_out` | whole-core transfer time for this grade, which is 062's burst figure unmodified |
| `T_bond_max_prior` | K | given | 543 K | — | the lowest temperature any earlier bond, solder or seal in 082's sequence survives |
| `p_area_max` | MPa | given | 15 MPa | — | pressure the assembled stack will take across its face without damage |

## 067 — The cabled grade

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_pair_spout` | 1 | given | 512 | — | differential pairs the cabled grade uses. Two thousand was the first figure and draws two hundred and sixty watts, which is three times even the transient port allowance -- a serial link costs picojoules a bit where a ten micron bond costs femtojoules, and that is the real reason this grade is narrow |
| `r_pair_spout` | bit/s | given | 3.2e+10 bit/s | — | rate one pair carries |
| `n_mate_cycle` | 1 | given | 200 | — | mating cycles the connector is rated for |
| `L_cable_spout` | mm | given | 1000 mm | — | reach |
| `e_ser_bit` | pJ/bit | measured | 4 pJ/bit | — | energy a bit costs over this link, both ends and the serialiser included |
| `B_cabled` | bit/s | derived | 1.638e+13 bit/s | `n_pair_spout * r_pair_spout` | what the cabled grade carries |
| `ratio_bonded` | 1 | derived | 1024 | `B_spout_burst / B_cabled` | how much narrower it is than the bonded grade |
| `ratio_net_c` | 1 | derived | 40.96 | `B_cabled / r_net_ref` | and how much wider than a network interface |
| `t_pane_ser` | s | derived | 1.024e-06 s | `n_pane_bit / B_cabled` | how long one pane takes to serialise, which is what makes it not the same thing arriving |
| `t_core_out_c` | s | derived | 0.03513 s | `C_core_usable / B_cabled` | whole-core transfer time on this grade |
| `P_cabled` | W | derived | 65.54 W | `e_ser_bit * B_cabled` | what it costs while running |
| `n_pad_spout_c` | 1 | derived | 1024 | `n_pair_spout * 2` | perimeter positions it needs |

## 068 — Byte mode

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_phase_byte` | 1 | given | 8 | — | bits sharing one conductor, and therefore pulses per pane |
| `p_bump_byte` | um | given | 32 um | — | microbump pitch this grade uses. Thirty is a quarter of a micron finer than the conductor count actually permits in the fine zone available |
| `a_mux_lane` | um^2 | measured | 260 um^2 | — | area of one eight-way multiplexer and its phase logic |
| `n_rework_byte` | 1 | given | 2 | — | rework attempts a microbump attach permits |
| `n_cond_byte` | 1 | derived | 2.097e+06 | `n_pane_bit / b1 / n_phase_byte` | conductors this grade needs |
| `p_byte_need` | um | derived | 30.26 um | `sqrt(A_fine / (n_cond_byte * (1 + f_gnd_ratio)))` | the pitch that many conductors actually require in the fine zone |
| `a_cond_byte` | um^2 | derived | 1024 um^2 | `p_bump_byte^2` | area one conductor has at the chosen pitch |
| `f_mux_area` | 1 | derived | 0.2539 | `a_mux_lane / a_cond_byte` | how much of it the multiplexer uses |
| `t_pane_byte` | s | derived | 8e-09 s | `n_phase_byte / f_spout_burst` | time for one pane in this grade |
| `t_core_out_y` | s | derived | 0.0002744 s | `n_pane_core * t_pane_byte` | whole-core transfer time |
| `t_skew_phase` | ps | derived | 1 ps | `t_skew_intra / n_phase_byte` | skew budget within one pulse, which is the hidden cost |
| `ratio_net_y` | 1 | derived | 5243 | `t_core_net / t_core_out_y` | how much faster than a network interface this grade still is |
| `E_pane_byte` | J | derived | 1.678e-07 J | `E_pane` | energy per pane, unchanged: the same bits cross the same bonds, just not at the same moment |
| `t_skew_min` | ps | given | 0.8 ps | — | the least skew any distribution can be held to at this scale, from the process |

## 069 — Knowing the pane arrived whole

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `w_hash_pane` | bit | given | 128 bit | — | hash width over one pane |
| `n_retry_max` | 1 | given | 3 | — | retries before a pane is declared permanently bad |
| `n_consec_fail` | 1 | given | 3 | — | consecutive failures in the same tile that mean a broken conductor rather than bad luck |
| `w_seq_pane` | bit | given | 32 bit | — | sequence number width |
| `p_bit_transient` | 1 | measured | 1e-18 | — | chance one bit is wrong in flight across a bond, per transfer |
| `w_win_reg` | bit | given | 64 bit | — | width of the pane window's aliasing register, which travels with every pane so the far end knows what it received |
| `w_oob` | bit | derived | 4320 bit | `w_hash_pane + w_seq_pane + w_win_reg + n_pane_tile * b1` | out-of-band state per pane: hash, sequence, the window register, and one inversion bit per tile |
| `p_pane_bad` | 1 | derived | 1.678e-11 | `n_pane_bit / b1 * p_bit_transient` | chance a pane carries at least one wrong bit |
| `p_undetected` | 1 | derived | 4.93e-50 | `p_pane_bad / 2^(w_hash_pane / b1)` | chance a wrong pane passes its hash |
| `rate_pane` | 1/s | derived | 5e+07 1/s | `f_spout_sust` | panes a second, sustained |
| `rate_undet` | 1/s | derived | 2.465e-42 1/s | `p_undetected * rate_pane` | undetected errors a second at that rate |
| `t_undet_mean` | s | derived | 4.056e+41 s | `1 / rate_undet` | mean time between them |
| `f_retry_cost` | 1 | derived | 5.033e-11 | `p_pane_bad * n_retry_max` | share of pane transfers spent retrying |
| `n_seq_wrap` | 1 | derived | 4.295e+09 | `2^(w_seq_pane / b1)` | panes before the sequence number wraps |

## 069a — The thing on the other end

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_variant` | 1 | given | 4 | — | far sides specified: a memory fabric, a peripheral bus, a network interface, and another translation unit |
| `d_rx_pane` | 1 | given | 4 | — | panes of buffering the unit holds |
| `r_host_ref` | bit/s | measured | 4.8e+11 bit/s | — | a fast host interface, as the reference far side |
| `t_host_stall` | s | given | 1e-05 s | — | the longest a host may stop accepting before the unit must be able to hold what has arrived |
| `a_rx_lane` | um^2 | measured | 40 um^2 | — | receive circuit area per conductor: capture, deskew and a share of the reorder logic. It cannot be larger than the sending side's, because the far end meets the same pitch -- a hundred and forty was assumed before that was noticed, and made the unit a multi-die assembly rather than the cheap part |
| `C_rx_buf` | MB | derived | 8.389 MB | `C_pane_mb * d_rx_pane` | buffering the unit needs |
| `C_rx_stall` | MB | derived | 0.6 MB | `r_host_ref * t_host_stall` | what a host stall alone demands, which must be less |
| `ratio_funnel` | 1 | derived | 3.495e+04 | `B_spout_burst / r_host_ref` | how much wider the cube's side is than the host's |
| `t_host_core` | s | derived | 1.199 s | `C_core_usable / r_host_ref` | how long the host takes for a whole core, against the cube's tens of microseconds |
| `f_cube_busy` | 1 | derived | 2.861e-05 | `t_core_out / t_host_core` | the share of that time the cube is occupied, which is the zero-cost claim as a number |
| `A_rx` | mm^2 | derived | 671.1 mm^2 | `n_pane_bit / b1 * a_rx_lane` | die area the receive side needs, which is what makes this a die rather than a board |
| `P_rx` | W | derived | 8.389 W | `e_pane_bit * B_spout_sust` | what receiving costs at the sustained rate |

## 069b — The cube as memory

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `t_win_move` | ns | given | 200 ns | — | to move the pane window: a control write over the port field and its acknowledgement |
| `t_rx_pipe` | ns | given | 40 ns | — | deskew, reorder and buffer at the far end |
| `n_host_req` | 1/s | given | 1e+05 1/s | — | host requests a second in ordinary use of this mode |
| `f_host_region` | 1 | given | 0.49 | — | share of usable capacity exposed to a host: the weight region, which is immutable while a model is loaded |
| `t_mem_read` | ns | derived | 295.6 ns | `t_win_move + t_pane_fill + t_pane_empty + t_rx_pipe` | latency for one host read, end to end |
| `C_host_vis` | GB | derived | 35.25 GB | `C_core_usable * f_host_region` | capacity a host can see |
| `B_host_seq` | bit/s | derived | 5.675e+13 bit/s | `n_pane_bit / t_mem_read` | sustained rate on sequential reads, one pane at a time |
| `t_cube_per_req` | s | derived | 5.461e-08 s | `t_pane_fill` | core time one host request costs |
| `f_cube_cost` | 1 | derived | 0.005461 | `n_host_req * t_cube_per_req` | share of the core's time spent serving a host at the stated request rate |
| `t_load_host` | s | derived | 0.005128 s | `C_weights / B_host_seq` | how long loading a model through this path takes, against 057's thirty milliseconds from drives |
| `ratio_dram` | 1 | derived | 3.695 | `t_mem_read / t_dram_ref` | how much slower than a host's own memory, which is the comparison that decides what this is for |
| `t_dram_ref` | ns | measured | 80 ns | — | latency of a host's own attached memory, as the comparison |

## 070 — Where the beat comes from

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `f_ref` | MHz | given | 100 MHz | — | the external crystal reference |
| `jit_ref` | ps | given | 0.5 ps | — | cycle-to-cycle jitter of the reference |
| `jit_mult` | ps | given | 3 ps | — | jitter one multiplier adds |
| `psrr_mult` | 1 | given | 0.02 | — | fraction of a supply excursion that appears as a timing shift at the multiplier's output |
| `t_lock_mult` | s | given | 0.0003 s | — | how long a multiplier takes to settle from cold |
| `n_ref` | 1 | given | 1 | — | references in the machine |
| `P_clockgen` | W | given | 4 W | — | all the multipliers together |
| `ratio_face` | 1 | derived | 14 | `f_face / f_ref` | multiplier ratio for the face domain |
| `ratio_core` | 1 | derived | 12.5 | `f_core / f_ref` | and for the core |
| `jit_total` | ps | derived | 3.041 ps | `sqrt(jit_ref^2 + jit_mult^2)` | jitter at a multiplier's output, the two sources added in quadrature since they are independent |
| `t_cycle_face` | ps | derived | 714.3 ps | `1 / f_face` | the face cycle time everything in 074 is budgeted against |
| `jit_supply` | ps | derived | 0.4286 ps | `psrr_mult * dV_droop_logic / V_logic * t_cycle_face` | timing shift caused by the supply drooping under a load step, which arrives at the worst possible moment |
| `jit_budget` | ps | derived | 3.47 ps | `jit_total + jit_supply` | what 074 must allow for altogether |
| `f_merge_gap` | 1 | derived | 1.12 | `f_face / f_core` | how much faster the faces run than the core, which is the question of whether the two domains can be one |

## 071 — Getting the beat everywhere

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_tree_level` | 1 | given | 3 | — | levels of distribution: within a die, across a face, between faces |
| `skew_die` | ps | given | 12 ps | — | skew within one die's balanced tree, which is where the tight number is |
| `skew_face` | ps | given | 6 ps | — | skew across the four dies of a face |
| `skew_proc` | ps | given | 20 ps | — | skew between two faces from process variation alone |
| `skew_temp_k` | ps/K | given | 0.4 ps/K | — | timing shift per kelvin of steady temperature difference between two faces |
| `dT_face_diff` | K | given | 12 K | — | steady temperature difference between a busy face and an idle one |
| `P_clocktree` | W | given | 22 W | — | all three levels together |
| `mesochronous` | 1 | given | 1 | — | whether faces share a frequency but not a phase. They do, and it is written as a value so that a blueprint assuming a common edge fails outright |
| `skew_temp` | ps | derived | 4.8 ps | `skew_temp_k * dT_face_diff` | the temperature term between two faces |
| `skew_supply` | ps | derived | 0.4286 ps | `psrr_mult * dV_droop_logic / V_logic * t_cycle_face` | the supply term, each face having its own regulator |
| `skew_face_all` | ps | derived | 20.57 ps | `sqrt(skew_proc^2 + skew_temp^2 + skew_supply^2)` | skew between two faces, the three independent sources in quadrature |
| `skew_intra` | ps | derived | 18 ps | `skew_die + skew_face` | skew a face's own timing closure must absorb, which is what 074 budgets |
| `L_arm` | mm | derived | 7 mm | `t_face` | length of one arm of the inter-face star; equal for all six by construction |
| `f_skew_cycle` | 1 | derived | 0.0252 | `skew_intra / t_cycle_face` | the intra-face skew as a share of a cycle |

## 072 — Six faces agreeing what cycle it is

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_sync_stage` | 1 | given | 3 | — | flip-flop stages in a synchroniser |
| `t_setup_ff` | ps | given | 15 ps | — | setup time of one |
| `tau_meta` | ps | given | 8 ps | — | metastability resolution time constant of the process |
| `t_meta_0` | s | measured | 1e-12 s | — | the metastability window: how wide a slice of each clock edge can put a flip-flop into an undecided state |
| `n_cross_site` | 1 | given | 3 | — | places two faces interact through memory, excluding the arbiter which is inside one domain. Three today; the reverse staging buffers make it four the day 076a is implemented, and 039's count moves with it |
| `w_timebase` | bit | given | 52 bit | — | width of the free-running timebase counter. Forty-eight wraps in under nine years, which is inside the life the machine is built for -- and two counters compared across faces after a wrap can disagree about which came first |
| `f_timebase` | MHz | given | 1 MHz | — | its rate: microsecond resolution, deliberately far below any clock so it cannot be mistaken for one |
| `t_resolve` | ps | derived | 2128 ps | `n_sync_stage * t_cycle_face - t_setup_ff` | time a synchroniser gives metastability to resolve |
| `mtbf_sync` | s | derived | 1.869e+109 s | `exp(t_resolve / tau_meta) / (f_face * f_core * t_meta_0)` | mean time between synchroniser failures at one crossing: the two clocks' rates and the window between them, against the time the synchroniser gives metastability to resolve |
| `mtbf_all` | s | derived | 1.557e+108 s | `mtbf_sync / (n_face * 2)` | and across all six links in both directions |
| `t_cross_face` | ps | derived | 2143 ps | `n_sync_stage * t_cycle_face` | latency a domain crossing adds, which lands in 053's stage budget |
| `t_timebase_wrap` | s | derived | 4.504e+09 s | `2^(w_timebase / b1) / f_timebase` | how long the shared timebase runs before wrapping |
| `res_timebase` | s | derived | 1e-06 s | `1 / f_timebase` | its resolution |
| `f_cross_stage` | 1 | derived | 1.333e-05 | `t_cross_face / t_stage` | the crossing's cost as a share of a pipeline stage |

## 073 — From cold to the first instruction

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_boot_step` | 1 | given | 10 | — | steps between supplies valid and ready |
| `t_reset_hold` | s | given | 1e-06 s | — | how long reset is asserted after the supplies are valid |
| `t_selftest` | s | given | 0.005 s | — | the reduced self test at step seven |
| `t_chain_build` | s | given | 0.002 s | — | building every descriptor chain, once, from the header |
| `t_link_train` | s | given | 0.001 s | — | deskewing every tile and remapping failed conductors |
| `t_repair` | s | given | 0.0001 s | — | mapping in spare rows and the redundant tier |
| `warm_keeps_core` | 1 | given | 1 | — | whether a warm reset preserves the core. It does, and it is a value so that a change which stopped it failing here rather than in the field |
| `t_mem_init` | s | derived | 0.002073 s | `C_core_raw / B_core` | writing every location once at the core's own bandwidth, which must happen before anything reads one |
| `t_boot_cold` | s | derived | 0.04978 s | `t_powerup + t_reset_hold + t_mem_init + t_repair + t_link_train + t_selftest + t_load_relay + t_chain_build` | supplies valid to ready, from cold |
| `t_boot_warm` | s | derived | 0.007001 s | `t_reset_hold + t_selftest + t_chain_build` | and from a warm reset, which keeps the core and therefore skips initialising and loading it |
| `ratio_warm` | 1 | derived | 7.11 | `t_boot_cold / t_boot_warm` | how much a warm reset saves, which is the argument for having two |
| `f_step_load` | 1 | derived | 0.6851 | `t_load_relay / t_boot_cold` | the model load's share of a cold boot |
| `f_step_mem` | 1 | derived | 0.04164 | `t_mem_init / t_boot_cold` | and memory initialisation's, which is the step nobody expects to be there at all |
| `t_boot_max` | s | given | 0.5 s | — | the longest a cold boot may take. Half a second is what somebody switching a machine on will tolerate before wondering whether it is broken |

## 074 — Does it close

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `t_logic_mac` | ps | given | 470 ps | — | logic delay through one multiply-accumulate cell at the slow corner |
| `t_wire_mac` | ps | given | 55 ps | — | wire delay on the same path |
| `t_setup` | ps | given | 25 ps | — | setup time at the capturing flip-flop |
| `t_hold` | ps | given | 18 ps | — | hold time at the same |
| `t_ocv` | ps | given | 45 ps | — | on-chip variation allowance across the path |
| `t_logic_min` | ps | given | 80 ps | — | shortest logic delay on any path, which is what a hold failure is measured against |
| `k_temp_delay` | 1/K | given | 0.0018 1/K | — | fractional increase in delay per kelvin near the operating point |
| `k_freq_power` | 1 | given | 3 | — | how power scales with frequency near the top of the range |
| `t_path_face` | ps | derived | 616.5 ps | `t_logic_mac + t_wire_mac + t_setup + skew_intra + jit_budget + t_ocv` | everything the face's critical path has to fit inside a cycle |
| `t_margin_face` | ps | derived | 97.82 ps | `t_cycle_face - t_path_face` | what is left |
| `f_margin_face` | 1 | derived | 0.1369 | `t_margin_face / t_cycle_face` | that as a share of a cycle |
| `t_path_hold` | ps | derived | 58.53 ps | `t_logic_min - skew_intra - jit_budget` | the shortest path, less what could make it shorter still |
| `t_path_core` | ps | derived | 790.5 ps | `t_access + t_setup + skew_die + jit_budget` | the core array's path |
| `t_cycle_core_p` | ps | derived | 800 ps | `1 / f_core` | and its cycle |
| `t_margin_core` | ps | derived | 9.53 ps | `t_cycle_core_p - t_path_core` | what is left there |
| `t_path_at_face` | ps | derived | 790.5 ps | `t_path_core * 1` | the core's path, for the question of whether it could run at the face clock |
| `f_boost_max` | 1 | derived | 1.034 | `(1 + margin_thermal * k_temp_delay)^(1 / k_freq_power)` | how much faster the faces could run if the whole thermal margin were spent on clock rather than on removing a chiller |
| `f_face_boost` | GHz | derived | 1.448 GHz | `f_face * f_boost_max` | and what frequency that would be |

## 075 — Cutting the model into six

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_layer_end` | 1 | given | 12 | — | transformer layers on face zero and on face five, which also carry the embedding and the projection |
| `n_layer_mid` | 1 | given | 14 | — | layers on each of the four faces between them |
| `n_layer_face` | 1 | derived | 14 | `n_layer_mid` | layers on the busiest face by count, which 048 sizes its chains against |
| `n_layer_check` | 1 | derived | 80 | `2 * n_layer_end + 4 * n_layer_mid` | layers assigned altogether, which must be the model's |
| `C_face_end` | MB | derived | 5835 MB | `C_layer_max + n_layer_end * C_layer_weights` | bytes face zero or face five reads per token |
| `C_face_mid` | MB | derived | 6177 MB | `n_layer_mid * C_layer_weights` | and one of the middle four |
| `C_face_mean` | MB | derived | 6063 MB | `C_weights / n_face` | what an evenly cut face would read |
| `C_face_worst` | MB | derived | 6177 MB | `max(C_face_end, C_face_mid)` | the slowest stage, which sets the rate |
| `f_imbalance` | 1 | derived | 0.01879 | `(C_face_worst - C_face_mean) / C_face_mean` | how far the slowest stage is above the mean, which is what 053 tolerates |
| `f_imbalance_c` | 1 | derived | 0.05 | `(max(n_layer_end, n_layer_mid) - n_layer / n_face) / (n_layer / n_face)` | what the imbalance would have been had the cut been made by layer count instead, which is the comparison that justifies balancing by bytes |

## 076 — The dataflow of one pass

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `C_qkv` | MB | derived | 43.25 MB | `d_model * (n_head + 2 * n_kv_head) * d_head * w_weight_eff` | one layer's query, key and value projections |
| `C_proj` | MB | derived | 34.6 MB | `d_model * n_head * d_head * w_weight_eff` | its projection back |
| `C_ffn` | MB | derived | 363.3 MB | `3 * d_model * d_ff * w_weight_eff` | its feedforward |
| `C_inter_layer` | MB | derived | 0.1065 MB | `(d_ff + 3 * d_model) * w_act` | intermediates inside one layer that never leave the die |
| `C_kv_token` | MB | derived | 0.002048 MB | `2 * n_kv_head * d_head * w_kv` | cache one layer writes for one token |
| `C_kv_read` | MB | derived | 8.389 MB | `C_kv_token * n_ctx` | and reads back, at full context, per layer per token |
| `B_weight_tok` | MB | derived | 3.638e+04 MB | `C_weights` | weight traffic per token: every weight once |
| `B_kv_seq` | MB | derived | 671.1 MB | `C_kv_read * n_layer` | cache traffic for one token of one sequence, at full context |
| `B_kv_tok` | MB | derived | 1.879e+04 MB | `B_kv_seq * batch_design` | and across the whole batch, which is a different number and is the one 079 uses |
| `B_act_tok` | MB | derived | 2.753 MB | `C_activation * n_stage * batch_design` | activations crossing between stages |
| `f_kv_traffic` | 1 | derived | 0.5166 | `B_kv_tok / B_weight_tok` | the cache's traffic against the weights', which is the ratio that changes character with context |
| `n_ctx_cross` | 1 | derived | 7929 | `n_ctx * B_weight_tok / B_kv_tok` | the context length at which the two are equal |
| `f_act_traffic` | 1 | derived | 7.567e-05 | `B_act_tok / B_weight_tok` | the handoff's share, which 050 asserts is negligible and this is where the number comes from |
| `C_handoff` | MB | derived | 0.07646 MB | `C_activation * batch_design / n_microbatch` | one staging buffer's worth: a microbatch of activation vectors |

## 076a — The sieve, run backwards

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `r_adapter` | 1 | given | 16 | — | rank of the low-rank adapter |
| `w_master` | bit | given | 32 bit | — | width of a master weight during training |
| `n_moment` | 1 | given | 2 | — | optimiser moments kept per trained parameter |
| `f_recompute` | 1 | given | 1.33 | — | how much more arithmetic recomputation costs than saving everything |
| `n_micro_train` | 1 | given | 12 | — | microbatches in flight when training, twice generation's |
| `p_adapter` | 1 | derived | 9.437e+07 | `n_layer * 2 * r_adapter * (d_model + d_ff)` | parameters in a rank-limited adapter across every layer |
| `C_adapter_st` | GB | derived | 1.51 GB | `p_adapter * w_master * (2 + n_moment)` | its master weights, gradients and moments |
| `C_ckpt_bound` | GB | derived | 5.369 GB | `n_layer * n_ctx * d_model * w_act * batch_train` | activations at layer boundaries only, which is what recomputation keeps |
| `batch_train` | 1 | given | 1 | — | sequences trained at once; one, because the checkpoints scale with it and the core does not |
| `C_full_train` | GB | derived | 1129 GB | `n_param * w_master * (2 + n_moment)` | what training every parameter would need, which is the number that does not fit |
| `C_train_res` | GB | derived | 43.26 GB | `C_weights + C_adapter_st + C_ckpt_bound` | resident during adapter training |
| `ratio_full` | 1 | derived | 15.69 | `C_full_train / C_core_usable` | how far out of reach full training is |
| `flop_train` | flop | derived | 5.164e+11 flop | `flop_token * (1 + 2 * f_recompute)` | arithmetic per token when training: the forward pass, the backward pass, and the recomputation |
| `B_grad_stage` | MB | derived | 0.01638 MB | `C_activation * batch_train` | what crosses between two stages on the way back |

## 077 — What four bits costs

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_lloyd_iter` | 1 | given | 8 | — | iterations of the fitting rule per group at packing time |
| `sigma_rel_4b` | 1 | measured | 0.021 | — | relative error one weight carries at four bits with a fitted table, root mean square over a group |
| `sigma_rel_8b` | 1 | measured | 0.0027 | — | the same at eight bits, for the comparison |
| `f_norm_supp` | 1 | measured | 0.55 | — | how much of a layer's accumulated error the normalisation at its output suppresses |
| `w_scale_8` | bit | given | 8 bit | — | the alternative scale width, for open question F1 |
| `sigma_scale_8` | 1 | measured | 0.0009 | — | extra relative error an eight-bit group scale introduces |
| `err_layer` | 1 | derived | 0.000124 | `sigma_rel_4b / sqrt(n_reduce_max)` | relative error at a layer's output: independent weight errors averaging down over the reduction |
| `err_pass` | 1 | derived | 0.0006101 | `err_layer * sqrt(n_layer) * f_norm_supp` | accumulated through the whole pass, errors adding in quadrature and each normalisation suppressing part |
| `err_ratio_8b` | 1 | derived | 7.778 | `sigma_rel_4b / sigma_rel_8b` | how much worse four bits is than eight, which is the accuracy side of 046's trade |
| `speed_gain_4b` | 1 | derived | 1.97 | `speed_ratio` | and the speed side, from 046 |
| `err_scale_8` | 1 | derived | 1.001 | `sqrt(sigma_rel_4b^2 + sigma_scale_8^2) / sigma_rel_4b` | what an eight-bit group scale would cost, which is open question F1 reduced to a ratio |
| `save_scale_8` | 1 | derived | 1.015 | `(w_weight + w_scale / n_group) / (w_weight + w_scale_8 / n_group)` | and what it would save on the read that dominates everything |
| `mag_operand` | 1 | derived | 128 | `range_operand` | the largest magnitude an engine operand carries |
| `sum_worst` | 1 | derived | 4.698e+08 | `n_reduce_max * mag_operand * mag_operand` | the largest a reduction can reach before rounding |
| `w_acc_need` | 1 | derived | 29.81 | `log(sum_worst) / log(2) + 1` | accumulator bits that requires, plus a sign |
| `n_exact_op` | 1 | given | 12 | — | operations in a forward pass whose result is exactly specified |
| `n_op_total` | 1 | given | 12 | — | operations in a forward pass altogether |
| `err_pass_max` | 1 | given | 0.02 | — | the most relative error a whole forward pass may accumulate before the model's output is meaningfully different. A judgement rather than a measurement, and the blueprint says so |

## 078 — What fits

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_layer` | 1 | given | 80 | — | transformer layers |
| `d_model` | 1 | given | 8192 | — | the residual stream's width |
| `n_head` | 1 | given | 64 | — | attention heads |
| `n_kv_head` | 1 | given | 8 | — | key and value heads, fewer than query heads so the cache is smaller |
| `d_head` | 1 | given | 128 | — | width of one head |
| `d_ff` | 1 | given | 2.867e+04 | — | the feedforward's inner width |
| `n_vocab` | 1 | given | 1.28e+05 | — | vocabulary entries |
| `n_ctx` | 1 | given | 4096 | — | context length the machine is provisioned for |
| `p_attn` | 1 | derived | 1.51e+08 | `d_model * (n_head * d_head + 2 * n_kv_head * d_head + n_head * d_head)` | parameters in one layer's attention: queries, keys, values and the projection back |
| `p_ffn` | 1 | derived | 7.046e+08 | `3 * d_model * d_ff` | parameters in one gated feedforward: two up, one down |
| `p_layer` | 1 | derived | 8.556e+08 | `p_attn + p_ffn` | parameters in one transformer layer |
| `p_embed` | 1 | derived | 1.049e+09 | `n_vocab * d_model` | the embedding table |
| `p_head` | 1 | derived | 1.049e+09 | `n_vocab * d_model` | the output projection, the same size |
| `n_param` | 1 | derived | 7.055e+10 | `n_layer * p_layer + p_embed + p_head` | the whole model |
| `n_reduce_max` | 1 | derived | 2.867e+04 | `d_ff` | the longest reduction anywhere in a forward pass, which 077 sizes the accumulator against |
| `C_weights` | GB | derived | 36.38 GB | `n_param * w_weight_eff` | the weights, resident, at the format in 046 |
| `C_layer_weights` | MB | derived | 441.2 MB | `p_layer * w_weight_eff` | one transformer layer's share |
| `C_layer_max` | MB | derived | 540.7 MB | `p_head * w_weight_eff` | the largest single thing a face must hold: the output projection, which is bigger than any layer |
| `C_activation` | MB | derived | 0.01638 MB | `d_model * w_act` | one token's activation vector, which is what crosses between stages |
| `C_kv_seq` | MB | derived | 671.1 MB | `2 * n_layer * n_kv_head * d_head * n_ctx * w_kv` | the cache one sequence needs at full context |
| `C_kv` | GB | derived | 18.79 GB | `C_kv_seq * batch_design` | and the whole batch's |
| `C_checkpoint` | GB | given | 0 GB | — | activation checkpoints; zero unless 076a's training is in use |
| `C_adapter` | GB | given | 0 GB | — | adapter and optimiser state; the same |
| `flop_token` | flop | derived | 1.411e+11 flop | `2 * n_param * n_flop_mac / 2` | operations to generate one token: two per weight |
| `f_kv_of_res` | 1 | derived | 0.3406 | `C_kv / C_resident` | the cache's share of residency, which is what makes capacity a surface |
| `n_ctx_at_full` | 1 | derived | 7753 | `n_ctx * (C_core_usable - C_weights) / C_kv` | the longest context that fits at this model and batch |

## 079 — The number twenty-eight

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `eta_array_util` | 1 | given | 0.82 | — | share of cycles a multiplier has an operand at the design microbatch, which is below one because a tile reload is not overlapped perfectly |
| `n_micro_min` | 1 | derived | 6 | `n_stage` | microbatches needed to keep every stage busy |
| `t_step_weights` | s | derived | 0.0009473 s | `C_weights / B_core` | time for one step's weight traffic, which does not depend on batch |
| `t_step_kv` | s | derived | 0.0004893 s | `B_kv_tok / B_core` | and its cache traffic, which does |
| `t_step_mem` | s | derived | 0.001437 s | `t_step_weights + t_step_kv` | the memory side of a step |
| `ops_achieved` | flop/s | derived | 3.611e+15 flop/s | `ops_machine * eta_array_util` | arithmetic the machine actually delivers rather than its peak |
| `t_step_arith` | s | derived | 0.001094 s | `batch_design * flop_token / ops_achieved` | the arithmetic side at the design batch |
| `batch_cross` | 1 | derived | 24.25 | `t_step_weights * ops_achieved / flop_token` | the batch at which the two are equal, ignoring the cache |
| `batch_cross_kv` | 1 | derived | 43.87 | `t_step_weights / (flop_token / ops_achieved - t_step_kv / batch_design)` | and including it. It comes out *higher*, not lower: the cache adds to the memory side in proportion to batch, so the arithmetic line has further to climb before it crosses |
| `f_batch_err` | 1 | derived | 0.1341 | `abs(batch_cross - batch_design) / batch_design` | how far the batch the machine is provisioned for sits from the crossover it derives |
| `micro_size` | 1 | derived | 4.667 | `batch_design / n_micro_min` | sequences in one microbatch at the design batch |
| `tok_per_s_1` | 1/s | derived | 1037 1/s | `1 / t_token` | tokens a second, one sequence |
| `tok_per_s_agg` | 1/s | derived | 1.949e+04 1/s | `batch_design / max(t_step_mem, t_step_arith)` | and aggregate at the design batch |
| `gain_batch` | 1 | derived | 18.8 | `tok_per_s_agg / tok_per_s_1` | what batching buys, which is the number an operator cares about most |

## 080 — The two numbers it is judged on

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `t_weights` | s | derived | 0.0009473 s | `C_weights / B_core` | reading every weight once |
| `t_cache` | s | derived | 1.748e-05 s | `B_kv_seq / B_core` | reading one sequence's cache, since the number this model is judged on is what one person waits |
| `t_arith` | s | derived | 3.907e-05 s | `flop_token / ops_achieved` | the arithmetic for one sequence |
| `t_handoff_m` | s | derived | 1.195e-08 s | `C_handoff * n_stage / B_core` | writing and reading the staging buffers |
| `t_barrier_m` | s | derived | 2.309e-07 s | `t_handoff * n_stage` | the barriers, once per stage |
| `t_stall_m` | s | derived | 1.2e-06 s | `t_stall_token * n_stage` | prefetches that arrived late |
| `t_bubble_m` | s | derived | 1.277e-05 s | `t_token * (f_fill_cost + f_bubble_cost)` | filling, draining, and sequences that ended |
| `t_token_model` | s | derived | 0.000979 s | `max(t_weights + t_cache, t_arith) + t_handoff_m + t_barrier_m + t_stall_m + t_bubble_m` | one token, everything counted: memory and arithmetic overlap, the rest do not |
| `f_t_weights` | 1 | derived | 0.9676 | `t_weights / t_token_model` | the weight term's share, which is what makes this a bandwidth machine |
| `f_overhead_tok` | 1 | derived | 0.01473 | `(t_token_model - t_token) / t_token` | what the five small terms cost above the memory-bound floor 053 derives |
| `n_model_term_d` | 1 | given | 7 | — | terms in this model, which 049 must have a counter for each of |
| `tok_s_single` | 1/s | derived | 1021 1/s | `1 / t_token_model` | tokens a second, one sequence |
| `tok_s_agg` | 1/s | derived | 1.949e+04 1/s | `batch_design / t_step` | and aggregate at the design batch, where the weights are read once for everybody |
| `t_prefill_tok` | s | derived | 3.907e-05 s | `flop_token / ops_achieved` | one prompt token during prefill, which is compute-bound |
| `tok_s_prefill` | 1/s | derived | 2.559e+04 1/s | `1 / t_prefill_tok` | prompt tokens a second |
| `gain_prefill` | 1 | derived | 25.06 | `tok_s_prefill / tok_s_single` | how much faster reading a prompt is than writing one |
| `B_ref_hbm` | bit/s | measured | 2.7e+13 bit/s | — | memory bandwidth of an accelerator with stacked memory, as the comparison |
| `C_ref_hbm` | GB | measured | 141 GB | — | and its capacity |
| `gain_bw` | 1 | derived | 11.38 | `B_core / B_ref_hbm` | the ratio the machine wins on |
| `gain_cap` | 1 | derived | 0.5103 | `C_core_usable / C_ref_hbm` | and the one it does not |

## 081 — Three nodes, not one

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `node_logic` | 1 | given | 3 | — | the compute dies' node, in nanometres as the industry names them |
| `node_array` | 1 | given | 5 | — | the memory tiers' node |
| `node_passive` | 1 | given | 65 | — | interposers, the cage's passive layers and the cold plates |
| `n_maskset` | 1 | given | 3 | — | mask sets, one per node |
| `ar_etch_max` | 1 | measured | 10 | — | the deepest aspect ratio the chosen passive line etches in silicon |
| `d0_logic` | 1/cm^2 | measured | 0.09 1/cm^2 | — | defect density at the logic node, mature |
| `d0_array` | 1/cm^2 | measured | 0.06 1/cm^2 | — | at the array node |
| `d0_passive` | 1/cm^2 | measured | 0.02 1/cm^2 | — | at the passive node |
| `cost_wafer_l` | 1 | measured | 2e+04 | — | relative cost of one wafer at the logic node, in arbitrary units so that ratios mean something and prices do not |
| `cost_wafer_a` | 1 | measured | 6000 | — | at the array node |
| `cost_wafer_p` | 1 | measured | 900 | — | at the passive node |
| `A_wafer` | cm^2 | measured | 706 cm^2 | — | usable area of one three hundred millimetre wafer |
| `n_die_wafer_l` | 1 | derived | 122 | `floor(A_wafer / A_die)` | compute dies per wafer, before yield |
| `n_tier_wafer` | 1 | derived | 44 | `floor(A_wafer / A_core_side)` | memory tiers per wafer, before yield |
| `n_plate_wafer` | 1 | derived | 26 | `floor(A_wafer / A_plate)` | cold plates per wafer |
| `n_die_stitch` | 1 | derived | 1 | `1` | exposures per compute die; one, because 042 sized the die to fit a field |
| `n_tier_stitch` | 1 | derived | 2 | `ceil(A_core_side / A_reticle)` | exposures per memory tier, which is more than one and is what 036 left open |

## 082 — The order it goes together in

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_step_assy` | 1 | given | 10 | — | steps in the sequence |
| `n_gate` | 1 | given | 6 | — | test gates: after tiers, the stack, the cage, dies, face assemblies, and final |
| `step_no_return` | 1 | given | 6 | — | the step after which a defect costs a cube rather than a part |
| `T_bond_tier` | K | given | 573 K | — | temperature the tier-to-lamina bonds are made at, step two |
| `T_bond_cage` | K | given | 553 K | — | the cage to the core, step three |
| `T_solder_face` | K | given | 523 K | — | microbumps and regulators on a face, step five |
| `T_bond_face` | K | given | 503 K | — | faces onto the cage, step six |
| `T_seal` | K | given | 423 K | — | curing the edge seals, step seven |
| `f_cover_five` | 1 | given | 0.985 | — | share of defects a face assembly test finds, which 083 uses and 084 must actually deliver |
| `n_step_before` | 1 | derived | 5 | `step_no_return - 1` | steps whose output can still be discarded cheaply |
| `n_step_after` | 1 | derived | 4 | `n_step_assy - step_no_return` | steps at which a defect scraps a whole cube |
| `f_value_risk` | 1 | derived | 0.4 | `n_step_after / n_step_assy` | share of the sequence spent past the point of no return |
| `T_ladder_ok` | 1 | derived | 1 | `1` | whether the bonding temperatures decrease monotonically; the constraints below are what establish it |

## 083 — The yield problem is the whole cost

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `y_bond` | 1 | measured | 1 | — | probability one microbump or pillar bond is good |
| `y_bond_hybrid` | 1 | measured | 1 | — | and one copper-to-copper hybrid bond, which is finer and better controlled |
| `y_handle` | 1 | measured | 0.998 | — | probability one handling and placement operation does no damage |
| `n_handle` | 1 | given | 40 | — | handling operations in a whole assembly |
| `f_cover_needed` | 1 | derived | 0.98 | `0.98` | test coverage 082's step five must reach for the numbers below to hold |
| `y_floor` | 1 | given | 0.4 | — | the least finished-cube yield that makes the machine worth building |
| `y_die_logic` | 1 | derived | 0.7803 | `(1 - exp(-A_die * d0_logic)) / (A_die * d0_logic)` | fraction of compute dies good, by the standard model for a mature process |
| `y_die_tier` | 1 | derived | 0.6428 | `(1 - exp(-A_core_side * d0_array)) / (A_core_side * d0_array)` | and of memory tiers |
| `y_die_plate` | 1 | derived | 0.7724 | `(1 - exp(-A_plate * d0_passive)) / (A_plate * d0_passive)` | and of cold plates |
| `n_die_good_l` | 1 | derived | 95.2 | `n_die_wafer_l * y_die_logic` | good compute dies per wafer |
| `n_tier_good` | 1 | derived | 14.14 | `n_tier_wafer * y_die_tier / n_tier_stitch` | good memory tiers per wafer, allowing for stitching |
| `y_stack_naive` | 1 | derived | 0.7857 | `0.99^n_tier` | what the tier stack would yield at ninety-nine per cent a tier with no redundancy, which is the alarming number |
| `y_stack_red` | 1 | derived | 0.9761 | `0.99^n_tier + n_tier * 0.01 * 0.99^(n_tier - 1)` | and with one redundant tier: all good, or exactly one bad |
| `n_bond_radial` | 1 | derived | 3.174e+07 | `n_radial_pad * n_face` | bonds in the six radial interfaces |
| `n_bond_all` | 1 | derived | 5.478e+07 | `n_bond_radial + n_bond_total + n_tier` | every bond in a cube |
| `y_rad_nospare` | 1 | derived | 0.04184 | `y_bond^n_bond_radial` | what the radial interfaces would yield with no spares at all |
| `y_rad_spare` | 1 | derived | 1 | `1 - n_bond_radial * (1 - y_bond) / n_radial_spare / n_face` | roughly, the chance the spares cover the failures; a crude bound rather than a distribution, and marked as such |
| `y_handling` | 1 | derived | 0.923 | `y_handle^n_handle` | surviving forty handling operations |
| `y_cube` | 1 | derived | 0.901 | `y_stack_red * y_rad_spare * y_array_spare * y_handling` | finished cubes per attempt, from known-good parts |
| `cost_si_cube` | 1 | derived | 1.549e+04 | `n_die * cost_wafer_l / n_die_good_l + n_tier * cost_wafer_a / n_tier_good + n_face * cost_wafer_p / (n_plate_wafer * y_die_plate)` | silicon in one cube, at known-good prices |
| `cost_cube` | 1 | derived | 1.72e+04 | `cost_si_cube / y_cube` | and per cube that works |
| `f_yield_cost` | 1 | derived | 1.11 | `cost_cube / cost_si_cube` | how much of the cost is yield rather than silicon |

## 084 — Reaching a die that is sealed inside a cube

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_scan_chain` | 1 | given | 8 | — | independent scan chains, so a whole cube is not one serial path |
| `f_scan_rate` | MHz | given | 50 MHz | — | rate the chains shift at |
| `n_scan_cell` | 1 | given | 2.4e+06 | — | scan cells in one cube |
| `n_pattern` | 1 | given | 4000 | — | patterns in the production test |
| `f_cover_scan` | 1 | given | 0.985 | — | share of defects the sealed-cube scan test finds |
| `f_cover_probe` | 1 | given | 0.995 | — | share a probe test before assembly finds |
| `n_untestable` | 1 | given | 4 | — | properties named as untestable by this blueprint |
| `t_scan_shift` | s | derived | 0.006 s | `n_scan_cell / n_scan_chain / f_scan_rate` | one full shift through the longest chain |
| `t_test_prod` | s | derived | 48 s | `t_scan_shift * n_pattern * 2` | production test time, shifting in and out for every pattern |
| `n_aux_only` | 1 | given | 1 | — | separate access paths that run on the auxiliary domain alone and share nothing with the interface they test |
| `f_cover_five_d` | 1 | derived | 0.995 | `f_cover_probe` | coverage at 082's step five, which is a probe test on an unsealed face assembly |
| `t_test_budget` | s | given | 60 s | — | the most production test may take per cube |

## 085 — A new cube on a bench

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_rung` | 1 | given | 10 | — | rungs in the ladder |
| `t_rung_1_4` | s | given | 300 s | — | powering up, locking, releasing reset and clearing the boot steps |
| `t_rung_5` | s | given | 600 s | — | writing and reading back every location with patterns |
| `t_rung_6` | s | given | 300 s | — | training every link and recording the remap counts |
| `t_rung_7_9` | s | given | 1800 s | — | one multiply, one layer, one face, one token, compared exactly |
| `t_rung_10` | s | given | 3600 s | — | sustained generation at temperature with the counters read |
| `t_fill_purge` | s | given | 1200 s | — | filling and purging the loop, and tripping the interlock |
| `n_action` | 1 | given | 10 | — | named next actions, one per rung |
| `n_criterion` | 1 | given | 10 | — | pass criteria, one per rung |
| `t_bringup_max` | hr | given | 4 hr | — | the longest bring-up may take and still fit inside one shift, so that a cube can be woken and handed over the same day |
| `remap_reject` | 1 | given | 0.5 | — | share of the spare conductors that, if already used at bring-up, rejects the cube |
| `t_bringup` | s | derived | 7800 s | `t_fill_purge + t_rung_1_4 + t_rung_5 + t_rung_6 + t_rung_7_9 + t_rung_10` | the whole procedure |
| `t_bringup_h` | hr | derived | 2.167 hr | `t_bringup` | in the unit a person plans a day around |
| `f_rung_exact` | 1 | derived | 0.2308 | `t_rung_7_9 / t_bringup` | the share spent on the exact comparison, which is where the value is |
| `n_counter_chk` | 1 | derived | 7 | `n_model_term_d` | terms of 080's model that rung ten must compare against 049's counters |
| `t_service_est` | s | derived | 1.1e+04 s | `t_service` | the cube swap time 019 carries as a target, which this procedure is what would time |

## 086 — How long it lasts

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_yr_target` | 1 | given | 10 | — | years a cube must run, continuously, replaced rather than repaired |
| `t_year` | s | measured | 3.156e+07 s | — | seconds in a year, as a symbol rather than a literal because a literal here would be dimensionless |
| `f_repl_annual` | 1 | given | 0.05 | — | annual replacement rate tolerable in a population, which is what the target is chosen against |
| `n_mech` | 1 | given | 9 | — | failure mechanisms |
| `a_silent` | 1 | given | 0.02 | — | share of the failure budget allowed to silent corruption, the smallest of the nine |
| `a_inside` | 1 | given | 0.38 | — | share allowed to everything inside the cube that has no repair path |
| `a_outside` | 1 | given | 0.6 | — | share allowed to the pump and the fluid, which are serviceable |
| `n_cyc_life` | 1 | derived | 1e+05 | `n_cyc_power` | power cycles over the life, from 018 |
| `t_life_seconds` | s | derived | 3.156e+08 s | `n_yr_target * t_year` | the life, in the unit every other blueprint's mean-time figures are in |
| `lam_target` | 1/s | derived | 1.584e-09 1/s | `f_repl_annual / t_year` | failure rate the target implies |
| `mtbf_target` | s | derived | 6.312e+08 s | `1 / lam_target` | and the mean time between failures it corresponds to |
| `lam_silent` | 1/s | derived | 3.169e-11 1/s | `lam_target * a_silent` | allocated to silent corruption |
| `lam_inside` | 1/s | derived | 6.02e-10 1/s | `lam_target * a_inside` | to everything unrepairable inside |
| `lam_outside` | 1/s | derived | 9.506e-10 1/s | `lam_target * a_outside` | to the serviceable loop |
| `mtbf_silent` | s | derived | 3.156e+10 s | `1 / lam_silent` | the mean time between silent corruptions that 040 and 069 must together beat |
| `a_sum` | 1 | derived | 1 | `a_silent + a_inside + a_outside` | the allocations, which must be the whole budget |
| `ratio_asym` | 1 | derived | 1.579 | `a_outside / a_inside` | how much more the serviceable half is allowed than the unrepairable half, which is the asymmetry 019's absence of repair justifies |
| `n_cyc_check` | 1 | derived | 1e+05 | `n_yr_target * 365 * 27.4` | power cycles at a plausible rate over the life, for comparison against what 018 assumed |

## 087 — Every seam checked in one place

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_seam` | 1 | given | 44 | — | places where a number produced in one phase is consumed in another |
| `n_seam_guarded` | 1 | given | 44 | — | of those, how many have a constraint asserting the agreement |
| `n_triple` | 1 | given | 3 | — | quantities derived by three independent routes |
| `n_alarm` | 1 | given | 8 | — | constraints asserted in the direction of alarm: always true, written so a reader meets a number rather than a claim |
| `n_count_only` | 1 | given | 5 | — | places a constraint counts where it should name, because the notation holds numbers and not lists |
| `n_seam_open` | 1 | derived | 0 | `n_seam - n_seam_guarded` | seams with no constraint on them, which is what finishing this project means driving to zero |
| `f_guarded` | 1 | derived | 1 | `n_seam_guarded / n_seam` | the share that are guarded |
| `n_bp` | 1 | solved | 84 | — | blueprints in the set -- from 103, which loads them rather than counting them from memory |
| `n_constraint` | 1 | solved | 552 | — | constraints in it -- from 103. Carried as a hand count until it was twelve short, which is the exact failure a self-describing document is prone to |
| `c_per_bp` | 1 | derived | 6.571 | `n_constraint / n_bp` | constraints per blueprint, which is a crude measure of whether any file is asserting nothing |

## 088 — What it is made from, and what that costs

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_part_cube` | 1 | given | 61 | — | pieces of silicon in one cube |
| `n_part_loop` | 1 | given | 9 | — | components in the external loop: two pumps, radiator, fan, reservoir, filter, couplings, tubing, sensors |
| `n_consumable` | 1 | given | 2 | — | consumables: the fluid and the filter element |
| `cost_assy` | 1 | measured | 3000 | — | relative cost of the assembly operations in 082, in the same arbitrary units as the wafers |
| `m_si_cube` | kg | derived | 0.06437 kg | `(n_die * A_die * t_die + n_tier * A_core_side * t_tier_si + n_face * A_plate * t_coldplate * f_solid_plate) * rho_si` | silicon in one cube by mass |
| `A_si_logic` | mm^2 | derived | 1.382e+04 mm^2 | `n_die * A_die` | silicon at the logic node |
| `A_si_array` | mm^2 | derived | 3.84e+04 mm^2 | `n_tier * A_core_side` | at the array node |
| `A_si_passive` | mm^2 | derived | 3.245e+04 mm^2 | `n_face * (A_plate + A_plate)` | at the passive node: the cold plates and the interposers |
| `A_si_total` | mm^2 | derived | 8.467e+04 mm^2 | `A_si_logic + A_si_array + A_si_passive` | all of it |
| `n_bond_cube` | 1 | derived | 5.478e+07 | `n_bond_all` | bonds in one cube |
| `cost_total` | 1 | derived | 2.02e+04 | `cost_cube + cost_assy` | one working cube, silicon and assembly, in arbitrary units |
| `f_cost_si` | 1 | derived | 0.7672 | `cost_si_cube / cost_total` | the share that is silicon at known-good prices |
| `f_cost_yield` | 1 | derived | 0.08428 | `(cost_cube - cost_si_cube) / cost_total` | the share that is yield |
| `f_cost_assy` | 1 | derived | 0.1485 | `cost_assy / cost_total` | and the share that is assembly |
| `m_total_wet` | kg | derived | 1.126 kg | `m_cube` | the finished object, from 013 |
| `V_consume_yr` | mm^3 | derived | 6615 mm^3 | `V_leak_life + V_spill_life` | fluid consumed in a year of service |

## 089 — The one page somebody reads

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_sheet_row` | 1 | given | 10 | — | sections on the sheet |
| `n_sheet_not` | 1 | given | 5 | — | sentences in the what-it-is-not section |
| `n_sheet_own` | 1 | given | 0 | — | symbols this blueprint declares of its own. It is a view, and a view that invents a number has a bug |
| `n_sheet_fig` | 1 | given | 34 | — | figures on the sheet, every one of which must resolve to a symbol declared elsewhere |
| `f_headline_bw` | 1 | derived | 11.38 | `gain_bw` | the ratio the sheet leads with |
| `f_headline_cap` | 1 | derived | 0.5103 | `gain_cap` | and the one it must not omit |
| `r_sheet_single` | 1/s | derived | 1021 1/s | `tok_s_single` | tokens a second, one sequence, as the sheet prints it |
| `r_sheet_agg` | 1/s | derived | 1.949e+04 1/s | `tok_s_agg` | and aggregate |
| `C_sheet_model` | GB | derived | 36.38 GB | `C_weights` | the largest model that fits, as weights |
| `m_sheet` | kg | derived | 1.126 kg | `m_cube` | mass |
| `P_sheet` | W | derived | 1891 W | `P_heat` | power drawn, and heat rejected, which are the same number |

## 090 — What a materials engineer receives

| symbol | unit | kind | value | from | meaning |
|---|---|---|---|---|---|
| `n_omission` | 1 | given | 7 | — | things this design does not tell a recipient, listed at the front |
| `n_content` | 1 | given | 7 | — | kinds of thing in the package |
| `n_read_step` | 1 | given | 6 | — | steps in the reading order |
| `n_software` | 1 | given | 2 | — | pieces of software assumed and not specified |
| `n_worked_eg` | 1 | given | 1 | — | worked examples of using the package as a machine |
| `n_bp_pkg` | 1 | derived | 84 | `n_bp` | blueprints delivered |
| `n_sym_pkg` | 1 | solved | 1409 | — | symbols in the ledger -- from 103. This was a hand count of something the ledger knows exactly, and it was wrong by fourteen before anybody looked |
| `n_con_pkg` | 1 | derived | 552 | `n_constraint` | constraints |
| `n_open_pkg` | 1 | solved | 0 | — | symbols still carried as targets rather than derivations -- from 103. None: the last one was 019's service time, which became the sum of nine steps rather than one number nobody could take apart |
| `n_solved_pkg` | 1 | solved | 20 | — | symbols a program produced because no expression in this notation could -- from 103 |
| `n_q_blocking` | 1 | given | 2 | — | blocking open questions in 009 |
| `n_q_open` | 1 | given | 18 | — | open questions altogether: the two blocking ones and sixteen carried. A hand count of 009's headings, and the one figure in this package that a program still does not produce |
| `f_derived` | 1 | derived | 0.5834 | `(n_sym_pkg - n_given_pkg) / n_sym_pkg` | share of the project's numbers that are worked out rather than chosen or measured |
| `n_given_pkg` | 1 | solved | 587 | — | symbols that are chosen or measured rather than worked out -- from 103: four hundred and seventy-one a person decided and a hundred and sixteen taken from a datasheet |

